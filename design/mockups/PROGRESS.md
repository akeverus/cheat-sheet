# PROGRESS — макеты design/mockups/ (леджер)

Язык: **Instrument** (гринфилд). Каденция: **эталон → аппрув → разворот**.
Правим только `design/mockups/`. Токены — `tokens.css`. Спека — `DESIGN.md`.

**АППРУВ ПОЛУЧЕН (юзер, 2026-07-11):** язык «Instrument» утверждён → **Фаза B идёт**.
Разворот по одному экрану за тик: result → session-summary → settings → stats → shell.
Незакрытые под-решения применяю по рекомендациям: hero = **A** (Newsreader serif, дефолт);
empty/done = **(b)** app-shell (при развороте); focus-order = **(c)**.

## ГЛОБАЛЬНЫЙ МАНДАТ: полная ширина (юзер, it.11) — приоритет над центрированием

Требование юзера дословно: «При просмотре веба любого разрешения не должно быть
пустот справа и слева экрана, мы должны использовать его весь».

Следствие для ВСЕХ экранов:
- Хром (шапка/бары/фоны секций) — **edge-to-edge**, только внутренний `--gutter`-паддинг.
- **НИКАКИХ** центрированных `max-width` колонок с симметричными пустыми полями.
- Горизонталь широких экранов наполняем **реальным контентом** (app-shell: основная
  зона + правый/левый рельс), а не растягиванием строки прозы. Проза держит меру по
  `ch` ВНУТРИ зоны, структура тянется на всю ширину.
- Проверка (getBoundingClientRect): `leftVoid`/`rightVoid` = только gutter и симметрично;
  хром `span` = 100% vw.
- Реализация: `.wrap { width:100%; padding-inline: var(--gutter) }` + `.focus-grid`
  (main + `.focus-aside`, брейк 1080px). Отменяет прежний it.7-фикс с центрированной
  `.col` в clamp-шелле (тот оставлял боковые пустоты).

## Открытое решение: голос героя (A/B) — ждёт выбор юзера

`focus-hero-ab.html` — санкционированный альтернативный вариант hero (пред-аппрув тик).
Тестирует главное напряжение из критики («красиво, но постер / тяжеловат для инструмента»):
- **A** — Newsreader serif, редакционный (текущий эталон). Вес + читаемость, но крупный.
- **B** — Space Grotesk sans, компактный «инструмент»: sans + плотный ритм + верхнее
  правило. Легче на мобиле, меньше драмы; риск — ближе к дженерик-tool.
Оба на одном `tokens.css`, детектор чист, обе темы + мобайл сняты. Решение = часть «ещё правки».

## Статус экранов

| Экран | Файл | Статус | Что дальше |
|---|---|---|---|
| **Экран вопроса (эталон)** | `focus-question.html` | 🟢 draft v3 · detector✅ · QA✅ | **Полноширинный app-shell (it.11):** `.wrap` edge-to-edge + `.focus-grid` (main + правый рельс с сессией/хоткеями) для active/result. 4 состояния + обе темы. Снова на аппруве языка |
| **Результат/фидбек** | `result.html` | 🟢 **Фаза B 1/5 · detector✅ · AA обе темы✅ · вьюпорты✅** | **ГОТОВ (it.17).** Полноширинный app-shell (main + рельс «разбор глубже»+действия). Оба варианта (верно/неверно). Осталось глобальное: ultra-wide мера option-строк (#4, общее с эталоном) |
| **Итоги сессии** | `session-summary.html` | 🟢 **Фаза B 2/5 · detector✅ · AA обе темы✅ · вьюпорты✅** | **ГОТОВ (it.18).** Полноширинный app-shell: main (редакц. вердикт + stat-плитки + таблица по темам + ошибки) + рельс (действия + рекомендации). Оба тона (сильная/слабая). Без hero-цифры (ban) |
| **Настройки** | `settings.html` | 🟢 **Фаза B 3/5 · detector✅ · AA обе темы✅ · вьюпорты✅** | **ГОТОВ (it.19).** Полноширинный app-shell: рельс-вкладки (Сессия/Оформление/Данные, WAI-ARIA) + панель. Оформление = сетка из 7 setting-карт (все оси персонализации). Сессия (фильтры+запуск+стрик), Данные (экспорт+опасная зона) |
| **Статистика** | `stats.html` | 🟢 **Фаза B 4/5 · detector✅ · AA обе темы✅ · вьюпорты✅** | **ГОТОВ (it.20).** Полноширинный app-shell дашборд: main (что-делать-дальше + обзор-плитки + 2 SVG-графика как Chart.js-заглушки + сортируемая таблица тем) + рельс (прогноз 7 дней + пробелы банка). Chart.js-зоны = статичные SVG-бары с id-хуками |
| **Shell/шапка** | `shell.html` | 🟢 **Фаза B 5/5 · detector✅ · AA обе темы✅ · вьюпорты✅** | **ГОТОВ (it.21).** Канонический хром: sticky-шапка (бренд/нав/тема/☰) + мобильное меню + подвал (3 кол) + skip-link + глобальная kbd-help модалка (role=dialog, aria-modal, focus-trap, ?/Esc). Демонстрируется на полноширинном домашнем экране (приветствие + быстрый старт + рельс «сегодня») |

**ЧЕКПОИНТ (it.2):** эталон чист по detector + читаем на 5 вьюпортах в обеих темах →
запрошен аппрув. **Ответ юзера: «Почти — есть правки»** → направление принято, но до
разворота нужны конкретные правки. Ждём список. Фазу B не начинать. Крон до получения
правок: НЕ гадать изменения (юзер драйвит), максимум — безопасный уже-выявленный полиш.

## Правки из UI/UX-критики (it.3) — что сделано

Источник: `~/Downloads/ui-ux-critique-focus-screen.md` (юзер). Разбито по приоритетам критика.

**P0**
- Состояния разведены: макет-переключатель (active / result / empty / done) — на экране
  ровно одно состояние (JS `[hidden]`). Убрано одновременное «вопрос + пусто».
- Код к вопросу показан сразу (`<details open>`); вопрос самодостаточен («…по коду ниже?»),
  один teal-токен вместо двух код-баннеров.
- Мобильная шапка компактная: нав → ☰, тема → иконка, одна строка (без второй «не влезло»).
- Keyboard-hints скрыты на touch/узких (`hover+pointer+min-width` гейт).

**P1**
- Мобильный заголовок меньше (clamp floor 2.1→1.75rem), hero line-height 1.2, `nowrap`
  вокруг токена, код в герое без плашки (mono+teal, не «баннер»).
- Разведены selected/correct/incorrect/focus: индикатор-кружок → галочка (форма, не
  только цвет); в разборе green-галочка / red-крест + теги «верный ответ» / «ваш ответ»;
  отдельное focus-кольцо; нативная radio-семантика.
- Основное действие — sticky нижняя панель на мобиле (safe-area), full-width.
- Убрано отладочное «состояние: нет вопросов».

**P2**
- Единый контейнер `.wrap` (60rem) для шапки и контента; читаемая колонка прижата к
  левому краю контейнера (одна композиция).
- Светлая тема: усилены тональные ступени (surface/surface-2/line/line-strong).
- Контраст мелких подписей поднят (ink-3 обе темы; бордеры карточек в тёмной).
- Кнопки без подчёркивания (`text-decoration:none`), ссылки — текстовые; радиусы едины.

**Мелочи:** «тема: авто» → контрол `◐ Авто`; `Ø` → иконка-воронка (пусто) / галочка (done);
прогресс толще (6px) и контрастнее; стрик приглушён; «Завершить сессию» → бордер-пилюля;
лейблы «Изменить фильтры» / «Начать новую тренировку»; варианты выровнены по длине/детализации.

## Маппинг на DOM-хуки (SERVER_CONTRACT.md) — экран вопроса

Что уже легло 1:1 (проводка потом тривиальна):
- `.focus-question` — вопрос-герой (в макете `h1`; при проводке под masthead → `h2`).
- `#interview-form` + `#interview-options[role=radiogroup]` + `.options`.
- `label[data-option-id] > input[name=optionId][value] + span.opt-text` — контракт §3.
- `#interview-submit` — сабмит «Проверить ответ».
- `.progress-track[role=progressbar]` (valuemin/max/now) — синк app.js `data-progress`.
- Пустое состояние: `.empty` + действия (map → `empty-action-settings`/`empty-action-retry`).
- `a.skip-link[href=#main-content]` + `<main id=main-content>` — a11y-каркас §8.

Открытые вопросы / долги:
1. ~~**IA активной сессии.**~~ **РЕШЕНО (юзер, it.3):** оставить полную десктопную
   навигацию всегда (Фокус/Аналитика/Настройки видны и во время сессии). Компактную
   session-шапку НЕ делаем. Мобильная свёртка нав в ☰ остаётся (P0-фикс).
2. **zone-chip / zone-hint** (`focusModeChipText` / `focusModeHintText`) — пока НЕ в
   макете; добавить над вариантами в полиш (после аппрува).
3. **Бейдж-буква через CSS-counter?** В контракте буква — CSS-счётчик, `optionText`
   в БД идёт как «A. текст», видимый span стрипает ведущее «A. ». В макете буква в
   `.opt-badge`; при проводке заменить на counter, span оставить чистым.
4. **Флешкарт-фаза** (`.flashcard-phase`, reveal + grade 1–4) и **study-LEARN**
   (`/study-confirm`) — первоклассные ветки §2 контракта; добавить состояниями.
5. `h1` vs `h2`: в макете active-вопрос = `h1`; при проводке под masthead станет `h2`
   (в result-состоянии макета вопрос уже `h2`, т.к. над ним вердикт — но семантику
   решим на проводке). Не баг — отметка.
6. **Touch-гейт подсказок** проверен только логикой media-query (эмулятор chrome не
   переключает pointer при resize) — на реальном тач-устройстве перепроверить.
7. **Порядок фокуса в app-shell (it.13, НА РЕШЕНИЕ ЮЗЕРУ).** После полноширинного
   релейаута (it.11) `.focus-aside` (с кнопкой «Завершить сессию») идёт в DOM ПЕРЕД
   `.focus-main`. На широких экранах рельс визуально СПРАВА, но Tab начинается с него
   (первый фокус = «Завершить сессию»), потом прыгает к вопросу слева — нюанс WCAG 2.4.3
   (порядок фокуса). Варианты фикса: (a) DOM main-first → вопрос первым по Tab на десктопе,
   но на мобиле прогресс уходит под вопрос ИЛИ кнопка «Завершить» визуально-сверху/таб-поздно;
   (b) оставить как есть (совпадает с мобильным «прогресс сверху», лёгкая десктоп-странность);
   (c) main-first + прогресс ПОД вопросом на мобиле (полностью консистентно «вопрос первый»,
   меняет мобильную раскладку). Рекомендация: (c) для чистого a11y, (a) если сохранять
   «прогресс сверху» на мобиле. Код НЕ трогал (UX-решение вынесено юзеру).
8. **empty/done vs мандат §4 (it.16, НА РЕШЕНИЕ ЮЗЕРУ).** Состояния «нет вопросов» и
   «завершено» держат `.centered { max-width: 42ch; margin: 0 auto }` — центрированная
   колонка. Хром (шапка/скаффолд-бар) edge-to-edge, но САМ блок на 2560+gutter даёт
   симметричные боковые пустоты ≈900px/сторону — формально противоречит мандату полной
   ширины. Это низкоконтентные состояния (иконка+заголовок+описание+2 кнопки), поэтому
   «чем наполнить ширину» — дизайн-решение, а не механический фикс. Варианты: (a) оставить
   центрированную фокус-композицию как ОСОЗНАННОЕ исключение мандата для confirmation-экранов
   (конвенция; но нарушает букву §4); (b) app-shell как у вопроса — слева итог/иконка,
   справа рельс с «что дальше» (следующие шаги/быстрые действия/мини-статы) → занимает
   ширину реальным контентом; (c) центрированный блок, но фон-секция несёт тонкий
   full-bleed паттерн/мету по краям. Рекомендация: **(b)** — единственный, что честно
   исполняет мандат и переиспользует уже готовый `.focus-grid`. Код НЕ трогал (это
   «трактовка empty/done», вынесенная юзеру; редизайн под ширину = спекулятивная правка §5).

## 🔁 НОВЫЙ ЦИКЛ (юзер, /loop 15m): критика макетов → улучшения → перенос во фронт

Юзер перезапустил цикл с расширенной задачей: **раскритиковать макеты, внести улучшения,
а ПОТОМ — изменения во фронтенд сервиса**. Это явно снимает прежний запрет §7 на прод-фронт
(теперь Фаза 2 разрешена этим промптом). Веду в два этапа:
- **Фаза 1 — критика+доводка макетов** (безопасно, только `design/mockups/`). Бэклог —
  в `CRITIQUE.md` (worst-first, каждая правка с причиной).
- **Фаза 2 — перенос** утверждённого языка в прод (`modules/quiz-app/.../templates,static/css,static/js`)
  по маппингам DOM-хуков (в журнале it.NN + секции маппинга) и `SERVER_CONTRACT.md`, не ломая
  контракт (id/классы/data-*/a11y/тему) и сборку/тесты. Начинаю Фазу 2 только когда макеты доведены.

**Прогресс критики (cr.N) — в `CRITIQUE.md`.** cr.1: 2 слоп-дефекта `shell` исправлены
(эмодзи→Lucide, glassmorphism-шапка→сплошная sticky); C3–C6 в бэклоге.

## 🚀 ФАЗА 2 — перенос «Instrument» во фронт (журнал f2.N)

**Юзер: «начинай Фазу 2, перенос во фронт» (2026-07-11).** Стратегия переноса —
**аддитивная, недеструктивная:** прод-фронт использует switchable-движок дизайнов
(10 штук: editorial-дефолт + linear/swiss/notion/mintlify/broadsheet/superhuman/
stripe/claude/theverge; каждый — полный набор токенов под `html[data-design="X"]`
в `tokens.css`, общий `base.css` + одни DOM-хуки). «Instrument» ложится как
**11-й opt-in дизайн** `instrument`: 0 регресса для существующих 10, SSR-дефолт
остаётся editorial, промоут в дефолт позже = 1 строка (решение юзера). Контракт
(id/классы/data-*/a11y/тему) не трогаю — все дизайны его разделяют.

**Ключевой факт:** прод и макеты используют РАЗНЫЕ словари токенов (прод: `--color-bg-*`/
`--color-text-*`/`--color-accent-*`/`--font-family-*`; макет: `--paper`/`--ink`/`--signal`).
Перенос = **маппинг OKLCH-палитры «Instrument» в прод-имена токенов** (блок как у claude).
AA by-construction: заливки → signal-STRONG (≥4.5 с near-white `-on`, как it.19),
текст/ссылки/статусы → signal/-ink (~5.2–5.6:1). Отступы/размеры/структура —
из base.css (per-design блок переопределяет ТОЛЬКО цвет+типографику+трактовку).

- **f2.3 (Фаза 2, тик 3) — статический аудит целостности порта vs SERVER_CONTRACT.**
  Read-only (сборку/дерево не трогаю; параллельная сессия активна в base.css/app.js).
  (1) **Token-parity (анти-leak):** набор токенов, которые переопределяет
  `html[data-design="instrument"]`, ТОЧНО совпал с эталонным `claude` — **50 light /
  32 dark, diff = 0 в обе стороны**. Значит ни один `--color-*`/`--font-*`, потребляемый
  base.css, не утечёт из editorial `:root` в instrument (частая ошибка неполного дизайн-
  блока). Маппинг f2.1 структурно полон. (2) **CSP (контракт §9/134):** Newsreader уже
  в whitelist `fonts.googleapis`; Space Grotesk — с того же хоста → CSP-безопасен, правка
  `SecurityConfig` НЕ нужна (CSP пиннит хосты, не семейства). (3) **Cache-bust (§9/138) —
  координационная заметка:** f2.1 намеренно ИСКЛЮЧИЛ общий v-bump-хунк (`tokens.css v=61→62`,
  сторонний, в рабочем дереве) чтобы не бандлить чужой WIP → мой коммит оставил head.html
  на `v=61`. Контент tokens.css изменён, поэтому реальная отдача instrument-блока
  возвращающимся юзерам зависит от pending v-bump дерева (любой bump инвалидирует кэш и
  отдаёт свежий tokens.css вкл. instrument). Низкий риск (новые юзеры — ок; блок и так
  применяется при выборе). Если сторонний WIP откатят — отдельный чистый тик обязан
  забампать `tokens.css ?v=` сам. (4) **Структура:** instrument — token-only, едет на
  существующей base.css-структуре, которая уже даёт ВСЕ §5-хуки (форма/опции/сайдбар/
  стрик/таблицы/kbd-help). Характерные структурные подписи «Instrument» (serif-hero,
  ring-card MCQ 2×2, sticky-masthead offset) — если нужны — лягут БЛОКОМ
  `html[data-design="instrument"] .X` в base.css (scoped, 0 регресса другим 10) отдельным
  тиком, КОГДА дерево base.css чисто (сейчас там сторонний WIP). **Итог: token-слой
  instrument верифицирован (AA-clean f2.2 + no-leak + CSP-safe); дальше — либо живой
  визуал-снапшот (bootRun, по случаю), либо instrument-signature в base.css (после
  очистки дерева).**
- **f2.2 (Фаза 2, тик 2) — детерминированный AA-гейт палитры `instrument`.**
  Проверил маппинг токенов (OKLCH→linear sRGB→WCAG, порог 4.5) по 19 критическим
  парам текст/фон/заливка в ОБЕИХ темах — **sub-4.5 = 0 / 0**. Ключевые: заливка
  кнопки `accent-on` на `accent-primary` = **6.01 light / 10.66 dark** (совпадает с
  фиксом it.19), текст-ссылка 6.93/8.96, accent-как-текст 6.93/8.96, статусы
  success 6.18/9.02 · error 6.38/6.89 · warning 5.95/10.43, выбранный (text на
  accent-wash) 14.67/11.88, самый низкий text-tertiary 5.75 light (комфортный AA).
  Скрипт `scratchpad/aa_instrument.py` (переиспользуемый). **Вывод:** маппинг
  AA-состоятелен by-construction, подтверждено расчётом без запуска приложения.
  Остальные пункты гейта (полная ширина/вьюпорты, клавиатура/фокус/reduced-motion) —
  свойства **base.css-СТРУКТУРЫ**, общей и неизменной для всех 11 дизайнов и уже
  верифицированной для 10 существующих; `instrument` меняет только цвет+шрифты →
  наследует это поведение. Инструмент-специфичный риск был AA (закрыт) + загрузка
  шрифтов (проведена в f2.1). Живой визуал-снапшот рендера (шрифты Newsreader/Space
  Grotesk, обе темы) — по случаю, когда удобно поднять bootRun; корректность-гейт уже met.
- **f2.1 (Фаза 2, тик 1) — регистрация дизайна `instrument`.** 4 точки касания +
  шрифты + доки, всё аддитивно: (1) `tokens.css` — новый блок `html[data-design="instrument"]`
  (light) + `[data-theme="dark"]` (dark, переопределяет только цвета+тени, шрифты/радиусы
  каскадят из базового блока — как claude/theverge); полный маппинг Instrument→прод-словарь.
  (2) `head.html` — `'instrument'` в массив `DESIGNS` + `instrument:{light:#FAFAFB,dark:#1B1B21}`
  в `THEME_COLORS` (mobile-chrome). (3) `head.html` — `Newsreader` (serif-display) +
  `Space Grotesk` (UI/body) в оба font-`<link>` (JetBrains Mono уже грузился = mono);
  обновил устаревший коммент «Newsreader убраны». (4) `settings.html` — 11-я seg-btn
  `data-design-pref="instrument"` (Instrument) + коммент «10→11». Плюс 2 коммента
  `tokens.css` (reading-width/density) «10→11 дизайнов». **QA:** `TemplateFragmentContractTest`
  зелёный (base.css/tokens.css структура цела, регресса нет); brace-balance ок; тест
  перечисления дизайнов НЕ исчерпывающий (проверяет 5) → не ломается. **Живой AA/визуал
  обеих тем — след. тик** (нужен bootRun + chrome-devtools). Дизайн выбираем через
  /settings → Оформление → Дизайн → Instrument (превью сразу, localStorage 'design').
  Дальше: живой QA рендера `instrument` (AA обе темы, 7 вьюпортов), затем — сверка
  структурных деталей экранов с макетами (masthead/рельсы/app-shell) там, где base.css
  расходится с «Instrument».

## ✅ ПРЕДЫДУЩИЙ ЦИКЛ ЗАВЕРШЁН (it.21, §8 стоп-условие выполнено)

**Все 6 экранов языка «Instrument» собраны, проходят QA-гейт §3 и полноширинны:**

| Экран | detector | AA (обе темы) | Вьюпорты 375–2560 |
|---|---|---|---|
| `focus-question.html` (эталон, **утверждён**) | ✅ | ✅ | ✅ |
| `result.html` (B 1/5) | ✅ | ✅ | ✅ |
| `session-summary.html` (B 2/5) | ✅ | ✅ | ✅ |
| `settings.html` (B 3/5) | ✅ | ✅ | ✅ |
| `stats.html` (B 4/5) | ✅ | ✅ | ✅ |
| `shell.html` (B 5/5) | ✅ | ✅ | ✅ |

Условие §8 (все 6 экранов проходят детектор чисто + AA в обеих темах + читаемы на всех
вьюпортах вкл. Apple/Retina + эталон утверждён) — **выполнено**. Цикл остановлен.

**Что осталось юзеру на решение (не блокирует, вынесено в «Открытые»):** голос hero A/B
(работает A serif); фокус-порядок app-shell №7; трактовка empty/done vs §4 №8;
ultra-wide мера option-строк №4. Это дизайн-решения, а не дефекты гейта.

**Следующая фаза (отдельная задача, НЕ этот цикл):** frontend-проводка утверждённого
языка в Thymeleaf-шаблоны по маппингам DOM-хуков (в каждом it.NN журнала + секции
маппинга). Токены `tokens.css` → прод-CSS. Границы §7 соблюдены: прод не тронут.

## Журнал

- **it.21** — **ФАЗА B, экран 5/5: `shell.html` собран с нуля** (канонический хром +
  домашний экран). Финальный экран разворота. **Содержит переиспользуемый каркас всех
  страниц:** sticky-**шапка** (бренд + нав Фокус/Аналитика/Настройки + kbd-help-кнопка +
  3-режимный тема-тогл + ☰), **мобильное меню-выдвижка** (data-open, aria-expanded↔aria-controls),
  **skip-link**→main-content, многоколоночный **подвал** (бренд+таглайн / Разделы / Материалы +
  мета-строка), и **глобальная kbd-help модалка** (role=dialog, aria-modal, focus-trap,
  `?` toggle / Esc close / возврат фокуса на триггер) — shell-компонент из контракта §8.
  Каркас показан в контексте на **полноширинном домашнем экране**: serif-приветствие
  (hero-clamp 2.1→3.15rem, teal-em) + быстрый старт (карта «Продолжить» на signal-wash +
  сетка 4 режимов Тренировка/Экзамен/Марафон/Изучение с Lucide-иконками) + рельс «сегодня»
  (к-повтору/выучено плитки + стрик-полоса + переходы + hint `?`). **QA (§3):** detector —
  сначала em-dash-overuse (5 в прозе); сократил до 1 (тире→двоеточие/запятая в lede и
  описаниях режимов) → **exit 0**. Также поправил 2 бага токенов до QA: `--shadow-lg`→`--shadow-lift`,
  и `?`-хендлер (кривое `!overlay.hidden===false` → чистый тогл). **AA:** OKLCH→linear свип
  35 узлов обе темы, **fails=[]** (light real-min 5.31 = mock/rail-kick/foot; brand «/трек»
  ink-3 на paper 5.75; dark min 6.67). Ложный «brand-sub 1.3» = артефакт селектора
  (`.brand span` цеплял декоративный teal `.brand-mark`, aria-hidden без текста) — исправил
  на `.brand > span:last-child`. **Лейаут §4:** 375/1280/1728/2560 — overflow 0, шапка И
  подвал edge-to-edge (void 0 обе стороны), gutter симметр. (20→56px), брейк 1080px (home-grid
  main+рельс ↔ одна колонка), на 2560 grid 95.6% (main 1944 + рельс 448), qs/mode-grid = 100%
  ширины main, подвал 3-кол на всю ширину; нав→☰ ниже 720px. **Клавиатура/a11y:** модалка —
  `?` открывает + фокус в окно, Esc закрывает + фокус возвращается на триггер, focus-trap
  (Tab зациклен), role=dialog/aria-modal/labelled; мобильное меню aria-expanded↔aria-controls;
  sticky-хедер z-40, модалка z-80. **reduced-motion** fail-safe (`.anim`/`.stagger`/pop —
  opacity 1 по умолчанию, анимации только в no-preference). **Маппинг DOM-хуков:**
  `a.skip-link[href=#main-content]`+`<main id=main-content>` (§8); тема `data-theme` на
  `<html>` + localStorage 'theme' (§9, pre-paint no-FOUC добавит проводка); `.kbd-help-overlay/
  -modal/-close` role=dialog (§8 focus-trap, `?`/Esc, хоткеи 1-9 гасятся при оверлее); нав/
  ☰ → header-хром; режимы→`#session-form` mode; «Продолжить»→buildNextQuestionHref; стрик→
  `#streak-*`. **Это последний экран — §8 стоп-условие выполнено (см. блок выше), цикл
  остановлен.**
- **it.20** — **ФАЗА B, экран 4/5: `stats.html` собран с нуля** (дашборд аналитики). По
  контракту (продовый stats.html + stats.js): секции next-actions / обзор / 2 графика /
  таблица тем / прогноз / пробелы. **Ключевое решение:** Chart.js в макет не грузится →
  зоны `#topicProgressChart` / `#topicAccuracyChart` реализованы **статичными SVG-барами**
  (viewBox-масштаб, role=img + осмысленный aria-label + скрытый `*Fallback` role=status) —
  честная заглушка Chart.js-зоны без внешней зависимости. Полноширинный app-shell: **main**
  = «что делать дальше» (3 карты, is-recommended = signal-акцент) + обзор (6 плиток
  total/выучено/к-повтору/точность/верно/ошибки) + charts-row (2 карты, 1fr 1fr ≥900px,
  стек ниже) + сортируемая **таблица тем** (Тема/Всего/Прогресс[мини-бар learned+due]/
  Точность[тон hi/mid/lo]/Сброшено/Зрелость) в `topic-table-wrap[overflow-x:auto]`; **рельс**
  = прогноз повторений 7 дней (day-row: имя+бар+счётчик, сегодня=spark) + пробелы банка
  (topic + spark-чип «N из M»). **QA (§3):** detector — сначала flat-type-hierarchy
  (хардкод `11px` c-label/c-value + `0.85em` стрелка th → кластер 11/13.6/16); фикс:
  оба литерала → `var(--fs-xs)` / убрал em-стрелку → **exit 0**. **AA:** OKLCH→linear
  свип 30 узлов обе темы, **fails=[]** (light min 5.31 = th/rail-kick/mock-tag; dark min
  6.67; тон-точности acc lo/mid/hi 5.96–6.9 light, chart-лейблы 14.4). **Лейаут §4:**
  375/768/1280/1728/2560 — overflow 0, chrome span=vw, gutter симметр. (20→56px), брейк
  1080px (main+рельс ↔ одна колонка, main над aside — верный порядок чтения), charts стек
  <900px; на 2560 grid 95.6% (main 1912 + рельс 480), **все блоки main = 100% ширины**
  (charts/table/overview/next-actions), void 0; широкая таблица (677px на 375) скроллится
  ВНУТРИ `topic-table-wrap`, страницу не рвёт (overflowX=0). **Клавиатура:** сортируемые th
  tabindex=0 + Enter/Space (toggle asc↔desc + live-region `#table-sort-status`),
  `topic-table-wrap` role=region tabindex=0 (скролл широкой таблицы с клавиатуры), 16
  фокус-целей. **reduced-motion** fail-safe (`.anim`/`.stagger` opacity 1 по умолчанию).
  **Маппинг DOM-хуков:** `#topic-stats-data` (JSON topicStatsJson) → таблица+графики;
  `#topicProgressChart`/`#topicAccuracyChart` (+`*Fallback` role=status) → Chart.js-зоны;
  `#topic-table` th.sortable[data-col][aria-sort] + `#table-sort-status` → сортировка stats.js;
  `#topic-table-wrap`[data-collapse-rows/role=region] + `#topic-table-expander` → сворачивание;
  next-actions → due/onlyWrong/weakTopics; forecast → reviewForecast[Max]; gaps → coverageGaps.
  Дальше — экран 5/5 `shell.html` (shell/шапка), затем §8 стоп-условие.
- **it.19** — **ФАЗА B, экран 3/5: `settings.html` собран с нуля** + **системный AA-фикс
  primary-кнопки на 4 файлах.** По контракту (продовый settings.html): tablist
  (Сессия/Оформление/Данные), панели `panel-*`. Полноширинный app-shell: **рельс-вкладки**
  (вертикальные на широких, горизонтальный скролл ≤1079) + **панель**. **Оформление** =
  сетка `set-grid` (auto-fit minmax 20rem) из **7 setting-карт** = все реальные оси
  персонализации: Дизайн / Тема / Размер шрифта (степпер A−A+) / Раскладка / Ширина чтения /
  Плотность / Движение (продакшн имеет 7, не 6 — включил полный набор для портируемости).
  **Сессия** = 2-кол launcher (фильтры: группа/тема/флаги-switch/порядок + запуск:
  режим/count/кнопка + стрик). **Данные** = экспорт JSON/CSV + опасная зона (полная
  error-рамка+wash, НЕ side-stripe). WAI-ARIA tabs (roving tabindex + стрелки/Home/End) и
  seg-control radiogroups (roving + стрелки) на JS. **QA (§3):** detector exit 0. **AA:**
  свип 3 панелей обе темы нашёл 1 провал — **`.btn-primary` текст (`signal-on` на `--signal`)
  = 4.40** в светлой (near-white на teal 0.55L). **Системный фикс:** фон primary → `--signal-strong`
  (темнее light / светлее dark) → **6.01 light / 10.66 dark**. Применён на ВСЕ 4 файла
  (`settings` + `result` + `session-summary` + эталон `focus-question`) — общий компонент,
  когерентность дизайн-системы; hover → `--signal-ink`. Прочее fails=[] (danger-кнопка 4.95,
  seg-checked 6.93/8.96). **Лейаут §4:** 375/768/1280/2560 — overflow 0, chrome span=vw,
  gutter симметр. (20→56px), брейк 1080px (рельс vertical↔horizontal), set-grid тянется на
  всю панель (2560: 6 кол×332px, void 0; 7 карт нечётно → штатный остаток последнего ряда,
  не void); launcher 2-кол ≥720px; сегменты не переполняют карту. reduced-motion fail-safe.
  **Маппинг DOM-хуков:** `role=tablist` `tab-session/appearance/data` + `panel-*`; seg-control
  `role=radiogroup` (design/theme/layout/reading-width/density/motion-pref) + font-stepper;
  `#filters-form` (group/topic/important/onlyWrong/shuffle/weakTopics/ordered), `#session-form`
  (mode/count), reset-options (data-confirm), export (X-Admin-Token fetch). Дальше — экран
  4/5 stats (дашборд, Chart.js-зоны заглушками).
- **it.18** — **ФАЗА B, экран 2/5: `session-summary.html` собран с нуля.** Данные из
  контракта (`summary.*`): accuracy/correctCount/wrongCount/totalQuestions/formattedDuration/
  mode/topicResults/mistakes/recommendations. **Ключевое решение:** `DESIGN.md` банит
  «hero-metric шаблон» → НЕ дженерик-дашборд с гигант-цифрой. Веду редакционно: заголовок-
  предложение (serif, данные внутри фразы) + сбалансированный ряд stat-плиток (mono, `fs-lg`,
  не одна доминанта). Полноширинный app-shell: **main** = вердикт + плитки + таблица по темам
  (тянется на всю ширину, скроллится внутри враппера) + список ошибок (кликабельны→тренировка);
  **рельс** = действия (Повторить ошибки / Новая тренировка / Скопировать+Печать) + карта
  рекомендаций. Тон точности через success/spark(warn)/error. **QA (§3):** detector exit 0;
  **AA обе темы × оба тона (сильная/слабая) fails=[]** (worst light: acc-warn 5.95, weak-acc
  5.52; dark всё ≥5.2); вьюпорты 375/768/1280/2560 — overflow 0, chrome span=vw, gutter
  симметр. (20→56px), брейк 1080px, на 2560 stat-strip = 5 плиток ×373px на всю ширину
  (void 0), таблица скроллится внутри, не рвёт страницу; reorder рекомендации→действия на
  одноколоночном (≤1079) покрыл мобайл+планшет; focus-ring + table-семантика (caption/scope=col);
  reduced-motion fail-safe. **Маппинг DOM-хуков:** `.summary-headline`/`.stat-tile[data-stat-field]`
  → summary.accuracy/correct/wrong/total/duration; `.topic-table` tbody → summary.topicResults
  (topic/total/correct/wrong/accuracy + тон-класс); `.mistake[href]` → summary.mistakes
  (клик→тренировка по теме); `.recs` → summary.recommendations; действия → onlyWrong/weakTopics
  сессия + self-contained copy/print (как в проде, app.js не грузится на /session-summary).
  Дальше — экран 3/5 settings (6 осей персонализации).
- **it.17** — **ФАЗА B, экран 1/5: `result.html` финализирован.** Аппрув языка получен
  → черновик доведён до эталонного качества и прогнан по полному QA-гейту (§3) на живом
  рендере (chrome-devtools, метрики; screenshot по-прежнему таймаутит — §3-фолбэк).
  **AA (OKLCH→linear sRGB, композитинг opacity):** найден 1 провал — `.v-sub` («B → A» /
  «+1 к стрику») **3.29** при 12px из-за `opacity:0.85` над wash. Убрал opacity → 5.24–6.53
  в обеих темах, оба варианта вердикта. Полный свип: verdict/opt-text/opt-tag/explain/
  takeaway/trace/chip/kicker/rail/btn-quiet/topic/nav/brand — **все ≥4.5, fails=[]** light+dark.
  **Лейаут (мандат §4):** 375/768/1280/1728/2560 × метрики — overflow 0, chrome span=vw
  (void 0), gutter симметр. (20→56px), брейк 1080px корректен (≤1079 одна колонка/рельс
  снизу, ≥1080 main+рельс справа). На 2560 main 1848 + aside 544 (пустот нет; ширина
  option-строк = как в утв. эталоне 1944 → не расхожусь, глобальный #4). **Reading-order:**
  в одноколоночном режиме рельс стекается вниз, но действия в DOM шли ПЕРЕД картой «глубже»
  → «Следующий вопрос» вклинивался до анализа. Фикс: `@media (max-width:1079px)` reorder
  (анализ order1, действия order2) — покрыл и мобайл (375) и планшет (768). **Клавиатура:**
  опции в result — статичные (read-only, без inputs), фокус-ring на skip/header/code-summary/
  action-кнопках/чипах. **reduced-motion** fail-safe. detector exit 0. **Маппинг DOM-хуков:**
  `.verdict` → вердикт (is-ok/is-err); `.opt--correct/.opt--incorrect` → разбор (правильность
  green/red отдельно от teal); «Следующий вопрос» → next; «В избранное» → favorite-toggle;
  `.analysis` (takeaway/trace/related-chips) → пост-ответный анализ (`#extra-analysis-content`
  из app.js-контракта, appendAnalysisBlock под действиями). Дальше — экран 2/5 session-summary.
- **it.16** — (пред-аппрув, без правок кода) пост-фикс верификация header/gutter (it.15)
  + аудит соответствия мандату §4. **detector повторно чист** на обоих правленых файлах
  (focus-question.html, result.html — exit 0), header-правка слопа не внесла. **Аудит §4:**
  active/result-состояния полноширинны (app-shell main+рельс, проверено it.11–13); **empty/done
  НАРУШАЮТ мандат** — `.centered max-width:42ch; margin:0 auto` даёт симметричные пустоты
  ≈900px/сторону на 2560. НО «трактовка empty/done» вынесена юзеру (открытый №8), а редизайн
  низкоконтентного состояния под ширину = спекулятивная правка (§5 запрещает до аппрува).
  Код НЕ трогал; занёс **открытый вопрос №8** с 3 вариантами + рекомендацией (b) app-shell.
  Прочие открытые (аппрув языка, hero A/B, фокус-порядок №7, ultra-wide мера карт) без
  изменений — жду. Держу гейт Фазы A.
- **it.15** — (юзер: «в шапке элементы слишком вплотную к боковым граням») **баг
  полноширинного релейаута.** Причина: `.site-head__inner` и `.mock-bar__inner` задавали
  `padding: var(--sN) 0` (shorthand обнулял ГОРИЗОНТАЛЬНЫЙ паддинг), перекрывая
  `.wrap { padding-inline: var(--gutter) }` → шапка/скаффолд-бар прижаты к граням (inset 0),
  тогда как контент тела держал gutter. Ранняя метрика it.11 «headVoidR 0» была этим багом,
  не признаком «ок». **Фикс (оба файла):** `padding: … 0` → `padding-block: …`, теперь gutter
  применяется и шапка ВЫРОВНЕНА с телом. Плюс поднял `--gutter` `clamp(1rem,5vw,2.5rem)` →
  `clamp(1.25rem,3.5vw,3.5rem)` (20px моб → 56px крупные) для воздуха на больших дисплеях,
  не создавая пустот. **Замеры:** brand/toggle/mock-tag/тело — все на gutter, `aligned:true`,
  overflow 0: 2560 (56px), 1728 result (56px), 375 (20px). detector чист оба файла. Хук-мап
  без изменений (только паддинг-лонгханд + токен). (пред-аппрув, tooling-заметка) попытка перегенерировать webp-матрицу под
  полноширинный эталон (шоты в `.shots/` устарели — они с центрированного it.7-лейаута).
  **chrome-devtools `take_screenshot` стабильно таймаутит эту сессию** (Page.captureScreenshot
  timed out — fullPage и viewport, с отключёнными анимациями, повторно). Захват картинки
  сломан на уровне инструмента, НЕ дизайна. Per §3-фолбэк не блокируюсь: QA полноширинного
  эталона полностью закрыт computed-метриками (it.11 лейаут, it.12 матрица 7 вьюпортов,
  it.13 AA+фокус). **Будущим тикам: НЕ ретраить скриншоты в этой сессии** — шоты помечены
  устаревшими, покрытие метриками достаточно; перегенерить когда capture починится. Кода
  не трогал; открытые вопросы юзеру без изменений (жду).
- **it.13** — (пред-аппрув QA, без правок кода) добит QA-гейт релейаута: (a) **AA новых
  карт рельса** обе темы — kbd `ink-2/surface-2` 7.72 dark / 6.56 light, rail-finish и
  hints-dd `ink-2/surface` 8.57 dark / 7.18 light — всё ≥4.5 ✓. (b) **Порядок фокуса**:
  первый Tab в active = «Завершить сессию» (рельс), затем code-summary, затем 4 radio.
  На широких экранах рельс визуально справа, но таб-первый → нюанс WCAG 2.4.3. Занёс как
  **открытый вопрос №7 (юзеру на решение)** с 3 вариантами фикса + рекомендацией; код НЕ
  трогал (UX-решение). Прочее (аппрув языка, hero A/B, ultra-wide мера карт, empty/done)
  без изменений — жду.
- **it.12** — (пред-аппрув QA, без правок кода) дозакрыта §3-матрица вьюпортов для
  полноширинного эталона (it.11). Замеры computed-метриками (screenshot-фолбэк, capture
  таймаутит): active-состояние на **768** (1 колонка 691, gutter 38∶38, рельс сверху) ·
  **1280** (grid 824+320, хоткеи `block`) · **1728** (1195+397) · **1920** (1342+442),
  плюс ранее **2560** (1944+480), **1440** result (973+331), **375** (1 кол, рельс сверху,
  хоткеи off). Во ВСЕХ: `overflow` = 0, хром `span` = 100% vw (void 0), боковой void =
  только симметричный gutter (38–40px), брейк 1080px корректен (≤1024 одна колонка,
  ≥1080 main+рельс справа), обе темы тронуты. **Мандат полной ширины подтверждён на всём
  диапазоне.** Правок не потребовалось. Открытые (юзеру на решение) не трогаю: аппрув
  языка, hero A/B, ultra-wide мера карт, empty/done-трактовка. (юзер: полноширинный мандат + переработка лупа) применён **ГЛОБАЛЬНЫЙ
  мандат полной ширины** (см. секцию вверху) к эталону `focus-question.html`:
  `.wrap` → edge-to-edge (`width:100%; padding-inline:gutter`), контент → `.focus-grid`
  (основная зона + правый `.focus-aside`, брейк 1080px). **Рельс наполняет правое поле
  реальным контентом:** сессионная карта (прогресс/стрик/«завершить») + карта хоткеев
  (десктоп) в active; сессионная карта в result. Узкий экран — одна колонка, рельс
  (прогресс) сверху, хоткеи скрыты. Отменяет it.7-центрирование. **Замеры (getBoundingClientRect):**
  2560 active — хром span 2560/void 0, grid 1944+480, gutter 40∶40 симметр., рельс справа;
  1440 result — grid 973+331, 40∶40, рельс справа, overflow 0; 375 active — 1 колонка 337px,
  overflow 0, рельс сверху, хоткеи `display:none`. **detector чист (exit 0)**, AA держится
  (карты на `--surface` — ink-2 7.18/8.57). Также создан standalone `result.html` (черновик
  Фазы B, полноширинный, правый рельс «разбор глубже»+действия, detector✅ AA обе темы✅) —
  **не развивать до аппрува.** Крон лупа перезаписан улучшенным промптом (job 6877085e):
  мандат ширины §4, Apple/Retina QA-гейт, webp-гигиена, capture-timeout фолбэк, гейт аппрува.
  Скрины: chrome-devtools `take_screenshot` таймаутил (Page.captureScreenshot timed out) —
  QA сделан computed-метриками (лейаут + AA), как предписывает фолбэк промпта. Фазу B держу.
- **it.1** — бутстрап: `tokens.css` (OKLCH light+dark), `DESIGN.md`, `PRODUCT.md`,
  `PROGRESS.md`; draft v1 эталонного экрана вопроса (состояния: вопрос+варианты,
  пусто; обе темы через тогл; motion + reduced-motion). **impeccable detect — чисто
  (exit 0)** после правки type-scale (xs/sm → 1.25) и унификации inline-code/kbd.
- **it.2** — QA эталона через chrome-devtools MCP. Скрины: 1280 light+dark, 375 light,
  768 light, 1440 light (fullPage). Вывод: рендерится чисто на всём диапазоне —
  masthead + сессионная рейка + serif-герой с `@Transactional`-бейджем + teal-выбранный
  вариант B (кольцо+wash) + keyboard-hints + пустое состояние. Dark: прохладная near-black
  бумага, teal-signal и амбер-стрик контрастны, герой читаем. Mobile 375: masthead
  переносит тогл на 2-ю строку, герой корректно wrap'ится с инлайн-бейджем, варианты
  стекаются. Токены аннотированы AA-ratios (body ≥4.5:1, крупный ≥3:1) — сверено при
  проектировании; пиксельный AA-замер отложен в полиш. Мелкий долг: keyboard-hints
  показываются на touch-вьюпортах (нет клавиатуры) → гейт `@media (hover:hover) and
  (pointer:fine)` в полиш. **ЧЕКПОИНТ ДОСТИГНУТ:** эталон detector-чист + читаем на
  5 вьюпортах в обеих темах → ПАУЗА, запрос аппрува визуального языка. Фазу B не начинаю.
- **it.3** — переработка эталона по UI/UX-критике юзера (P0/P1/P2, см. секцию выше).
  `focus-question.html` переписан: 4 состояния через макет-переключатель, единый
  контейнер, адаптивная шапка, семантика вариантов, sticky-CTA, touch-гейт; `tokens.css`
  — тональные ступени light + бордеры/контраст dark + менее залитый selected + мобильный
  hero-clamp. **detector чист (exit 0)** (унификация инлайн-code к 0.85em). QA
  chrome-devtools: active/result/empty/done × light+dark + мобайл 390 — все рендерятся
  чисто, ровно одно состояние. Снова на аппруве; открыт IA-вопрос по десктоп-session-шапке.
- **it.4** — (безопасный тик, пока юзер не прислал «ещё правки») дозакрыта полная
  матрица вьюпортов v2: 390/768/1280/1440/1920 × light+dark, состояния active/result/
  empty/done. Поломок вёрстки нет; контейнер 60rem центрируется, читаемая колонка
  прижата влево, левый край шапки=вопроса на всех ширинах; на мобиле компактная шапка +
  sticky-CTA; подсказки скрыты <641px. **IA сессии решена** (полная десктоп-нав).
  Дальнейшие пред-аппрув тики: ДЕРЖАТЬ паузу (не спекулятивные правки), ждать список
  юзера, чтобы не конфликтовать с его направлением.
- **it.5** — (санкционированный пред-аппрув артефакт «один альтернативный hero»)
  `focus-hero-ab.html`: A/B голоса вопроса — serif-редакционный (текущий) vs
  sans-компактный «инструмент», один и тот же контент и `tokens.css`. detector чист;
  сняты light/dark/мобайл. Отправлено юзеру на выбор голоса (часть «ещё правки»).
  Эталон `focus-question.html` НЕ тронут. Дальше — жду решение: A / B / другой курс.
- **it.10** — (санкц. пред-аппрув QA, без правок кода) верифицирован motion-слой
  (гейт emil-design-eng). Замеры на живом рендере: entrance `rise` (opacity 0→1 +
  translateY 8px→0) = 260ms, easing `cubic-bezier(0.22,1,0.36,1)` (ease-out-quart);
  stagger вариантов delay 40/80/120/160ms; **все** transition-длительности в документе
  ∈ {120ms, 260ms} → max 260ms, ничего >300ms; кастомный easing везде. Reduced-motion
  сделан **fail-safe**: `.anim{opacity:1}` — дефолт, анимации только под
  `@media (prefers-reduced-motion: no-preference)` → пользователи reduce получают
  статичный конечный кадр без анимаций. Правок не потребовалось.
  **Пред-аппрув QA-свип завершён:** detector✅ · AA обе темы✅ · Apple/Retina 375–2560✅ ·
  клавиатура/a11y✅ · motion/reduced✅. Дальнейшие тики до аппрува — ТОЛЬКО пауза
  (без спекулятивных правок), ждём список «ещё правок» + выбор голоса героя A/B.
- **it.9** — (санкц. пред-аппрув QA, без правок кода) верифицирован клавиатурный
  фокус и radio-семантика через **реальный ввод** chrome-devtools (Tab/стрелки, не
  программный `.focus()`). Закрыт пункт критики «не видно keyboard focus». Замеры на
  живом рендере: (1) вариант в фокусе матчит `:has(input:focus-visible)` → **двойное
  кольцо**: outline 2px teal @offset 3px + box-shadow ring-focus 3px — визуально
  ОТЛИЧАЕТСЯ от selected (wash+галочка); (2) кнопки (`.btn-primary` «Проверить ответ»)
  ловят глобальный focus-visible 2px teal @2px; (3) `role=radiogroup` +
  `aria-label="Варианты ответа"`, 4 нативных `input[type=radio]` (неявный
  `aria-checked`), стрелки ↑↓ двигают выбор нативно. Радио скрыты доступно
  (`opacity:0; 1×1px`, не `display:none`) → остаются фокусируемыми. Правок не
  потребовалось — эталон уже держит a11y. Эталон/токены не тронуты, Фазу B не начинаю.
- **it.8** — (санкц. пред-аппрув полиш) закрыт документированный долг «пиксельный
  AA-замер отложен». Прогнал измеренный WCAG-контраст по **вычисленным** цветам
  рендера (OKLCH→linear sRGB→relative luminance, обход к непрозрачному фону) для
  всех текст-узлов в active/result/empty/done × light+dark. **Итог: всё AA-чисто.**
  Найден 1 маргинальный промах — декоративный разделитель `·` в кикере темы (light
  4.43 < 4.5, на `--signal`); **исправлено** → `--signal-ink` (7.26). Худшие проходы:
  light-active кикер 5.75; dark-active нав 6.87; light-result «верный ответ» 5.19 /
  вердикт 5.67; empty/done min 4.51 (CTA на заливке). detector чист (exit 0). Скрины
  не пересобирал: правка — цвет одного `·`-глифа, на масштабе скринов неразличима.
- **it.7** — (по прямому запросу юзера) подгонка эталона под разрешения Apple/Retina
  (MBP 14"/16", Studio Display 5K — логическая ширина 1512–2560 при DPR 2). **Дефект:**
  читаемая колонка `.col` (46rem) была прижата к левому краю 960px-шелла → на ≥1600px
  правое поле раздувалось, композиция «валилась» влево, много пустоты. **Фикс** (2 строки
  CSS в `focus-question.html`): (1) `.col` центрируется в шелле (`margin-inline:auto`) —
  симметрия на всех ширинах, мера ~46rem сохранена для читаемости; (2) шелл `.wrap` мягко
  растёт `clamp(60rem, 52rem + 8vw, 72rem)` — на больших дисплеях занимает больше места,
  не «теряясь». Замеры: 1280 шелл 960 / кол симметр. 265∶280; 1728 шелл 970 / 489∶504;
  2560 шелл 1037 / 912∶912. detector чист (exit 0), регрессий на 1280 нет. **Матрикс
  переснят при DPR 2 (Retina)**, старые удалены: **26 webp** (~2.5 МБ) —
  active × **7 ширин** (375·768·1280·1440·**1728**·1920·**2560**) × light+dark = 14;
  result/empty/done × {375, **1728**} × light+dark = 12. Эталон только докручен (санкц.
  пред-аппрув полиш «доводка эталона»), токены не тронуты, Фазу B не начинаю.
- **it.6** — (по прямому запросу юзера) «скрины всего интерфейса во всех форматах,
  старые удалить, формат webp». Старые `.shots/*.webp` удалены. Снят свежий матрикс
  `focus-question.html` через chrome-devtools MCP, **22 webp** (~2 МБ), scaffold-бар
  скрыт для чистого продуктового кадра. Схема имени `focus-{state}-{viewport}-{theme}.webp`:
  - **active** — 375/768/1280/1440/1920 × light+dark = 10 (полная responsive-матрица);
  - **result / empty / done** — 375+1280 × light+dark = 12.
  Спот-проверка: mobile-dark active (компактная шапка ☰/иконка + sticky-CTA + radio-марки,
  B выбран) и desktop-light result (вердикт-баннер, A green-«верный ответ», B red-«ваш
  ответ», serif-разбор, полная нав) — оба чистые. `.shots/` gitignored (не коммитятся).
  Эталон и токены НЕ тронуты. Аппрув-гейт держится: Фазу B не начинаю.
