# Журнал улучшений Frontend / UI / UX

Лог бесконечного итеративного цикла улучшения интерфейса quiz-app. Каждая
итерация — небольшая хирургическая порция изменений, не ломающая бизнес-логику
и дизайн-систему. Работаем как senior frontend + product UI/UX + accessibility
reviewer.

## Стек и ограничения (важно для каждой итерации)

- **Стек:** Spring Boot + **Thymeleaf** шаблоны + **vanilla CSS/JS**. НЕ npm/React.
  Поэтому шаги `npm run lint/typecheck/test/build`, Storybook, Cypress из общего
  workflow — **N/A**. Проверки = Gradle-static (когда нужно) + живой chrome-devtools.
- **CSS-архитектура:** `tokens.css` (токены 10 дизайнов) + `base.css` (структура).
  `editorial.css` — мёртвый код, не подключён. Бампать `?v=N` в `fragments/head.html`
  (2 ссылки) при правке CSS; `app.js ?v=N` в `result.html` / `settings.html` /
  `focus-training.html`; `stats.js` в `stats.html`.
- **Дизайн-система:** 10 переключаемых дизайнов (editorial=дефолт, linear, swiss,
  notion, mintlify, broadsheet, superhuman, stripe, claude, theverge) × 2 темы.
  Персонализация — атрибуты `data-*` на `<html>`. Дефолт = отсутствие атрибута.
- **Живой прогон:** правки `static/`/`templates/` нужно `cp` в
  `build/resources/main/...` чтобы попали в работающий bootRun (LiveReload).
- **Запрещено:** менять бизнес-логику/API ради фронта; новые зависимости; трогать
  MCQ JSON-сидеры (их ведёт внешний auto-improve); трогать mermaid; добавлять
  контролы в минималистичный focus-режим; commit/push без явного разрешения
  (пользователь пушит сам). Деструктивные действия в живом приложении (отправка
  ответа, старт/финиш сессии) — НЕ выполнять, проверять статически.

## Методология приоритизации

Каждое улучшение оценивается по 4 осям:

```
Impact:     high / medium / low   — насколько улучшает опыт
Risk:       high / medium / low   — шанс что-то сломать
Effort:     high / medium / low   — объём работы
Confidence: high / medium / low   — уверенность что это реальная проблема
```

Берём сначала **high impact + low/medium risk + high confidence**. Каждое
изменение должно иметь причину (читаемость / сценарий / консистентность / a11y /
responsive / меньше дублирования / ближе к дизайн-системе / понятнее состояние /
меньше когнитивной нагрузки), а не «покрасивее».

## Скриншот-рутина (после каждого цикла)

`docs/ui-critique/screenshots/` (gitignored). **Старые скрины удаляются, новый
набор перезаписывается** каждый цикл. Набор: 5 страниц × 2 темы desktop
(1440×900, fullPage) + 4 ключевых mobile (375×812×2, light):

| # | Страница | URL |
|---|----------|-----|
| 1 | Главная (вопрос) | `/` |
| 2 | Фокус-тренировка | `/training?topic=java-concurrency` |
| 3 | Настройки | `/settings` |
| 4 | Аналитика | `/stats` |
| 5 | Ошибка 404 | `/<любой-несуществующий-путь>` |

Ловушки: dev-браузер копит персонализацию в localStorage (ключ `design` уводит
дизайн с дефолта) — перед набором чистить ключи и форсить `data-design=editorial`.
Компоновщик chrome-devtools иногда зависает на повторных захватах — лечится
свежей вкладкой (`new_page`). Тему ставить через `setAttribute('data-theme',…)`
после навигации (reload сбрасывает).

## Формат записи итерации

```
### Раунд N — YYYY-MM-DD — <тема>
Коммит: <hash>
Находок применено: X/Y
| # | Область | Проблема | Правка | Impact/Risk/Conf |
Скрины: обновлён полный набор (14 шт.)
```

---

## История раундов (сжато)

Полный цикл уже прошёл ~14 раундов. Лёгкие победы выбраны; остаток —
recompile-gated или decision-gated. Ключевые вехи:

- **Р1–Р5** — крупный adversarial-аудит: break-word/overflow, плюрализация
  счётчиков, iOS tap-highlight, masthead-тогглы (PE hidden-then-reveal),
  гуманизация тем, print-паритет. Коммиты `eef91360`…`dd1cae83`, `0fca7a91`.
- **Р6–Р9** — cross-page/таблицы/скролл/transition/цвет; busy-state + aria-live
  politeness; print-паритет + терминология; disclosure-чеврон + error-copy id-leak.
  Коммиты `f2a285a6`, `69dd62c8`, `3a59fd6c`, `7149f7ec`.
- **Редизайн-волна** — 10 переключаемых дизайнов, контейнированные сцены вместо
  full-bleed, переключаемые раскладки (`data-layout`), 7 осей персонализации
  (раскладка/дизайн/тема/шрифт/ширина-чтения/движение/плотность), `/settings` во
  вкладки, эйбрау-кикер темы на «Фокусе». Коммиты `8cce67b8`, `7bf01599`,
  `f1ad567f`, `910d44eb`, `0eb762a5`, …
- **A11y-проходы** — keyboard-scroll таблиц, overscroll модалок, focus-return,
  forced-colors + reduced-transparency, тематизация browser-autofill,
  доступная admin-модалка вместо `window.prompt`. Коммиты `4c2b43b1`,
  `b21acbea`, `e4586d53`, `653c706e`.

### Раунд 14 — 2026-06-23 — a11y / print / perf
Коммит: `6dc4c3cd` · Находок применено: 8/8
- `aria-describedby` на все контролы настроек (seg-controls, шафл, порядок,
  font-stepper) → подсказки связаны со скрин-ридером.
- `@media print`: `break-inside: avoid` для результата/таблиц/итогов → не рвём
  карточки/строки между страницами.
- Chart.js `defer` + комментарий о порядке загрузки stats.js.
- `localStorage` в `initSessionFormSync` обёрнут в try/catch → приватный режим
  не роняет восстановление счётчика.
- Удалены no-op `<link rel=preload as=style>` (render-blocking same-origin CSS).

### Цикл 2026-06-30 — обслуживание скриншот-набора
- Полный набор из 14 скринов пересобран начисто в дефолтном дизайне editorial
  (старый набор был снят при дрейфе на superhuman из-за грязного localStorage —
  удалён и переснят). Визуально р14 ничего не сломал. Зафиксирована ловушка
  дрейфа дизайна и процедура чистки персонализации перед набором.

### Раунд 15 — 2026-06-30 — empty/error-states + микрокопирайтинг
Находок применено: 3/6 (adversarial-скептик отклонил 3 как ложные/ломающие)
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | Empty-state «Фокус» | Один `<h2>` «Вопросы временно недоступны» над 5 взаимоисключающими `th:if`-ветками; противоречит ветке «Сессия завершена» (зелёный CTA «Посмотреть итоги») | `focus-training.html:162` → «Сейчас нет вопросов» (нейтрально для всех веток) | medium/low/high |
| 2 | Форма фильтров `/settings` | Кнопка `Применить` неинформативна и расходится с `/stats` («Применить фильтры») | `settings.html:95` → «Применить фильтры» (паритет + самоописательное имя для скрин-ридера) | low/low/high |
| 3 | Поиск-empty `/stats` | Текст обещал «сбрось фильтры», но контрола сброса не было | `stats.html:66` → ссылка `<a th:href="@{/stats}">сбрось поиск и фильтры</a>` (голый `/stats` = полный сброс) | medium/low/high |

Доп.: тест-сентинелы `NoAiModeStartupIntegrationTest` (строки 37/57/61) обновлены
под новый текст empty-state, защитный смысл сохранён (`not(containsString(…))`).
Отклонено скептиком: (a) ложное «двойной POST /finish роняет на error-страницу»
(бэкенд null-safe — путь прослежен); (b) патч, ломавший confirm-гард `/reset-options`;
(c) хардкод label, который на деле динамический per-mode + ломал тесты.
Live-verify: `/settings` кнопка = «Применить фильтры»; `/stats?q=<no-match>` ссылка
сброса рендерится, `href=/stats`. Скрины: обновлён полный набор (14 шт.).

### Раунд 16 — 2026-07-01 — Danger Zone для деструктивного сброса (SE-4)
Коммит: (см. git) · CSS v=52→53 · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `/settings` → вкладка «Данные» | Деструктивный «Сбросить банк вариантов» отделён от безопасного экспорта лишь тонкой нейтральной линией → необратимое действие легко спутать с экспортом | Обёрнут в `.danger-zone`: заголовок «⚑ Опасная зона», error-wash фон + **полная** error-рамка (НЕ side-stripe — impeccable-ban), `+«Действие необратимо.»`; собственный divider формы погашен. Confirm-гард на кнопке не тронут | high/low/high |

### Раунд 19 — 2026-07-01 — Empty-state «нет данных по темам» на /stats (AN-8)
Коммит: (см. git) · ШАБЛОН-ONLY (без бампа) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `/stats` при пустом `topicStats` | Все секции данных (графики/таблица/прогноз/пробелы) гейтятся на непустой `topicStats`. При пустом (новый пользователь без ответов либо фильтр без совпадений) страница показывала лишь фильтр-карту + сетку нулей и пустоту — без объяснения и пути дальше. Search-empty был, data-empty — НЕ был | Добавлен `<section class="card stats-empty">` сразу после stats-grid: «Пока нет данных по темам» + объяснение + primary CTA «Начать тренировку →» + (если активен фильтр) «Сбросить фильтры». Условие — ТОЧНАЯ инверсия гейтов секций ниже, под-выражения reused из этого шаблона | medium/low/high |

Реализация: `stats.html` — новая секция, гейт `${(topicStats == null or topicStats.isEmpty())
and (searchQuery == null or #strings.isEmpty(searchQuery))}` (последнее — чтобы не было
двух «пусто» подряд с search-empty). Reuse классов `.card`/`.muted`/`.mt-0`/`.btn next-btn`/
`.btn secondary-btn` → 0 CSS/JS, **бамп версии НЕ нужен** (чистый template-текст). Reset-link
гейтится на активный фильтр (`selectedGroup`/`filter?.topic`/`importantOnly`/`onlyWrong` —
проверенные выражения). **Live-verify (curl):** /stats=200 (шаблон валиден, нет expr-ошибки);
**с данными empty-state СКРЫТ (0 вхождений)**; таблица «Детализация по темам» рендерится;
все фильтр-комбо (`?important`/`?onlyWrong`/`?topic&group`)=200 (нет expr-ошибки ни в одном
пути). **Положительный рендер (пустой topicStats → секция видна) — by-construction:** dev-банк
непустой и topicStats игнорирует фильтры (пробовал important/onlyWrong/несущ.-топик — таблица
всегда есть) → пустое состояние недостижимо живьём; условие = точная булева инверсия рабочих
гейтов + reused-выражения → высокая уверенность по построению. **Без визуальной дельты в
стандартном наборе** (empty-state не рендерится при данных) → скрины не перезаливал.

### Раунд 18 — 2026-07-01 — Паритет приглушения unselected-wrong на focus-page (AF-7)
Коммит: (см. git) · CSS v=54→55 · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | Пост-проверочная иерархия вариантов на focus-page | `.option-other` (невыбранный неверный) приглушён `opacity:0.85` ТОЛЬКО под `.result-page` (no-JS фоллбэк). На focus-page app.js при наличии пояснения снимает `.option-dimmed` (0.55) и вешает `.option-other` → **без общего правила вариант прыгал на полную 1.0** → пояснение делало неверный вариант ЗАМЕТНЕЕ (назад к шуму, ломало иерархию «вот правильный / вот твой выбор») | Un-scope правила из `.result-page` → `html[data-design] .option-other{opacity:.85}` (тот же паритет-фикс, что у `.option-status-label`). result.html не меняется (и было 0.85); focus-page теперь 0.85 (рецессивно к correct/your-wrong 1.0+wash, чуть читаемее пустого dimmed 0.55) | medium/low/high |

Реализация: `base.css` — одно правило un-scoped (строка 2101), комментарий разводит
семантику dimmed(0.55, без контента)/other(0.85, с пояснением). **Live-verify
(chrome-devtools DOM-probe, body.focus-page, editorial light, base.css?v=55):**
correct=1.0, wrong=1.0, **other=0.85 (был 1.0 — баг устранён)**, dimmed=0.55 (не
тронут). **Без визуальной дельты в стандартном 14-наборе** (пост-проверочное
состояние focus-page требует POST /answer = SRS-загрязнение + непустой банк) →
набор не перезаливал; верификация DOM-probe/CSSOM.

### Раунд 17 — 2026-07-01 — Мягкий inline-`code` в вариантах (answer-fairness, AF-5)
Коммит: (см. git) · CSS v=53→54 · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | Текст варианта ответа (`.options`) | Глобальный `:not(pre) > code` даёт залитый beige-«чип»; в технических MCQ верный термин часто в бэктиках → залитая плашка «выпрыгивает» и подсвечивает correct ещё ДО проверки (UI усиливает контентный перекос; задача UI — НЕ усиливать) | Гасим ТОЛЬКО заливку+паддинг в тексте выбора: `.options label code` (фокус, до проверки) + `.options > [role=listitem] > span code` (результат) → `background:transparent; padding:0; color:inherit`. Моноширинный шрифт остаётся (код читается формой, не плашкой); `overflow-wrap:anywhere` (long-token guard) НЕ переопределён | medium/low/medium |

Реализация: `base.css` — новое правило сразу после heading-softening блока
(специфичность `0,2,1` бьёт глобальный `0,1,2`, но переопределяет лишь bg/padding/color
— `overflow-wrap` наследуется от глобального). **Прицел узкий, осознанно:** НЕ трогаем
`.option-explanation` (там проза — чип помогает читать) и не трогаем заголовки (у них
своё softening-правило выше). Комментарий в коде разводит мотивацию: heading = ЧИТАЕМОСТЬ
(48px-чип доминирует), вариант = ЧЕСТНОСТЬ (плашка-tell). Отменяет более раннее решение
«в вариантах чип ОСТАВЛЯЕМ — там читается» (то было до явной инструкции юзера про
«мягкий inline-code» в fairness-контексте; читаемость сохранена монофоном).
**Live-verify (chrome-devtools, DOM-probe + CSSOM, editorial light):** в empty-инстансе
банк пуст → реальных `.options` нет, поэтому инжектил синтетические опции + контроли.
focus-option code: bg `rgba(0,0,0,0)` + padding 0 + JetBrains Mono + overflow-wrap
`anywhere`; result-option code: то же; **контроли — чип СОХРАНЁН:** explanation-code и
prose-code оба bg `rgb(227,218,204)` + padding 6.3px (не регрессировали). Тема-инвариантно
(transparent/0/inherit). **Без визуальной дельты в стандартном 14-скрин наборе** (ни одна
из 5 reachable-страниц не рендерит вариант-с-кодом в пустом dev-банке) → набор не
перезаливается (политика: не плодить байт-идентичные картинки на невизуальных циклах).

### Раунд 16 — 2026-07-01 — Danger Zone для деструктивного сброса (SE-4)
Коммит: (см. git) · CSS v=52→53 · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `/settings` → вкладка «Данные» | Деструктивный «Сбросить банк вариантов» отделён от безопасного экспорта лишь тонкой нейтральной линией → необратимое действие легко спутать с экспортом | Обёрнут в `.danger-zone`: заголовок «⚑ Опасная зона», error-wash фон + **полная** error-рамка (НЕ side-stripe — impeccable-ban), `+«Действие необратимо.»`; собственный divider формы погашен. Confirm-гард на кнопке не тронут | high/low/high |

Реализация: `settings.html` — `<div class="danger-zone">` + `<h3 class="danger-zone-title">`
(иконка `#i-flag` из спрайта, `aria-hidden`); `base.css` — `.danger-zone` (`--color-status-error-wash`
+ `--color-status-error` 1px full-border + `--border-radius-md`), `.danger-zone-title`
(error-цвет, semibold), гашение `border-top/padding-top` формы внутри зоны. Токены
error-wash есть на все 10 дизайнов × 2 темы → зона тематизируется автоматически.
Live-verify (chrome-devtools CSSOM, обе темы editorial): light bg `#F4E0DA` + border
`rgb(179,38,30)` 1px; dark bg `#392523` + border `rgb(236,133,128)` 1px; заголовок
error-цвет; форма внутри зоны border-top=0. **Урок верификации:** app-дефолт =
`data-design="editorial"` (явный атрибут, не отсутствие) → в скрин-рутине ставить
`setAttribute('data-design','editorial')`, НЕ `removeAttribute` (тот гасит правила
`html[data-design] …` в момент чтения computed-style). Скрины: полный набор (14 шт.),
вкладка «Данные» активна в settings-шотах — показывает новую зону.

### Раунд 20 — 2026-07-01 — `aria-hidden` на декоративной accuracy-bar (AN-9)
Коммит: (см. git) · версии не бампались (правка HTML-фрагмента) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `fragments/stats-grid` → `.accuracy-bar` (общий фрагмент: /stats, /result, /settings) | Полоса точности — осмысленная data-viz (ширина заливки = `Точность N%`), но без `role`/`aria`/лейбла. Хуже: заливка 32,6% стоит СЛЕВА (под «Всего/К повтору»), а не под своим лейблом «Точность» (6-я колонка) → для скринридера безымянная сбивающая графика. Значение и так озвучено текстовой плиткой «Точность N%» сразу выше | Добавлен `aria-hidden="true"` на `.accuracy-bar` → AT чисто игнорирует избыточный визуальный дубль; значение несёт текстовая плитка. Рекомендованный паттерн для data-viz, дублирующей соседний текст (не `role=progressbar` — он бы озвучивал точность дважды). Комментарий во фрагменте объясняет мотив | medium/low/high |

Реализация: `fragments/stats-grid.html` — атрибут `aria-hidden="true"` на контейнере
`.accuracy-bar` (одна правка чинит все 3 включения фрагмента консистентно). Чистый
HTML-текст → версии CSS/JS НЕ бампались. **Live-verify (chrome-devtools, /stats,
editorial light, после `cp` в build):** `aria-hidden="true"` присутствует; полоса
ВИЗУАЛЬНО рендерится без изменений (height 4px, заливка 358px `rgb(217,119,87)`);
текст «Точность 32,6%» на месте → значение для AT сохранено, потеряна только
безымянная дублирующая графика. **Без визуальной дельты** (sighted-вид идентичен) →
14-скрин набор не перезаливается (политика невизуальных циклов). Почему не
`role=progressbar`: значение уже в тексте рядом → progressbar-роль дала бы двойное
озвучивание; декоративный дубль корректнее скрыть. Токены полосы (`--color-bg-tertiary`
трек / `--color-accent-primary` заливка) и так тема-адаптивны — отдельная проверка
dark не требовалась. Примечание: на /result и /settings полоса невидима (нет
`.result-page/.settings-page .accuracy-bar` height-правила) — `aria-hidden` там
безвреден (скрывает пустой 0-height div). AN-3 («поиск отделён странной линией»)
НЕ переоткрывается: вертикальный hairline + центрирование поиска — осознанное
решение с задокументированным rationale в `base.css:1476-1480` (пустота = намеренный
воздух), переоткрытие было бы churn.

### Раунд 21 — 2026-07-01 — «N из M вопросов к повтору» в today-hero (AN-2)
Коммит: (см. git) · версии не бампались (правка HTML-фрагмента) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `fragments/today-widget.html` → `today-hero` (крупная карточка верха /settings) | today-**chip** (компактный, /focus) ОСОЗНАННО показывает «N из M к повтору» (знаменатель = честная пропорция, а не интимидирующая 4-5-значная стена — задокументировано в коде, lines 49-54). А today-**hero** (САМАЯ заметная поверхность) показывал голое «9886 вопросов к повтору» — менее продуманный паттерн на более видном месте | Перенёс тот же паттерн+гард на hero-лейбл: `total > due ? 'из {total} {plural} к повтору' : '{plural} к повтору'`. Существительное у hero сохранил (есть место, читается полнее терсного chip). Большое число (`data-stat-field=due`) и live-обновление не тронуты | medium-high/low/high |

Реализация: `fragments/today-widget.html` — изменено `th:text` у `.today-hero-due-label`
(было `@plural.pick(stats.due,…) + ' к повтору'` → стало тернар с гардом `stats.total >
stats.due`, мирроринг гарда chip'а line 57). Чистый Thymeleaf-текст → версии CSS/JS НЕ
бампались. **Гард-ветка (`total == due` → голое «N вопросов к повтору», чтобы не
подсвечивать «ты ничего не сделал») — прямой мирроринг проверенного гарда chip'а, верна
by-construction** (те же бины `@plural`, та же логика). **Live-verify (chrome-devtools,
/settings, после cp в build, обе темы editorial):** big «9886» + label «из 9888 вопросов
к повтору» = читается «9886 из 9888 вопросов к повтору»; 1 строка, без переноса; light+dark
идентично (тема-инвариантно, текст наследует цвет). **Mobile 375×812 (emulate, true vw=375):**
label 1 строка, figure 1 строка, **БЕЗ горизонтального переполнения** (scrollW 375 == clientW
375) → длинный лейбл responsive-safe. **ВИДИМАЯ дельта (только /settings — hero рендерится
лишь там; home/training используют НЕтронутый chip):** пересняты 3 settings-скрина
(3-settings-light/dark + 7-settings-mobile-light); остальные 11 НЕ перезаливал (byte-идентичны,
hero там нет — политика «не плодить байт-идентичные»). md5-dedup: 0 stale-кадров, 14 шт total.
Content-verify 3-settings-light: settings-page + «9886 из 9888» (не stale). AN-id: STA-2/AN-2 → DONE.

### Раунд 22 — 2026-07-01 — Консистентность `<title>` страниц (HEAD-1)
Коммит: (см. git) · версии не бампались (правка текста шаблонов) · Находок применено: 1/1 (2 файла)
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `<title>` страниц (`settings.html`, `session-summary.html`) | 3 вторичные страницы следуют паттерну «<Имя> — Подготовка к собеседованию» (stats=«Аналитика — …», result=«Результат — …», error=«Ошибка — …»), но 2 выбиваются: settings=«Настройки сессии», summary=«Итоги сессии» — БЕЗ суффикса-имени приложения → вкладки/история/закладки/SR-озвучка заголовка не идентифицируют приложение, несогласованно | Привёл 2 outlier'а к паттерну: «Настройки сессии — Подготовка к собеседованию», «Итоги сессии — Подготовка к собеседованию» (первая часть = header pageTitle, как у stats). Home (`/`, `/training`) оставлен «Подготовка к собеседованию» — для домашней голое имя приложения = конвенция | medium/low/high |

Реализация: `settings.html`+`session-summary.html` line 3 — параметр `title=…` у
`fragments/head :: head(...)`. **Title задаётся на УРОВНЕ ШАБЛОНА** (литерал в fragment-параметре),
НЕ в контроллере → правка чисто фронтовая, без Java/рекомпиляции (curl подтвердил:
home-title — это template-литерал, не model-attr). Чистый текст шаблона → версии НЕ бампались.
**Live-verify (curl, после cp в build):** /settings = «Настройки сессии — Подготовка к
собеседованию» ✓; /stats/result/error паттерн целы; / и /training = «Подготовка к
собеседованию» (home-конвенция). session-summary — тот же механизм (литерал в том же
fragment-вызове), верно by-construction (достижим только через POST /finish = SRS-загрязнение,
не курлил; settings с идентичным механизмом верифицирован живьём). **НЕвизуально для viewport**
(title в chrome браузера, не в области страницы) → 14-скрин набор НЕ трогал (актуален).
Тесты не ассертят голые старые заголовки (grep src/test = 0) → ничего не сломано.
**Урок: titles в этом проекте — template-литералы (head-fragment param), фиксятся без Java;
паттерн «<header-name> — Подготовка к собеседованию», home = голое имя приложения.**

### Раунд 23 — 2026-07-01 — body-копия 400 на error-странице (ERR-1)
Коммит: (см. git) · версии не бампались (правка текста шаблона) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `error.html` `.error-body` | Заголовок ошибки различает 6 кодов (404/403/401/**400**/≥500/else), а body — только 4 (404/403/401/else). Для **400 «Некорректный запрос»** заголовок говорит «клиент прислал плохой запрос», а body падает в else с СЕРВЕРНОЙ рамкой «Запрос не дошёл до результата… посмотри в логи приложения» — несоответствие (400 = проблема запроса клиента, не серверных логов) | Добавил 400-ветку в body-тернар (мирроринг 400-ветки заголовка): «Запрос составлен неверно — проверь введённые данные и попробуй ещё раз.». Теперь гранулярность body = гранулярности заголовка; else остаётся 5xx/generic | low-medium/low/high |

Реализация: `error.html` lines 27-31 — добавлена `(code == 400 ? '…' : …)` в `th:text`
body-тернар (+1 вложенность, +1 закрывающая скобка; парность проверена: 3 open / 3 close).
Чистый текст шаблона → версии НЕ бампались. **Live-verify (curl после cp в build):** /404
по-прежнему http=404, заголовок «Такой страницы нет» рендерится (тернар парсится — будь
синтаксис битый, Thymeleaf отдал бы 500, а не 404-заголовок); новый литерал «Запрос
составлен неверно» присутствует в served-шаблоне (grep build = 1). **400-ветка верна
by-construction** (мирроринг заголовочной 400-ветки; реальный 400 не триггернуть без
malformed-запроса). **404-вид НЕ изменился** (заголовок/body 404 те же) → стандартные
5-error-скрины валидны, НЕ перезаливал; 400-копия видна лишь на реальном 400 (не в наборе).
Тесты не ассертят error-копию. **Урок: error.html — статус-aware (заголовок+body+nav под код);
при правке держать гранулярность body == гранулярности заголовка.**

### Раунд 24 — 2026-07-01 — Гуманизация дат прогноза повторений на /stats (AN-13)
Коммит: (см. git) · версии не бампались (правка шаблона + inline-PE-скрипт) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `stats.html` секция «Прогноз повторений на 7 дней» | `.forecast-day` рендерил **сырую ISO-строку** из БД — `2026-07-01`, `2026-07-06`. ISO `yyyy-MM-dd` — машинный формат разработчика, протёкший в русскоязычный UI; человеку не говорит «сегодня это или через неделю». Источник — `record ForecastDay(String day,…)`: дата уже String из SQL, не temporal → починка «в источнике» = Java/SQL (recompile-gated) | (а) `<span>`→`<time th:datetime>` — корректная HTML5-семантика даты, ISO остаётся машинно-читаемым в `datetime`; (б) inline PE-скрипт гуманизирует видимый текст через `Intl.DateTimeFormat('ru-RU')`: `Сегодня` / `Завтра` / `6 июля`, плюс `title` с днём недели («среда, 1 июля»). Без recompile, локаль-независимо (явная ru-RU, не локаль сервера) | medium/low/high |

Реализация: `stats.html` — строка 233 `<span class="forecast-day" th:text>` → `<time class="forecast-day"
th:datetime="${day.day()}" th:text="${day.day()}">`; после `</section>` прогноза добавлен
самодостаточный inline `<script>` (паттерн PE как в session-summary/header): берёт `.forecast-day`,
парсит ISO регэкспом `^(\d{4})-(\d{2})-(\d{2})$` (guard), считает diff в днях от полуночи сегодня →
`Сегодня`/`Завтра`/`Intl ru-RU 'd MMMM'`; `title` = `weekday long, d MMMM`. Guard-цепь: `!days.length`,
`!window.Intl`, regex-miss, `isNaN` → graceful (остаётся ISO). **Live-verify (cp в build + reload оба
состояния):** DOM-проба — `datetime` сохраняет `2026-07-01`/`2026-07-06`, видимый текст `Сегодня`/`6 июля`,
`title` `среда, 1 июля`/`понедельник, 6 июля` (дни недели сверены — верны для реальных дат); консоль
чиста (единственная ошибка — пред-существующий CSP-блок Chart.js `.js.map`, не моя). Скрин-дельта НИЖЕ
фолда (после 2 графиков + длинной таблицы), но скрины fullPage → перезаписал `4-stats-{light,dark}` +
`8-stats-mobile-light` (mobile content-verify: days=[`Сегодня`,`6 июля`], vw=375); остальные 11 не трогал.
CSS `.forecast-day` класс-таргет (не `span.`-квалиф.) → `<time>` безопасен; гуман-лейблы (6-7 симв.)
**короче** ISO (10 симв.) → в 100/84px-колонку влезают без overflow. **Урок: SQL-проекции дат как String
утекают ISO в UI; чинить на клиенте через `<time datetime>` + `Intl` с явной локалью, не полагаясь на
локаль сервера; PE-фоллбэк (ISO) однозначен. Аудит шапки в этом раунде: 3 тоггла (layout/design/theme)
полностью обновляют aria-label И title на цикле через `sync()`+change-события — НЕ stale; `aria-pressed`
только у theme (бинарный флип), у layout/design нет (3-state cycle, pressed семантически неверен) —
осознанно, не дефект.**

### Раунд 25 — 2026-07-01 — Объяснение метрики «Точность» в таблице тем /stats (AN-11/STA-11)
Коммит: (см. git) · версии не бампались (чистый Thymeleaf `title`/`th:title`) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `stats.html` колонка «Точность» в таблице тем | Соседние jargon-колонки **Сброшено** и **Зрелость** имеют поясняющие `title`, а **Точность** — нет (несогласованно). Метрика реально неоднозначна: точность = `correct/(correct+wrong)` (доля среди **отвеченных**), но рядом колонка «Всего N» провоцирует прочтение «X% от всех N вопросов». Сама дробь скрыта — пользователь видит «40,0%», но не «2 из 5»; «—» (нет ответов) тоже не объяснён | (а) `title` на `<th>Точность` — паритет с Сброшено/Зрелость: «Доля верных ответов среди отвеченных (не из всех вопросов темы)»; (б) `th:title` на ячейку раскрывает дробь: «Верных ответов: N · Отвечено: M» (числа после двоеточий → grammar-safe для любых N/M, без проблем согласования рус. числительных), а для «—» → «Пока нет ответов по этой теме» | medium/low/high |

Реализация: `stats.html` — строка 150 `<th data-sort-label="Точность">` +`title="Доля верных ответов
среди отвеченных…"`; ячейка `td.accuracy-cell` (строки 175-181) +`th:title` тернар: пусто→«Пока нет
ответов по этой теме», иначе «Верных ответов: ${item.correct} · Отвечено: ${item.correct+item.wrong}».
`item.correct/.wrong` уже использовались в classappend+text → новых данных/API НЕ нужно. **Live-verify
(cp в build + reload editorial):** header.title присутствует и совпадает по стилю с siblings; ячейка
40,0% → «Верных ответов: 2 · Отвечено: 5» (2/5=40% ✓ математика верна, скрытая дробь раскрыта); «—»-ячейки
→ «Пока нет ответов по этой теме»; консоль чиста (только пред-сущ. CSP `.js.map`). **Невизуальный цикл:**
`title` виден лишь при hover → в статик-скрине дельты НЕТ → 14-набор НЕ перезаливал (политика невизуальных
циклов; stats-скрины из р24 по статике актуальны). Чистый Thymeleaf-текст → версии НЕ бампались. **Урок:
при добавлении поясняющих `title` на jargon-колонки держать ПАРИТЕТ — если 2 из 3 «странных» колонок
объяснены, третья тоже должна; процентная метрика без видимого знаменателя/числителя двусмысленна —
раскрывать дробь в `title` ячейки, формулируя с числами после двоеточия (рус. числительные не
согласовывать).**

### Раунд 26 — 2026-07-01 — Mode-специфичный CTA запуска сессии на /settings (SET-12)
Коммит: (см. git) · app.js v=51→52 (3 шаблона: settings/result/focus-training) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `app.js initSessionModeForm` — CTA запуска на /settings | Кнопка старта была бинарной: `isTraining ? 'Начать тренировку' : 'Начать сессию'`. 4 из 5 режимов (Изучение/Флешкарты/Экзамен/Интенсив) сваливались в **дженерик «Начать сессию»**, который не говорит, ЧТО запускается. Принцип микрокопирайтинга: label кнопки = что произойдёт после клика (verb+конкретный объект) | Карта `MODE_CTA` (ключи = enum SessionMode): TRAINING→«Начать тренировку», STUDY→«Начать изучение», FLASHCARD→«Начать флешкарты», EXAM→«Начать экзамен», MARATHON→«Начать интенсив»; `MODE_CTA[value] \|\| 'Начать сессию'` — дженерик остаётся defensive-фоллбэком для неизвестного значения. Падежи вручную (тренировка→тренировку — единств. неноминатив) → карта, не lowcase(option) | medium/low/high |

Реализация: `app.js:234-246` `initSessionModeForm` — `const MODE_CTA={…}` (5 ключей), `startBtn.textContent =
MODE_CTA[modeSelect.value] || 'Начать сессию'` (было тернарное isTraining). `isTraining` сохранён для
`countField.classList.toggle('hidden',…)`. **app.js-правка → бамп v=51→52** в `result.html`/`settings.html`/
`focus-training.html` (контракт версии). **Live-verify (cp app.js+3 шаблона в build, reload /settings,
недеструктивно меняю select без сабмита):** все 5 режимов дают свой лейбл (Тренировка→«Начать тренировку»,
Изучение→«Начать изучение», Флешкарты→«Начать флешкарты», Экзамен→«Начать экзамен», Интенсив→«Начать
интенсив»); BOGUS-значение → фоллбэк «Начать сессию» ✓; консоль чиста (app.js без syntax-error). **Delta
interaction-gated:** дефолт=TRAINING → кнопка «Начать тренировку» как ДО → дефолтный статик-скрин
байт-идентичен → 14-набор НЕ перезаливал (delta видна лишь после смены режима; политика). Риска клиппинга
нет — все новые лейблы ≤ ранее-самого-длинного «Начать тренировку» (17 симв.), который кнопка уже вмещала.
PE: без JS кнопка = серверный «Начать сессию» (валидный дженерик-фоллбэк, как и было). **Аудит /settings
в этом раунде — структурно чисто:** 6 personalization-радиогрупп `aria-labelledby` резолвятся в текст
(«Дизайн»/«Тема»/…); ARIA-вкладки образцовы (tablist `aria-label="Разделы настроек"`, roving tabindex
0/-1/-1, `<button>`, неактивные панели `display:none` → нет keyboard-trap); session-form контролы обёрнуты
в `<label class=field>` (неявная ассоциация). **Урок: CTA-кнопка должна называть конкретное действие, а не
родовое «сессия»; при динамическом тексте кнопки покрывать ВСЕ значения enum явной картой + defensive-фоллбэк.**

### Раунд 27 — 2026-07-01 — Контраст `.zone-chip` на экране вопроса: AA-провал в editorial-light (A11Y-14)
Коммит: (см. git) · CSS v=55→56 (`fragments/head.html`, обе ссылки) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `.zone-chip` — чип режима («ТРЕНИРОВКА») на экране вопроса (home `/` = question-view), editorial-light | Чип красился `color: var(--color-accent-strong)` (#C96442, тёмный Clay). На ivory (rgb 250,249,245) это **3.70:1** — токен ЯВНО документирован в tokens.css:67-69 как «AA large» (порог крупного текста 3:1). Но чип — mono/uppercase **12px** (`--font-size-xs`) = МЕЛКИЙ текст, порог AA = 4.5:1 → **провал WCAG AA**. Editorial-dark то же место = 4.8:1 (проходит через #D97757). Замер солид-цветом (не градиент) — доверенный | Добавил `.zone-chip` в **уже существующий** editorial-override (base.css:3185-3189), где братья-метки `.ed-masthead-kicker` + `.flashcard-badge` были ОСОЗНАННО сняты с Clay на `--color-text-secondary` («pure typographic label» [spec], комментарий прямо: «Clay … не AA для 12px caps»). Чип — тот же класс метки, и его собственный комментарий (base.css:666-668) заявляет «единый язык метаданных» с `.ed-eyebrow`, а `.ed-eyebrow` (base.css:395) = `text-secondary`. Т.е. accent-strong на чипе был **рассинхроном с задекларированным намерением**. Токен НЕ трогал (6+ заголовочных usages корректно крупные, 3.7 AA-large им ок). Правка editorial-scoped → остальные 8 дизайнов сохраняют accent-чип | medium/low/high |

Реализация: `base.css:3185-3189` — селектор-лист editorial-override расширен с `.ed-masthead-kicker, .flashcard-badge`
до `.ed-masthead-kicker, .zone-chip, .flashcard-badge { color: var(--color-text-secondary) }`; комментарий обновлён
(указан accent-strong 3.7:1 «AA large» vs 12px-метки + отсылка к `.ed-eyebrow`). **CSS-правка → бамп v=55→56** обеих
ссылок в `head.html`. **Live-verify (cp base.css+head в build, hard-reload /, base.css?v=56 подтверждён):**
editorial-light чип `rgb(94,93,89)` на `rgb(250,249,245)` = **6.26:1** ✓ (было 3.70); editorial-dark `rgb(197,193,180)`
на `rgb(38,38,36)` = **8.42:1** ✓ (было 4.8). **Скоуп-регресс-чек всех дизайнов (замер обеих тем):** правка НЕ
затронула notion/swiss/linear/superhuman/stripe/theverge — все проходят (light: notion 5.53, swiss 19.8, linear 6.25,
superhuman 6.16, stripe 7.81, theverge 5.33; dark аналогично ≥5). **Скрины (chip есть ТОЛЬКО на question-view =
home; stats/settings/error без чипа → байт-идентичны, не переснимал):** `1-home-light` / `1-home-dark` /
`6-home-mobile-light` — чип читается как ink-метка в один ряд с эйбрау «ALGORITHMS» и кикером «CHEAT · SHEET»
(намеренно, не сломано); опции в нейтральном pre-check; сдвига лейаута нет. **Побочная находка (залогирована в
PLAN §5 как A11Y-15, СЛЕД. раунд):** дизайн `claude` даёт на `.zone-chip` **3.8:1** (rgb 193,96,64 — его clay-акцент) —
тот же класс дефекта, но у claude clay = бренд-акцент → нужна отдельная развилка (снять на нейтраль vs затемнить его
токен), не бандлил в этот раунд («хирург, не экскаватор»). **Урок: accent-токен, помеченный «AA large», нельзя
применять к мелкому тексту (<18.66px bold / <24px); чип-метки 12px caps держать на `text-secondary` в ряду с эйбрау —
дизайн-система уже задала прецедент на kicker/badge, чип был пропущен.**

### Раунд 28 — 2026-07-01 — Контраст clay-меток дизайна `claude` в light: AA-провал kicker+chip+badge (A11Y-15/BAS-20)
Коммит: (см. git) · CSS v=56→57 (`fragments/head.html`, обе ссылки) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | Дизайн `claude` (token-only), LIGHT: `.ed-masthead-kicker` (ГЛОБАЛЬНЫЙ masthead) + `.zone-chip` + `.flashcard-badge` — все три 12px caps метки | Twin-дефект R27, но обнаружен ШИРЕ: claude — token-only (0 base.css-правил), поэтому наследует base-правила метки `color: var(--color-accent-strong)`. Токен claude-light `--color-accent-strong: #C16040` ЯВНО документирован (tokens.css:1159) как «3.7:1 как текст (AA-large)» → на 12px = **3.8:1**, провал AA-small **на ВСЕХ ТРЁХ метках** (kicker — на каждой странице!). НЕ только chip как в BAS-20-заметке. В DARK claude accent-strong светлее (#E08B6D, **7.1:1**) → проходит | Зеркалю editorial-паттерн, но **light-scoped** (`html[data-design="claude"]:not([data-theme="dark"])`): три метки → `--color-text-secondary` (#5E5D59, документирован «6.3:1»). **Почему только light:** claude-dark 7.1 проходит И несёт намеренный бренд-coral (token-only дизайн осознанно наследует base-акцент, в dark он AA-годен) → «не трогать то, что работает». Токен НЕ трогал | medium/low/high |

Реализация: `base.css` — сразу ПОСЛЕ editorial-override (3190-3192) добавлен claude-блок с комментарием: три селектора
`html[data-design="claude"]:not([data-theme="dark"]) {.ed-masthead-kicker,.zone-chip,.flashcard-badge}` → `text-secondary`.
`:not([data-theme="dark"])` матчит дефолт (нет атрибута) + явный light, исключает dark (специфичность (0,3,1) > base (0,2,1)
→ выигрывает; source-order после base). **CSS-правка → бамп v=56→57** обеих ссылок. **Live-verify (cp в build, hard-reload,
base.css?v=57 подтверждён, замер обеих тем):** claude-light kicker+chip `rgb(94,93,89)` = **5.98:1** ✓ (было 3.8);
claude-dark kicker+chip `rgb(224,139,109)` = **7.1:1** ✓ (СОХРАНЁН бренд-coral, не тронут); editorial 6.26/8.42 без изменений;
notion 5.53/7.39, stripe 7.81/7.79 — не затронуты (правка claude-scoped). **Скрин:** дельта в claude-**light** (не дефолтный
editorial-путь) → стандартный 14-набор editorial БАЙТ-ИДЕНТИЧЕН (не перезаливал). Верификационный `verify-claude-home-light.png`:
kicker «CHEAT · SHEET» + чип «ТРЕНИРОВКА» — ink-метки в ряд с эйбрау «ALGORITHMS» (намеренно, не сломано); лейаут цел.
**После R27+R28 контраст `.zone-chip`/kicker/badge закрыт во ВСЕХ 10 дизайнах × 2 темы (AA-small ≥4.5 везде).** **Урок:
token-only дизайны наследуют base-правила метки → editorial-override их НЕ покрывает; проверять контраст метки в КАЖДОМ
дизайне (не только default+той что чинил). AA-large accent-токен на 12px caps проваливается в LIGHT, но может проходить в
DARK (там значение токена светлее) → фикс scoping-ить по теме, не глушить проходящее dark-состояние.**

### Раунд 29 — 2026-07-01 — Тултипы для stat-grid карточек /stats: паритет с колонками таблицы (AN-14/SGR-4)
Коммит: (см. git) · ШАБЛОН-фрагмент (без бампа) · Находок применено: 1/1 · Диагностика: contrast/touch-target/keyboard-hint/grade-buttons/headings — ВСЁ чисто (см. ниже)
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `fragments/stats-grid.html` — 6 stat-карточек (Всего/К повтору/Выучено/Верно/Ошибки/Точность), самые заметные метрики /stats | Карточки несут SRS-жаргон («К повтору», «Выучено») БЕЗ пояснений, тогда как менее заметные колонки таблицы тем получили `title`-тултипы в R24/R25 (Сброшено/Зрелость/Точность). Несогласованность + пробел ясности на самой видной метрике. «К повтору 9886»/«Выучено 0» — 9886/0 чего? непонятно новому юзеру | `title` на каждый `.stat-item` (плейн static-атрибут, Thymeleaf пропускает). Формулировки СВЕРЕНЫ с SQL (`QuestionStatsRepository.getAggregatedStats`): due=`next_review_at<=now` («по расписанию подошло время повторения»), learned=`repetitions>=threshold` («достаточно успешных повторений подряд»), correct/wrong=`SUM(correct_count/wrong_count)` («за все попытки»), accuracy=`Верно/(Верно+Ошибки)`. Общий фрагмент → тултипы консистентны во всех 3 включениях (/stats,/result,/settings) | medium/low/high |

Реализация: `fragments/stats-grid.html:11-19` — добавлен `title="…"` на 6 `.stat-item`. Точность-тултип показывает формулу
«Верно / (Верно + Ошибки)» (зеркалит R25-принцип «раскрывать знаменатель у процентной метрики»: 32,6% = 59/(59+122)).
**Пер-метрика:** Всего=«Всего вопросов в банке», К повтору=«по расписанию подошло время повторения», Выучено=«достаточно
успешных повторений подряд», Верно/Ошибки=«всего … за все попытки», Точность=формула. **Чистый шаблон-текст → бамп НЕ нужен.**
**Live-verify (cp фрагмента в build, hard-reload /stats):** все 6 `.stat-item` резолвят свой `title` (DOM-проба: label/value/title
верны). **НЕвизуальный цикл** (title виден лишь на hover) → дельты в статик-скрине НЕТ → 14-набор editorial НЕ трогал (как R25).
**📋 Диагностика раунда (worst-first разведка, всё оказалось чисто — залогировано чтобы не пере-аудитить):** (1) **element-level
контраст-скан** (walk по `body *` с direct-text, effBg-цепочка, webkitTextFillColor-aware, settle-delay для тема-транзишенов):
home/settings/stats/error × editorial light+dark = **0 провалов** → после R27/R28 контраст-ось ЗАКРЫТА element-level (не только
токен-гейт). Один ложный флаг `.ed-nav-link` 1.22:1 в dark = **артефакт**: `getComputedStyle().color` сразу после
`setAttribute(data-theme)` вернул ДО-транзишен значение (nav-link имеет `transition: color`); реальный цвет rgb(240,238,230)
на dark = высокий контраст. **Урок скана: при тема-свиче в JS ждать settle транзишенов + читать webkitTextFillColor.** (2)
**touch-target скан @375:** флаги (radio 1×1, checkbox 18×18) — ЛОЖНЫЕ: обёрнуты в `<label>` 44px-высоты (реальная цель — лейбл).
(3) **keyboard-hint @375:** `.keyboard-hint` уже `display:none` на `(pointer:coarse)`. (4) **flashcard-grade-btn:** min-height 48px,
текст-лейблы (не только цвет), role=group+aria-labelledby+per-button aria-label, семантик-цвет (red/green) лишь на hover у
крайних 1/4 — образцово. (5) **heading-hierarchy home/stats:** 1×h1, 0 skip, landmarks/canvas-labels/img-alt ок. **Вывод: лёгкие
a11y-оси исчерпаны; находки уходят в consistency/ясность-ниши. Урок: контраст-скан element-level ловит то, что токен-гейт
(design-token-audit.py) пропускает (применение AA-large токена на мелком тексте) — но осторожно с транзишен-артефактами.**

### Раунд 30 — 2026-07-01 — Подзаголовки графиков /stats: честность среза + видимый actionable-порядок (STA-5/STJ-1)
Коммит: (см. git) · CSS v=57→58 (новое правило `.chart-subtitle`) · Находок применено: 1/1
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `stats.html` — 2 графика («Прогресс по темам», «Точность по темам») + `base.css` | Оба графика рисуют НЕ все ~300 тем, а релевантный top-25 (`MAX_BARS=25` в stats.js): прогресс — сортировка по активности desc, точность — **от слабых к сильным** (accuracy asc, специально ради actionability). Но заголовок «…по темам» + aria «те же данные в таблице» подразумевают ВСЕ темы, а ключевой порядок (слабые впереди) невидим. 25 баров под «Точность по темам» читаются как «все мои темы», и юзер не понимает, что левые красные бары — это его слабые места, с которых стоит начать | Под каждый h2 — `<p class="chart-subtitle">` мелким muted-текстом, называющий срез/порядок: «Самые активные темы» / «Сначала самые слабые». Aria-label обогащены под тот же смысл + «те же данные»→«**полные** данные — в таблице ниже» (точнее: график = top-25 подмножество, таблица = полный набор). CSS: `.chart-subtitle` = тот же muted-sm, что и уже-проверенный `.chart-fallback` (`--color-text-secondary` + `--font-size-sm`), прижат к h2 отрицательным `margin-top: -space-2` | medium/low/high |

Реализация: `stats.html:109-112,119-122` (2 подзаголовка + 2 aria-label), `base.css:1604-1614` (новое `.chart-subtitle`),
`head.html:298-299` (v=57→58). **Почему это ценно, а не декор:** график точности сортируется weakest-first именно ради
actionability, но этот замысел был НЕвидим — подзаголовок «Сначала самые слабые» делает его явным и превращает график из
«непонятного набора баров» в «начни с этих». Скриншот подтверждает: под «Сначала самые слабые» бары растут 26%→40% (красные)
→ 100% (зелёные) — порядок теперь назван. **Контраст:** переиспользован токен `.chart-fallback` → light `#5E5D59`/ivory = 6.3:1,
dark `#C5C1B4`/`#30302E` = **7.34:1** (оба ≥AA-small, dark ещё и AAA); измерено на живом рендере с settle-delay+webkitTextFillColor
(урок R29). **Текст без хардкода числа** («самые активные»/«самые слабые», не «25 тем») → правдив при любом объёме данных (≤25
активных тем → показаны все, формулировка всё равно верна). **Live-verify оба состояния:** gapH2ToSub=8px, gapSubToChart=16px
(подзаголовок группируется с заголовком, не с графиком), subs резолвятся, charts рендерятся. **Скрин-набор:** дельта только на
`.stats-page` (правило scoped) → пересняты 3 stats-скрина (4-stats-light/dark desktop + 8-stats-mobile-light), остальные 10
пиксель-идентичны на v=58 (home/training/settings/error не затронуты scoped-правилом); md5 троицы различны (нет stale-race).

### Раунд 31 — 2026-07-01 — Приоритет CTA в empty-state тренировки: primary совпадает с текстом-инструкцией (FT/empty)
Коммит: (см. git) · ШАБЛОН-текст (без бампа) · Находок применено: 1/1 · Диверсификация: со /stats на recovery-flow /training
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `focus-training.html` — empty-state «Сейчас нет вопросов», ветка «фильтры пусты» (самая частая, видна живьём на `/training?topic=…`) | Описание прямо просит «**Открой настройки** и расширь выборку», НО главным (залитым primary) CTA была «Обновить тренировку», а «Открыть настройки» — вторичной. Для filter-mismatch refresh = **no-op** (перезагружает тот же пустой запрос → уводит по кругу), тогда как настройки — единственный реальный фикс. Primary-эмфаза противоречила собственной инструкции-копии | Флип приоритета ТОЛЬКО в filters-ветке: `th:with settingsPrimary=${!generationUnavailable and !reviewMode and interviewSession == null}` (тот же expr, что у описания — тест TemplateFragmentContractTest:94 остаётся зелёным). Настройки → `next-btn` при settingsPrimary; retry → `secondary-btn` при `finished or settingsPrimary`. Прочие ветки (AI недоступен / сессия не готова / review) settingsPrimary=false → рендер **байт-идентичен** прежнему (retry-логика сводится к `${finished}`) | medium/low/high |

Реализация: `focus-training.html:171-194` — `th:with` + 2 `th:classappend` (settings и retry). **Классы `.next-btn`/`.secondary-btn`
уже существуют → чистый шаблон-текст, бамп НЕ нужен.** **Контракт-тесты сверены ПЕРЕД правкой (bootRun-wedge → не гоняю suite):**
TemplateFragmentContractTest (empty-action-settings/retry + focusEmptyRetry href/text + filters-expr:94 — все сохранены),
InterviewControllerTest:557-560 (settings/retry/«Обновить тренировку» — сохранены, класс next/secondary не ассертится),
VisualBaselineContractTest:145 (точный `class="btn next-btn"` — это /result-shell, а empty-кнопки несут `empty-action-*` →
никогда не матчат эту строку). **Live-verify (cp в build, hard-reload /training?topic=java-concurrency, editorial):** filters-ветка
→ settings `next-btn` primary (Clay bg `rgb(217,119,87)`, order 0), retry `secondary-btn` (bg transparent, order 1) — primary-эмфаза
теперь = текст-инструкция; mobile 375 без гориз. переполнения (scrollW 375==innerWidth). Прочие ветки не триггернуть живьём (нужны
server-состояния AI-down/сессия), но верны **by-construction** (settingsPrimary=false → identical). **Скрин-набор:** дельта только
на /training empty-state → пересняты 3 training-скрина (2-training-light/dark + 9-training-mobile-light), остальные 11 не тронуты;
md5 троицы различны (нет stale-race). **Урок: primary-CTA обязан совпадать с guidance-копией empty-state; refresh-кнопка бесполезна
для filter-mismatch (перезагружает тот же пустой запрос) — эмфаза должна вести к реальному фиксу (настройки). Флип скоупить по
причине пустоты (settingsPrimary-expr), чтобы прочие ветки (AI/сессия/review, где refresh осмыслен) остались нетронуты.**

### Раунд 32 — 2026-07-01 — Орфан-разделители в 10-опционном дизайн-контроле /settings (SET-3/SET-5)
Коммит: (см. git) · CSS v=58→59 · Находок применено: 1/1 · Диверсификация: на forms /settings (вкладка «Оформление»)
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `base.css` `#design-pref-control` — seg-control выбора дизайна (10 опций) на вкладке «Оформление» /settings | Единственный из 6 seg-controls, который **переносится** (10 опций → 3 ряда). Разделители — `border-right` на `.seg-btn`, а `:last-child{border-right:0}` гасит ТОЛЬКО глобально-последнюю кнопку → у концевых кнопок перенесённых рядов (Notion в ряду 1, Superhuman в ряду 2) на торце **болталась вертикальная линия в пустоте** (классический wrap-orphan). Видимый polish-баг на surface кастомизации | Скоуп по `#id` (прочие 5 контролов 2–3 опции, не переносятся → не тронуты): для 10-опционного пикера набор **чипов с зазором** чище склеенных сегментов. Контейнер: `border:0; background:transparent; gap:space-2`. Каждый `.seg-btn`: собственная `border: rule-weight solid border-primary` + `border-radius: --border-radius-sm` (токен design-adaptive: editorial 0=квадрат, rounded-дизайны 6px). Орфанов нет (у каждого чипа полная рамка), перенос читается намеренным | medium/low/high |

Реализация: `base.css:2694-2712` (новый блок после `.seg-btn`), `head.html:298-299` (v=58→59). **Скоуп по `#design-pref-control`
= zero-risk остальным 5 контролам** (theme/layout/reading-width/density/motion — 2–3 опции, один ряд, склеенные сегменты верны).
**Специфичность выверена:** `html[data-design] #design-pref-control .seg-btn` (1,2,1) бьёт base `.seg-btn`(0,2,1)+`:last-child`(0,3,1)
по id-колонке → полная рамка на всех чипах включая последний; `.is-active`(bg accent) и `:hover`(bg tertiary) я НЕ трогаю (не задаю
bg кнопки) → активный/hover-стейт цел (DOM-проба: Editorial-чип bg `rgb(217,119,87)` сохранён). **Live-verify оба состояния (cp в
build, hard-reload, base.css?v=59):** light — 10 чипов с рамкой+gap 8px, орфан-линий нет, Editorial accent-filled; theme-контрол
рядом = склеенные сегменты (border-right 1px, container border 1px) НЕ тронут; dark — чип-рамки `rgb(62,60,54)` = тот же токен
`--color-border-primary`, что у соседнего theme-контрола/карточек (subtle by-design, не регресс — старый контейнер имел рамку того
же цвета). **Скрин-набор:** дельта ЗА вкладкой «Оформление» (дефолтный /settings-скрин = вкладка «Сессия») → 14-набор дефолтных
видов НЕ меняется → не пересобирал (политика interaction-gated, как R26); верификация — скрины appearance-панели light+dark.
**Урок: seg-control с `border-right`-разделителями ломается при переносе (orphan divider на торце ряда — `:last-child` не ловит
концевые кнопки рядов). Для >5 опций набор чипов (индивидуальная рамка + gap) устойчивее склеенных сегментов; скоупить по `#id`,
чтобы малые контролы сохранили склеенный вид. `--border-radius-sm` design-adaptive → чип наследует радиус-язык дизайна.**

### Раунд 33 — 2026-07-01 — Трекинг кириллических ALL-CAPS лейблов в editorial: 0.12em → 0.09em (BAS-16/TY-1)
Коммит: (см. git) · CSS v=59→60 · Находок применено: 1/1 · Диверсификация: типографика токен-слой (дефолт-дизайн, cross-page)
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `tokens.css` `:root` (editorial-дефолт) `--letter-spacing-wide` | Дефолтный дизайн (его видит большинство) держал `--letter-spacing-wide: 0.12em` — **самое широкое во всей системе** и **единственное без обосновывающего комментария**; все 9 сиблинг-дизайнов явно оттюнены до 0.07–0.09em с комментами про Cyrillic-safety («безопасно для кириллицы», «выше 0.06em»). На 12px mono ALL-CAPS лейблах (stat-label, thead th, eyebrow, nav-tab, «ФИЛЬТРЫ»/«ПОИСК») 0.12em = 1.44px между глифами → кириллица (сама широкая и однородная, без выносных) **разваливалась на отдельные буквы** («Ф И Л Ь Т Р Ы»), теряя связность слова. Замерено live + подтверждено скрином | Один токен, скоуп = editorial-дефолт (`:root`): `0.12em → 0.09em` (1.44px → 1.08px). 0.09em = валидированная в системе Cyrillic-safe верхняя граница (ровно Swiss-сигнатура «выше 0.06em») → editorial остаётся у широкого края тюнингованной полосы (airy-label характер цел), но слова снова читаются как слова. Прочие 9 дизайнов не тронуты (у них свой override) | medium/low/high |

Реализация: `tokens.css:113-121` (значение + обоснующий комментарий, зеркалящий стиль сиблингов), `head.html:298-299` (v=59→60,
обе ссылки tokens+base). **Почему не signature, а баг:** память `project_design_elevation_round1` перечисляет «deliberate, не баги»
и letter-spacing там НЕТ; голое значение без комментария при 9 закомментированных сиблингах = неотюненный оригинальный дефолт.
**Live-verify (cp tokens+head в build, hard-reload /stats, editorial):** до — `.stat-label`/`thead th` = `1.44px` (0.120em); после —
`1.08px` (0.090em) при v=60; DOM-проба nav-tab/eyebrow/stat-label/thead — все 1.08px. **Оба состояния:** менял `data-theme`
light↔dark — трекинг темонезависим (токен один), проверил light (скрин) + dark (14-набор). **Скрин-набор пересобран полностью
(14 шт):** дельта — cross-page (uppercase-лейблы на КАЖДОЙ странице дефолтного дизайна: masthead-eyebrow, nav-tabs, stat-labels,
thead, секции «ФИЛЬТРЫ»/«ПОИСК») → это НЕ interaction/tab-gated (в отличие от R32) → полный regen оправдан + ловит возможный
label-reflow. Все 14 размеров различны (нет stale-frame race); /stats-desktop-light проверен глазами — лейблы связнее, ни один
не перенёсся/не сломал раскладку. **Урок: при добавлении новых дизайнов в токен-систему легко забыть протянуть Cyrillic-tuning
на дефолт — дефолтный `:root` может остаться с исходным Latin-ориентированным значением, пока сиблинги тюнятся. Проверять кросс-
дизайн консистентность именно у дефолта: он самый видимый. Кириллические капители требуют трекинг ≤0.09em, иначе теряют связность
(латиница терпит 0.12em, кириллица — нет).**

### Раунд 34 — 2026-07-01 — Кнопки экспорта /settings: сырой юникод «↓» → Lucide SVG-иконка download (IC-4)
Коммит: (см. git) · БЕЗ бампа (чистый шаблон+спрайт) · Находок применено: 1/1 · Диверсификация: компоненты/иконки на вкладке «Данные» /settings
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `settings.html` кнопки экспорта (вкладка «Данные») + `fragments/icons.html` спрайт | Обе кнопки экспорта несли **сырой юникод-глиф «↓»** ПОСЛЕ текста («Экспорт JSON ↓»), тогда как вся система стандартизована на монохромный Lucide-SVG-спрайт (`.ed-icon` + `<use href="#i-…">`; только 💡🎲🤔💪 — намеренные эмодзи-исключения). Юникод-«↓» рендерится по-разному на разных ОС/шрифтах (тут в Inter-кнопке), выбивается из icon-системы, И стоял ПОСЛЕ текста — тогда как все прочие icon-кнопки (copy/print/book-open/flag/star/refresh) **ведут** иконкой. Двойная несогласованность | (а) добавил канонический Lucide `#i-download` (лоток+стрелка вниз, 3 path, viewBox 0 0 24 24) в спрайт; (б) заменил «текст ↓» на «`<svg class="ed-icon" aria-hidden>` текст» в обеих кнопках — SVG вместо юникода И icon-lead как у сиблингов. `aria-label` (полное имя) + `data-export-format` + текст «Экспорт JSON/CSV» сохранены | low-medium/low/high |

Реализация: `icons.html` (+1 `<symbol id="i-download">`, currentColor-монохром как весь спрайт), `settings.html:255-256` (2 кнопки). **Чистый шаблон/фрагмент HTML → CSS/JS не трогались → бамп НЕ нужен.** **Контракт-тесты сверены ПЕРЕД правкой (bootRun-wedge):** `InterviewControllerTest:257-258` ассертит `contains("Экспорт JSON")`/`"Экспорт CSV"` — текст СОХРАНЁН (снял только « ↓», добавил ведущий SVG); `:255-256` `data-export-format="json"/"csv"` — атрибуты целы; `TemplateFragmentContractTest:145` `id="data-export-block"` — цел; `assertNoInlineUiAttributes` — SVG через `class`, без inline `style=` (точная копия паттерна session-summary/result). **Live-verify оба состояния (cp icons+settings в build, hard-reload /settings, клик вкладки «Данные»):** символ `#i-download` резолвится (3 path); обе кнопки — SVG 16×16 `use href="#i-download"`, сырого «↓» нет (`rawArrow:false`); light — тёмная монохром-иконка ведёт лейбл, в ряд с flag-иконкой «Опасной зоны» (консистентно); dark — иконка `currentColor` = rgb(240,238,230) light-ink, видима, тема-адаптивна. **Скрин-набор:** дельта ЗА вкладкой «Данные» (дефолтный /settings-скрин = вкладка «Сессия») → 14-набор дефолтных видов НЕ меняется → не пересобирал (tab-gated, политика R32/R26); верификация — inline-скрин панели «Данные» light. **⚠️ Побочная находка (СЛЕД. раунды, НЕ бандлил — хирург):** CTA-кнопки несут сырой юникод «→» («Начать повторение →», «Начать сессию →») — тот же класс (unicode-глиф вне icon-системы), но ШИРЕ распространён (next-btn паттерн) → отдельный раунд, чтобы не превращать в экскаватор. Залогировано в PLAN §5 IC-5. **Урок: юникод-глифы-affordance («↓» download, «→» next) — это НЕ иконки системы; у download есть канонический Lucide-символ, добавить в спрайт + `<use>` вместо глифа. Держать icon-lead (иконка ПЕРЕД текстом) как у всех прочих icon-кнопок. Спрайт-добавление + swap = чистый шаблон, бампа не требует.**

### Раунд 35 — 2026-07-01 — `scope="col"` на заголовках таблиц /stats (A11Y, WCAG 1.3.1/H63); app-деградация → curl-верификация
Коммит: (см. git) · БЕЗ бампа (чистый HTML-атрибут) · Находок применено: 1/1 · Диверсификация: a11y таблиц /stats · **⚠️ Раунд под app-деградацией (см. ниже)**
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `stats.html` `topic-table` (6 thead `<th>`) + `data-table` (2 thead `<th>`) | Обе таблицы статистики имели `<caption>` (visually-hidden) и `<thead>`, но **ноль `scope`-атрибутов** на заголовочных ячейках. По WCAG 1.3.1 (H63) колонки-заголовки в data-таблице должны нести `scope="col"`, чтобы ассоциация «ячейка данных → заголовок колонки» была ЯВНОЙ, а не выведенной браузерным эвристиком (ненадёжно для AT, особенно у 6-колоночной sortable-таблицы с aria-sort) | `scope="col"` на все 8 thead `<th>` (topic-table 6 + data-table 2). **Чистый инертный атрибут — 0 визуального/CSS/JS-эффекта** → бамп НЕ нужен, парность гарантирована | low-medium/low/high |

Реализация: `stats.html:149-154` (topic-table thead) + `:277` (data-table thead). **`scope="col"` — инертный атрибут (0 CSS/JS/пиксельного эффекта) → бамп НЕ нужен.** **Контракт-тесты:** `VisualBaselineContractTest:78` (`id="topic-table"`) + `InterviewControllerTest:459` (`data-sort-label=`) — оба сохранены (id/data-sort-label целы); никакой тест не ассертит th-разметку/scope. stats.js **не читает** ячейки по `td`/`.cells`/`tBodies` (grep пусто) → sort цел.
**⚠️ APP-ДЕГРАДАЦИЯ (важный контекст раунда):** в начале раунда `/` отдал 500, затем ВСЕ эндпоинты — 000 (hang), docker CLI завис на 2 мин. Диагностика: java-процесс (PID 92484) жив и слушает :8080, OrbStack-postgres жив на :5433, но приложение интермиттентно ошибит (curl ловил 200, браузер — error-page) — **DB/OrbStack-слой подвис под нагрузкой** (шквал graphify-rebuild'ов от моего R34-коммита + непрерывного потока внешних pedago(mcq)-коммитов, каждый триггерит ~30с rebuild). chrome-devtools navigation таймаутил на 10с (ждёт `load`-событие / медленные сабресурсы), показывал stale error-кадры. **Адаптация:** раз визуальная верификация ненадёжна — выбрал НЕвизуальную (инертный `scope`-атрибут), верифицировал через **curl** (рендер-HTML содержит 6 `scope="col"` в topic-table thead, Thymeleaf распарсил без 500; data-table 2 — за conditional coverage-gaps, safe by-construction, тот же паттерн). Скрины НЕ пересобирал (нет визуальной дельты И app-деградация). **🔑 Побочная находка (СЛЕД. раунд, залогирована PLAN §5 STA-16/A11Y):** РЕАЛЬНО ценный row-header — `td.topic-name → <th scope="row">` (SR при навигации по ячейке «40%» озвучит тему строки, сейчас — только «Точность 40%» без контекста темы). НЕ сделал сейчас: `<th>` даёт UA-дефолт `font-weight:bold` (тема-ссылка заболдится), нужен CSS `.topic-name{font-weight:normal}` → бамп + требует ВИЗУАЛЬНОЙ верификации (парность провабельна, но при деградации не увидеть). Отложено до visual-verify раунда. **Урок: (1) app интермиттентно ошибит под graphify-rebuild-штормом (мои коммиты + внешние pedago) — curl ловит хорошие моменты, браузер плохие; при деградации выбирать НЕвизуальные (инертные a11y-атрибуты), верифицировать curl'ом рендер-HTML. (2) `scope="col"` — дешёвый безопасный H63-инкремент; row-headers (`th scope=row`) ценнее, но td→th бволдит контент (UA th{bold}) → нужен font-weight:normal + визуальный чек. (3) navigation-timeout ≠ мёртвый app — DOM бывает ready, но `load` не срабатывает из-за медленных сабресурсов; `evaluate_script` работает, curl HTML работает.**

### Раунд 36 — 2026-07-01 — `<caption>` для coverage-gaps data-table /stats (A11Y, парность имён таблиц, H39/STA-17)
Коммит: (см. git) · БЕЗ бампа (чистый HTML-элемент) · Находок применено: 1/1 · Диверсификация: a11y таблиц /stats (продолжение R35-нити) · Среда всё ещё под нагрузкой (load avg ~10, активны IntelliJ/Zoom/Telegram/Happ) → снова НЕвизуальный curl-верифицируемый раунд
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `stats.html` `data-table` (секция «Пробелы в банке вопросов») | Таблица тем topic-table несёт ТРИ слоя a11y-имени (`<h2>` + `role="region" aria-label` обёртка + `<caption class="visually-hidden">Статистика по темам</caption>`), а coverage-gaps `data-table` — только `<h2>` + (с R35) `scope="col"`, но **без `<caption>`**. `<caption>` = accessible name таблицы в режиме table-navigation SR (отдельный от `<h2>` в heading-navigation); topic-table задаёт этот паттерн ПРИ наличии своего h2 → у data-table пробел парности (та же линза, что R25/R29: две из трёх — объяснены/имеют, третью тоже) | +`<caption class="visually-hidden">Темы с неполным банком вопросов</caption>` (краткая noun-phrase, описывает содержимое строк, НЕ дублирует h2 «Пробелы в банке вопросов» дословно; тот же класс+паттерн, что topic-table:142). Region-обёртка НЕ нужна: data-table 2 колонки → нет гориз.overflow (в отличие от 6-колоночной topic-table). **Чистый visually-hidden элемент — 0 layout/CSS/JS-эффекта** → бамп НЕ нужен | low-medium/low/high |

Реализация: `stats.html:276` (+1 `<caption>` внутри `data-table`, зеркалит topic-table:142). **`.visually-hidden` (base.css:269 — clip-based SR-only, 1×1px) → 0 визуального эффекта → бамп НЕ нужен.** **Контракт-тесты:** grep src/test по `data-table`/`coverage-gaps`/`Пробелы`/`coverageGaps` = ПУСТО → никакой тест не пиннит структуру data-table; правка чисто аддитивна (нельзя сломать `contains`). Нет `caption`-специфичного CSS в base/editorial → visually-hidden гарантированно скрывает. **Верификация (среда под нагрузкой, browser-navigation ненадёжна → curl, как R35):** `/stats` → 200/513KB, template парсится чисто (битый дал бы 500); topic-table `<caption>Статистика по темам</caption>` рендерится ЖИВЬЁМ (доказывает паттерн `.visually-hidden` caption работает); **coverage-gaps секция НЕ рендерится** в текущем DB (`th:if=coverageGaps!=null and !isEmpty` — все темы имеют ≥5 вопросов) → мой caption safe **by-construction** (тот же статический литерал+класс+паттерн, что проверенный topic-table caption; ровно ситуация R35-data-table scope). grep source+build = литерал на месте (line 276), в файле теперь 2 caption'а (142 topic + 276 data). Скрины НЕ пересобирал (visually-hidden = 0 пиксельной дельты + среда под нагрузкой). **Урок: `<caption>` = accessible name таблицы (режим table-nav SR), НЕ избыточен с соседним `<h2>` (разные режимы навигации) — topic-table доказывает прецедент. Держать парность a11y-инструментовки между однотипными таблицами (caption+scope). Под нагрузкой продолжать НЕвизуальные curl-верифицируемые правки; условно-рендеримые секции (coverage-gaps) верифицировать by-construction через идентичный проверенный паттерн-сиблинг.**

### Раунд 37 — 2026-07-01 — Row-header `td.topic-name → <th scope="row">` в /stats (A11Y, SR row-context, STA-16)
Коммит: (см. git) · CSS v=60→61 · Находок применено: 1/1 · Диверсификация: завершение stats-table a11y-нити (R35 scope-col → R36 caption → R37 row-header) · Среда восстановилась (app 3/3 200/<0.14s, load ~8.7) → браузер-верификация вернулась
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `stats.html` topic-table tbody + `base.css` | Первая ячейка каждой из 319 строк (`td.topic-name` с темой-ссылкой) — обычная `<td>`, НЕ row-header. При навигации SR по data-ячейке (напр. «40%» в колонке Точность) не озвучивается тема строки → метрика без контекста. WCAG 1.3.1: имя строки в data-таблице должно быть `<th scope="row">` | `td.topic-name → <th scope="row" class="topic-name">` (319 строк). UA даёт `th{font-weight:bold}` → компенсирующее правило `.topic-table th.topic-name{font-weight:var(--font-weight-normal)}` (визуальный паритет с прежним td; text-align:left уже задан на th+td выше). Завершает парность a11y: caption+scope-col (R35/R36) + scope-row | M/L/H |

Реализация: `stats.html:159-163` (`<td class="topic-name">…</td>` → `<th scope="row" class="topic-name">…</th>`), `base.css:1639-1642` (+правило font-weight:normal с комментарием), `head.html:298-299` (v=60→61 обе ссылки). **🔑 КРИТИЧЕСКИЙ РИСК ПРОВЕРЕН ПЕРЕД правкой — sort:** stats.js:238-239 читает `a.children[col]`/`b.children[col]` — `children` = HTMLCollection ВСЕХ элементов-детей (и `<td>`, И `<th>`) → первая ячейка остаётся index 0, `data-col`-маппинг не сдвигается; `th.sortable`-сканы (215/233) берут только thead (мой body-th без класса `sortable` исключён). **CSS-регресс всех правил проверен:** 1618-1619 (padding/border/**text-align:left явный** → гасит UA `th{center}`) применяется к th+td; 1620 thead-scoped (не мой tbody-th); 1627-1636 требуют `.sortable`/`aria-sort` (нет у body-th); 1638 `td:not(.topic-name)` (другие ячейки td, целы); 1639 `.topic-name a` класс-based (жив). Единственный UA-эффект = `th{bold}` → погашен. **Контракт-тесты:** VisualBaselineContractTest:78 (`id="topic-table"` — id цел, тронул body-ячейку) + InterviewControllerTest:459 (`data-sort-label=` — на thead th, целы); ни один тест не пиннит `td.topic-name`/tag/scope. **Live-verify chrome-devtools (v=61 загружен, reload ignoreCache):** first cell `TH` scope=`row`, class `topic-name`, все **319 строк** `th[scope=row]`; cellFontWeight/linkFontWeight **400** (компенсация работает, НЕ bold), text-align **left**, sibling Всего остаётся `TD`. **Sort протестирован (недеструктивно, только reorder DOM):** клик «Тема» → asc «Agentic patterns…» (A-первый) aria-sort=ascending; toggle → desc «Zero trust…» (Z-первый) aria-sort=descending → `children[0]`=новый th читается корректно, sort ЦЕЛ. **Оба состояния:** dark (после settle-delay, урок R29) — cellFontWeight 400, link `rgb(240,238,230)` ivory (видим, цвет пре-существующий `.topic-name a`, я не трогал). **🔑 НЕТ ВИДИМОЙ ДЕЛЬТЫ:** компенсация делает рендер ПИКСЕЛЬНО-ИДЕНТИЧНЫМ прежнему (font-weight 400 до и после, left align, те же цвета обе темы) → дельта чисто семантическая (accessibility tree: th+scope vs td, скрины не ловят) → 14-набор НЕ пересобирал (политика невизуальных R35/R36; CSS-бамп есть, но его ЭФФЕКТ = сохранить вид). **Урок: row-header (`th scope="row"`) даёт SR контекст строки при навигации по data-ячейкам — завершает table-a11y после scope-col+caption. td→th безопасен для sort, ЕСЛИ JS читает `children[]`/`cells[]` (th-agnostic), НЕ `querySelectorAll('td')` — проверять ОБЯЗАТЕЛЬНО перед правкой. UA `th{bold}` гасить `font-weight:var(--font-weight-normal)` для визуального паритета; UA `th{center}` уже был погашен явным text-align:left. Компенсированный td→th = невизуальная (семантическая) дельта, скрины не нужны, хотя CSS-бамп формально требуется.**

### Раунд 38 — 2026-07-01 — Валидационный кросс-экранный аудит (без правки кода): подтверждение R37-архитектуры + переклассификация backlog
Коммит: (см. git) · БЕЗ бампа (кода не менял — только PLAN_FRONTEND.md) · Находок применено: 0 (валидация) · Диверсификация: полный обход достижимых экранов + заострение backlog
| # | Область | Что проверено | Вывод | Impact/Risk/Conf |
|---|---------|---------------|-------|------------------|
| 1 | Обе data-таблицы /stats + /session-summary | Почему topic-table и summary-table используют РАЗНЫЕ паттерны row-header (`<th scope="row">` vs `<td role="rowheader">`) | **R37 архитектурно валиден.** topic-table: `display:table` (overflow на обёртке `.topic-table-wrap`) → нативная table-семантика жива → `<th scope="row">` работает нативно. summary-table: `display:block` (overflow на самой таблице для скролла) → браузер СТРИПАЕТ нативную table-семантику → нужны явные ARIA-роли (role=table/row/rowheader), и первая ячейка ОБЯЗАНА остаться `<td role="rowheader">`, НЕ `<th>` — потому что share-JS читает `querySelectorAll('td')` и требует ≥5 td/строку (SUM-5/SUM-7 контракт). td→th сломал бы копи-шеринг. Обе таблицы корректны, каждая своим паттерном под свою display-модель | — (валидация) |
| 2 | `session-summary.html` `.summary-actions` | Дубль ли secondary «Продолжить тренировку» при наличии ошибок (SUM-3) | **НЕ дубль — намеренный dual-path IA.** При ошибках: primary «Повторить ошибки»(/review) + secondary «Продолжить тренировку»(/) — РАЗНЫЕ пути (ревью ошибок vs продолжить с новыми). Комментарий шаблона (стр.34-35) это фиксирует. Переклассифицировал SUM-3 в PLAN: 🌱 BACKLOG → ✅ verified-deliberate | — (валидация) |
| 3 | IC-5 (сырой юникод «→» на CTA-кнопках) | Точный scope + возможность чистого swap | 7 CTA несут сырой «→» (today-widget:46,47; result:107; session-summary:37,38; stats:99; error:52). **НЕ чистый swap** — форсит дизайн-решение о конвенции (icon-lead как у прочих icon-кнопок vs directional trailing arrow). Консистентный «→» скорее намеренный. Требует мультистраничного visible-delta прохода (regen 14-набора) → на спокойную среду. Уточнил scope+блокер в PLAN §5 IC-5, Confidence L | M/L/L |

Реализация: **кода НЕ трогал** — только PLAN_FRONTEND.md (SUM-3 → verified-deliberate; IC-5 scope+блокер уточнён). Валидационный раунд: после исчерпывающего обхода всех достижимых экранов (home/вид-вопроса, today-widget, /settings-вкладки, обе data-таблицы, result.html, session-summary.html, error.html) — приложение исключительно отполировано, чистых surgical-находок нет. Ценность раунда: (а) доказал, что расхождение row-header-паттернов между двумя таблицами — НЕ недосмотр, а корректное следствие display:table vs display:block (нативная семантика vs стрип+ARIA), что задним числом валидирует R37-решение проверять JS перед td→th; (б) снял ложную SUM-3-находку (dual-path намеренный); (в) заострил IC-5 до точного scope+блокера для будущего dedicated дизайн-раунда. **Урок: `<table display:block>` (для overflow-скролла) СТРИПАЕТ нативную table-семантику в браузерах → требует явных ARIA-ролей; `<table display:table>` с overflow на ОБЁРТКЕ сохраняет семантику → нативные `scope` работают. Выбор `<th scope>` vs `<td role=rowheader>` диктуется (1) display-моделью таблицы И (2) тем, что читает связанный JS (`children[]` th-agnostic — можно th; `querySelectorAll('td')` — обязан остаться td). Не всякий раунд = правка: валидация, снимающая ложную находку и подтверждающая архитектуру прошлого раунда, — легитимный хирургический цикл (backlog становится точнее, риск будущих правок падает).**

### Раунд 39 — 2026-07-02 — Answer fairness: adaptive single-column, neutral selected, Escape clear
Коммит: (см. git) · CSS v=64→65 · JS v=52→53 · Находок применено: 4/4 · Диверсификация: тренировка / честность выбора
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `/` MCQ options | Wide/grid layout мог делать длинный и code-heavy вариант визуально главным. | `app.js` вычисляет skew вариантов: `max/min > 1.4` или перекос inline-code → `data-answer-layout="single"`; 2×2 включается только для `balanced`. | high/medium/high |
| 2 | Pre-check selected | Выбор до проверки был слишком акцентным и мог читаться как смысловое состояние. | Selected-before-check переведён в нейтральный border/surface/ink marker; success/error остаются только на `.option-correct/.option-wrong`. | high/low/high |
| 3 | Keyboard | Выбрать вариант можно было клавиатурой, но снять выбор без мыши нельзя. | `Esc` снимает выбранный radio, дисейблит submit и возвращает фокус на radio. Hint обновлён. | medium/low/high |
| 4 | Process | Prompt требовал UI/UX backlog. | Добавлен `docs/ui-ux-backlog.md` с AF/AN/SE/AX задачами. | medium/low/high |

Проверки: `node --check app.js`; `./gradlew :quiz-app:processResources :quiz-app:test --tests TemplateFragmentContractTest --tests VisualBaselineContractTest` — OK. Live QA на `localhost:8080`: HTML грузит `base.css?v=65` и `app.js?v=53`; на реальном вопросе layout=`single`, skew=`2.43`, reason=`inline-code-skew`, grid columns=`960px`; клавиша `2` выбирает вариант и включает submit, `Esc` снимает выбор и снова выключает submit; mobile 375×812 без horizontal overflow, hotkey hint скрыт. Скрины: `output/playwright/fairness-focus-desktop-selected.png`, `output/playwright/fairness-focus-mobile.png`. Console: один внешний таймаут CDN Mermaid, не связан с этим diff.

### Раунд 40 — 2026-07-02 — Analytics next actions: CTA above charts with weakest-topic cue
Коммит: (см. git) · CSS v=65→67 · stats.js v=11→12 · Находок применено: 1/1 · Диверсификация: аналитика / следующий учебный шаг
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `/stats` после KPI | Дашборд показывал состояние банка, но не говорил, что делать дальше: повторять due, разбирать ошибки или идти в слабые темы. | Добавлен блок `Что делать дальше` с тремя CTA над фильтрами/графиками. Recommended-логика: due → ошибки → weakTopics. Третий CTA получает JS-enhancement из `topicStatsJson`: показывает конкретную слабую тему и точность (`Backtracking · 26%` в live DB), без JS остаётся fallback по числу тем. | medium/low/high |

Проверки: `node --check app.js`; `node --check stats.js`; `./gradlew :quiz-app:processResources :quiz-app:test --tests com.cheatsheet.quiz.ui.TemplateFragmentContractTest --tests com.cheatsheet.quiz.ui.VisualBaselineContractTest` — OK. Live QA на `localhost:8080/stats`: HTML грузит `base.css?v=67` и `stats.js?v=12`; desktop 1440×900 — 2-column action block, 3 CTA, `weakSummary="Слабее всего: Backtracking · 26%"`, overflowX=false; mobile 375×812 — 1-column block, CTA heights stable, overflowX=false. Скрины: `output/playwright/stats-next-actions-desktop.png`, `output/playwright/stats-next-actions-mobile.png`.

### Раунд 41 — 2026-07-02 — Находка a11y (today-hero-hint AA-small) ОТЛОЖЕНА: base.css под активным UX REVIEW PASS пользователя
Коммит: (см. git) · БЕЗ бампа (кода не трогал — только PLAN+log) · Находок применено: 0 (заблокировано collision) · Диверсификация: контраст hint-ов /settings + защита чужой in-flight работы
| # | Область | Находка | Вердикт | Impact/Risk/Conf |
|---|---------|---------|---------|------------------|
| 1 | `.today-hero-hint` (подсказка под CTA today-hero /settings, 14px) | Единственный `*-hint`, красящийся `--color-text-tertiary` (все прочие — zone-hint/keyboard-hint/control-inline-hint/*-forecast-hint — на `text-secondary`). tertiary калиброван «AA-large» (3:1), а hint = мелкий текст 14px (порог AA 4.5:1). **Замер live chrome-devtools (settle-delay, урок R29):** claude-light `#76756E`/`rgb(250,249,245)` = **4.39:1 → ПРОВАЛ AA-small**; editorial-light 4.89, editorial-dark 4.99, claude-dark 5.07 — проходят, но пограничные (в 0.5 от порога). Фикс = 1 property `tertiary → secondary` (паритет со всеми *-hint, AA-safe во всех 10×2) | **ОТЛОЖЕНО (blocker).** Верифицирована и готова, НО не применена | M/L/H |

Реализация: **кода НЕ трогал итогово** — сделал правку `.today-hero-hint tertiary→secondary` в base.css, live-замерил (подтвердил claude-light 4.39 fail), затем **ОТКАТИЛ**. **🔑 Причина отката — защита незакоммиченной работы пользователя:** на старте раунда `git status` показал АКТИВНЫЙ in-flight UX REVIEW PASS пользователя во ВСём фронте — `base.css` (+193 стр: «UX REVIEW PASS — 2026-07-01» блок [stats-filter padding, mobile chrome], + MCQ options grid `2×2 → single-column` с новым `data-answer-layout="balanced"` opt-in — ровно деликатная answer-fairness тема), `app.js`, `head.html` (base.css bumped 61→65), `result.html`, `settings.html`, `stats.html`, `focus-training.html`. На R38-end рабочее дерево было ЧИСТО от этих файлов → пользователь редактирует ПРЯМО СЕЙЧАС. **Правила лупа: «не затирай чужие изменения; если файл изменён пользователем — особенно осторожен».** `git add base.css` свернул бы все 193 чужие строки в мой коммит; `git add -p` (изолировать мой хунк) недоступен в среде. Плюс tokens.css-правка требует бампа head.html (изменён), stats.js → stats.html (изменён), app.js → result/settings/focus (изменены) — **каждый путь фикса упирается в файл под активной правкой пользователя.** → Выбрал НЕ конкурировать с live-редизайном: откатил base.css к состоянию пользователя (verified: back to +193/-6, tertiary восстановлен), находку записал в PLAN §5.C.2 BAS-21 (🌱 BACKLOG blocked). Коммичу ТОЛЬКО доки (PLAN+log — не в списке изменённых пользователем, per-pathspec, без collision). **Урок: перед CSS/JS-правкой в луп-раунде — `git status` на весь `templates/`+`static/` scope, не только на целевой файл; если пользователь ведёт активный многофайловый pass (base.css+app.js+templates разом) — НЕ конкурировать, работать только в доках/незатронутых фрагментах ИЛИ делать finding-defer раунд. Версия-бамп связывает файлы (tokens↔head, stats.js↔stats.html, app.js↔result/settings/focus) → правка одного «свободного» файла может потребовать бампа в «занятом». today-hero-hint = единственный `*-hint` на tertiary (AA-large на 14px), claude-light 4.39 провал — применить когда пользователь закоммитит.**

### Раунд 42 — 2026-07-02 — Mobile option explanations: disclosure вместо стены текста
Коммит: (см. git) · CSS v=67→69 · app.js v=53→54 · Находок применено: 1/1 · Диверсификация: post-answer mobile flow
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | `/` после ответа на мобильной ширине | После wrong-answer первые экраны забивались длинными explanation-блоками внутри выбранного wrong и правильного варианта; unselected wrong уже скрывались, но две важные карточки всё равно превращались в стену текста до пост-разбора. | `app.js` на mobile (`max-width:760px`) превращает `.option-explanation` в нативные `<details>` с summary: «Почему выбранный ответ неверен» / «Почему это правильный ответ». Для `option-other` в focus-flow disclosure скрыт тем же мобильным guard, что и старые unselected explanations. No-JS/server-render остаётся раскрытым. | high/medium/high |

Проверки: `node --check app.js`; `./gradlew :quiz-app:processResources :quiz-app:test --tests com.cheatsheet.quiz.ui.TemplateFragmentContractTest --tests com.cheatsheet.quiz.ui.VisualBaselineContractTest` — OK. Live QA на `localhost:8080/` mobile 375×812: HTML грузит `base.css?v=69`, `app.js?v=54`; после wrong-answer `detailsCount=4`, `visibleDetails=2`, `hiddenOtherDetails=2`, обе visible summaries закрыты, overflowX=false. DOM-click summary раскрывает details (`open:false→true`). Скрины: `output/playwright/af4-focus-mobile-collapsed.png`, `output/playwright/af4-focus-mobile-open.png`.

### Раунд 43 — 2026-07-07 — APPLY DONE: долги пост-AI-removal применены (collision-guard снят пользователем)
Коммиты: `0ed95133` (test-cleanup) + `c0b997ba` (fix) · поверх WIP-коммита `41782d54` · app.js v=54→55 · Находок применено: 4 долга + 2 открытия
| # | Область | Проблема | Правка | Impact/Risk/Conf |
|---|---------|----------|--------|------------------|
| 1 | result.html / focus / post-answer — «доп. анализ» (AIR-1) | Кнопка «Показать доп. анализ» слала 4 fetch'а к удалённым с AI эндпоинтам (`/api/wrong-feedback·comparison·takeaway·code-trace`) → каждый клик падал в «Не удалось загрузить». | Вырезаны мёртвые fetch'и/константы из `app.js`; удалена кнопка+пустой контейнер с `result.html`; единственный пост-разбор — серверные «Похожие вопросы»; кнопка на focus переименована. | high/med/high |
| 2 | focus-training — STUDY LEARN→QUIZ (RIA-2) | Фаза LEARN не имела UI: рендерилась форма вариантов (как TRAINING), `studyAnswerHtml` вычислялся, но не выводился, `POST /study-confirm` был недостижим (0 триггеров). | Учебная карточка `studyLearnPhase` (ответ + «Проверить себя →» → /study-confirm → switchToQuizPhase); форма вариантов гейтится `!studyLearnPhase`. Семантика из enum `StudyPhase`. | high/med/high |
| 3 | settings — «количество» (SET-15) | `count value="20"` хардкод игнорировал серверные per-mode дефолты. | `data-default-count` на `<option>` + `app.js` ставит дефолт при смене mode (EXAM=20; STUDY/FLASHCARD/MARATHON=50). | med/low/high |
| 4 | focus-training — мёртвая копия (AIR-fallout) | «AI временно недоступен» под флагом `generationUnavailable`, зашитым в false — никогда не рендерилась. | Строка удалена. | low/low/high |
| 5 | InterviewControllerApiTest (открытие) | Тесты `/api/takeaway·comparison·code-trace·wrong-feedback·regenerate·hint` остались после вырезания эндпоинтов и падали 404 — базлайн был КРАСНЫМ (скрыто `-x test`). | Удалены 6 семейств тест-методов + осиротевшие импорты (отдельный коммит `0ed95133`). | — |

Проверки: `node --check app.js` OK. `./gradlew :quiz-app:test` по затронутым классам — `InterviewControllerApiTest` BUILD SUCCESSFUL (мёртвые тесты сняты); `TemplateFragmentContractTest`, `VisualBaselineContractTest`, `SessionFlowServiceTest`, `InterviewControllerTest` (кнопка-текст) — зелёные; **новый RIA-2 render-тест `studyModeLearnPhaseRendersConfirmButtonThenQuizShowsOptions` — `tests=1 skipped=0`, ассерты исполнились** (LEARN: card+«Проверить себя», форма вариантов скрыта; /study-confirm → QUIZ: форма вариантов есть, карточки нет). CSRF-урок: session-POST-флоу проверять MockMvc `.with(csrf())`, не raw curl (`CookieCsrfTokenRepository.withHttpOnlyFalse()` не пишет XSRF cookie на GET). **⚠️ Оставлено пользователю:** `statsTableHeadersExposeKeyboardSortableContract` красный из-за WIP-правки `stats.html` (добавлен `scope="col"` → `th class="sortable"` больше не подстрока) — территория незавершённого a11y-pass юзера, не трогаю.
