{
  "topic_slug": "graalvm-native-interview",
  "product_version": "GraalVM for JDK 21/24 (Oracle GraalVM 24.x, native-image; актуально на 2026-07-16). ВНИМАНИЕ version-sensitive: с GraalVM for JDK 17+ дистрибуция перешла на schedule Oracle JDK (версии 21/24), пакет 'reachability metadata' в native-image; Spring Boot 3.x native через buildpacks/GraalVM native-image plugin (Spring Native как отдельный проект УСТАРЕЛ, слит в Boot 3).",
  "checked_date": "2026-07-16",
  "sources": [
    "GraalVM Reference — Native Image (native-image AOT, build-time vs run-time initialization, reachability metadata, closed-world assumption)",
    "GraalVM Native Image — Reflection/Resources/Serialization config (reflect-config.json / RuntimeHints), '--initialize-at-build-time', 'native-image-agent' tracing agent",
    "Spring Boot 3.x Reference — GraalVM Native Image support (AOT processing, org.graalvm.buildtools native-gradle/maven plugin, buildpacks); Spring Native слит в Boot 3, отдельный стартер устарел",
    "GraalVM release cadence — согласован с Oracle JDK (21 LTS, 24); Serial/G1 GC в native-image, PGO в Oracle GraalVM"
  ],
  "notes": "Ядро (AOT closed-world, build-time init, reachability metadata, reflection config, native-image-agent, отсутствие JIT-профилирования в рантайме) стабильно концептуально. Version-sensitive: нумерация GraalVM выровнена на JDK (21/24, не старые 22.x); Spring Native устарел (слит в Boot 3 native support). Проверять при version-bump GraalVM/JDK."
}
