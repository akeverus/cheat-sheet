# MCQ Answer-Parity & Factual-Integrity Audit — BEFORE (2026-07-11)

Аудит до рефакторинга пяти файлов по спецификации пользователя (Option Parity, факто-логика,
единственность ответа, неугадываемость по форме, fact-freshness). Гейт —
`scripts/mcq-answer-parity-gate.py` (детерминированные проверки §7). Машинный снимок —
`gate-snapshot-before.json`.

## Сводка гейта (все 5 файлов — FAIL)

| Файл | Q | CLR ≤0.40 | LEN_RATIO ≤1.35 | AVG 0.9–1.1 | SENT ≤1 | DETAIL ≤0.40 | ABS_GAP ≤0.25 | POS ≤2.0 | DUP ≤3 | STYLE ≤0.40 | SRC |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--|
| api-versioning | 20 | **0.65** | **2.94** | **1.21** | **3** | **0.45** | 0.05 ✓ | 1.0 ✓ | **17** | **0.68** | ✗ |
| hibernate-relationships | 15 | **0.67** | **2.80** | **1.43** | **3** | **1.00** | 0.09 ✓ | 1.33 ✓ | 0 ✓ | **1.00** | ✗ |
| linkerd | 20 | **0.95** | **3.71** | **1.68** | **4** | **0.80** | **0.35** | 1.0 ✓ | 0 ✓ | **0.95** | ✗ |
| resilience4j | 20 | **0.70** | **13.0** | **1.34** | 1 ✓ | **0.70** | 0.13 ✓ | 1.0 ✓ | 0 ✓ | **0.91** | ✗ |
| conflict-resolution | 20 | **0.95** | **3.31** | **1.69** | **2** | **0.65** | **0.37** | 1.0 ✓ | 0 ✓ | **0.95** | n/a |

Жирным — проваленные проверки. `CLR` = доля вопросов, где correct самый длинный. `STYLE` =
детерминированный proxy «угадать correct без знания темы» (шанс = 0.25).

## Диагноз (совпадает с жалобой пользователя)

1. **Correct систематически длиннее и насыщеннее.** В 65–95% вопросов правильный вариант — самый
   длинный; средняя длина correct/incorrect до **1.69×**; correct доминирует по code-spans/числам/
   скобкам вплоть до 100% блоков (hibernate). Ответ угадывается по форме на 68–100%.
2. **Дистракторы — заглушки и карикатуры.** linkerd и conflict-resolution: неправильные варианты
   заметно чаще несут абсолютные маркеры (`всегда/никогда/только`) — разрыв 0.35–0.37 → угадываются
   по тону.
3. **Копипаста.** api-versioning: 17 повторяющихся 10-грамм между разными опциями (варианты собраны
   из одинаковых кусков) — форма плоская и предсказуемая.
4. **Нет фактологической прослеживаемости.** 4 из 5 файлов version-sensitive (linkerd, resilience4j,
   api-versioning, hibernate) содержат версии/годы/«current/latest/deprecated», но ни один не имеет
   fact-freshness sidecar с целевой версией, датой проверки и первичными источниками.

## Что делаем дальше

1. `scripts/mcq-answer-parity-gate.py` — новый жёсткий гейт (создан).
2. Fact-freshness sidecars `docs/mcq-quality/fact-freshness/<topic>.json` (версия + дата + источники).
3. Пофайловый рефакторинг: фактология (§5) → переписать дистракторы под паритет (не раздувать) →
   гейт зелёный → verify-mcq-json = 0 → независимая проверка reviewer-субагентом.
4. Обновление `mcq-quality-fixer` SKILL: числовые пороги паритета + требование fact-freshness + LLM
   blind-checks.

Итоговый отчёт с метриками до/после — `audit-2026-07-11-after.md` (по завершении).
