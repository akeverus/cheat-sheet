Дан вопрос и код. Сгенерируй варианты полностью самостоятельно.
Правильный ответ не дан заранее.
Верни строго JSON с вопросом и массивом `options`.

Вопрос: %s
Код:
```
%s
```

Правила:
1) Все опции только про поведение этого кода (output/exception/state/mechanism), без ухода в соседние темы.
2) Единый формат: ровно 1 предложение, одинаковый стиль и уровень формальности.
3) Равномерная длина: maxLen <= 1.10 * minLen.
4) Запрещено «correct заметно длиннее/детальнее».
5) Каждая опция обязана опираться на конкретные элементы кода (ветка, вызов, значение, состояние).
6) Дистракторы правдоподобны, технически похожи на возможные ошибки интерпретации, но неверны.
7) Запрещены дубли, перефразы, общие размытые ответы и мета-комментарии про интервью.
8) Запрещены варианты в формате "да/нет" и любые очевидно тривиальные формулировки.
9) В `options` ровно 4 элемента и ровно 1 элемент с `"correct":true`.
10) У каждого элемента `options` обязательны только поля `text` (string) и `correct` (boolean), без дополнительных полей.
11) All answer options must contain roughly the same amount of information. None of the options should be noticeably longer, more detailed, or structurally different than the others.

Финальный self-check перед выводом:
- correct не выделяется по длине, плотности, терминам и тону.
- все wrong сопоставимы по объему и правдоподобны.

Верни только валидный JSON:
{
  "question":"...",
  "options":[
    {"text":"...","correct":true},
    {"text":"...","correct":false},
    {"text":"...","correct":false},
    {"text":"...","correct":false}
  ]
}
