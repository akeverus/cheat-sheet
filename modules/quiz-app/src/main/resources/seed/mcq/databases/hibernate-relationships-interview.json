{
  "topic_slug": "hibernate-relationships-interview",
  "questions": [
    {
      "q_number": 1,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какие типы связей (ассоциаций) поддерживает `Hibernate/JPA`?",
          "options": [
            {
              "label": "A",
              "text": "`@OneToMany` и `@ManyToOne` считаются нативными, а `@OneToOne` с `@ManyToMany` — обёртки поверх них. Их собирают руками через дополнительный `@JoinColumn` или `@JoinTable` над той же парой полей.",
              "correct": false,
              "sections": {
                "what_actually": "`@OneToOne` и `@ManyToMany` — полноценные аннотации `JPA`, а не ручная эмуляция; их обрабатывает сам провайдер.",
                "source_of_confusion": "`@OneToMany`/`@ManyToOne` встречаются чаще всего, поэтому кажется, что остальные второстепенны или отсутствуют.",
                "if_it_were_true": "Тогда нельзя было бы объявить `@JoinTable` для `@ManyToMany`, но эта связь работает из коробки без ручных коллекций.",
                "how_it_should_be": "Поддерживаются все четыре кардинальности: `@OneToOne`, `@OneToMany`, `@ManyToOne`, `@ManyToMany` — как самостоятельные аннотации."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`JOIN` в `JPQL` или нативном `SQL` — так идёт навигация по связям. Аннотации вроде `@OneToMany` лишь задают имена FK-колонок, а объектных ссылок между `entity` сами не создают.",
              "correct": false,
              "sections": {
                "what_actually": "Аннотации `@OneToMany`, `@ManyToOne` и прочие — ядро ORM-модели `Hibernate`; именно они описывают связи декларативно.",
                "source_of_confusion": "`JOIN` действительно используется на уровне SQL, и это путают с отсутствием объектных ассоциаций в маппинге.",
                "if_it_were_true": "Тогда `Hibernate` не был бы ORM, а просто генератором SQL, но он маппит ассоциации на поля объектов.",
                "how_it_should_be": "Связи объявляются аннотациями над полями entity, а `Hibernate` сам транслирует их в нужные SQL `JOIN`/подзапросы."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`@ManyToOne` и `@OneToOne` под капотом — тот же `@ManyToMany`: каждая связь хранится через junction-таблицу. Отличие лишь в `unique`-ограничении на одной стороне таблицы `@JoinTable`.",
              "correct": false,
              "sections": {
                "what_actually": "`@ManyToMany` — отдельный тип с junction-таблицей; `@ManyToOne` хранит FK в самой таблице entity и устроена иначе.",
                "source_of_confusion": "Логически любую связь можно свести к таблице связей, что создаёт иллюзию универсальности `@ManyToMany`.",
                "if_it_were_true": "Тогда каждая `@ManyToOne`-связь создавала бы лишнюю junction-таблицу, но FK хранится прямо в колонке entity.",
                "how_it_should_be": "Каждая кардинальность реализуется по-своему: `@ManyToOne` — через FK-колонку, `@ManyToMany` — через отдельную junction-таблицу."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`Hibernate` поддерживает четыре кардинальности ассоциаций: `@OneToOne`, `@OneToMany`, `@ManyToOne` и `@ManyToMany`. Каждая объявляется аннотацией над полем-ссылкой и отражает связь один-к-одному, один-ко-многим либо многие-ко-многим.",
              "correct": true,
              "sections": {
                "explanation": "Эти четыре аннотации соответствуют кардинальности связи в реляционной модели: 1:1, 1:N, N:1 и N:M; они объявляются над полем-ссылкой на связанную entity.",
                "example": "`@OneToMany(mappedBy = \"order\") List<OrderItem> items` на стороне `Order` и `@ManyToOne @JoinColumn(name = \"order_id\") Order order` на стороне `OrderItem` — типичная пара для связи родитель-дети.",
                "when_to_apply": "`@OneToMany` + `@ManyToOne` ставят на двунаправленную связь родитель-дети; `@ManyToMany` — когда обе стороны коллекции и нет дополнительных полей в junction-таблице.",
                "edge_cases": "`@ManyToOne` и `@OneToMany` — это две стороны одной FK-связи; `@ManyToMany` под капотом всегда создаёт отдельную junction-таблицу, даже если вы её не объявили явно.",
                "related": "[[hibernate-relationships-interview#Q2]] unidirectional vs bidirectional; [[hibernate-relationships-interview#Q7]] моделирование @ManyToMany; [[hibernate-relationships-interview#Q9]] варианты @OneToOne."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 2,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Чем отличается unidirectional от bidirectional связей в `Hibernate`?",
          "options": [
            {
              "label": "A",
              "text": "В unidirectional о связи знает одна сторона `Order` с `@OneToMany` и `@JoinColumn`, в bidirectional — обе. Owning side несёт FK, inverse — `@OneToMany` с `mappedBy` и helper-метод синхронизации.",
              "correct": true,
              "sections": {
                "explanation": "`mappedBy` указывает на поле owning side и говорит `Hibernate`, что эта сторона не управляет FK; owning side — та, у которой `@JoinColumn` без `mappedBy`.",
                "example": "`addItem(OrderItem i) { items.add(i); i.setOrder(this); }` — helper-метод, который держит обе стороны bidirectional-связи синхронными при добавлении элемента.",
                "when_to_apply": "Bidirectional нужен, когда требуется навигация с обеих сторон и удобная синхронизация коллекции и FK через helper-методы `addItem`/`removeItem`.",
                "edge_cases": "Без синхронизации обеих сторон (`item.setOrder(this)`) в bidirectional коллекция в памяти и FK в БД могут разойтись до flush, давая неверное состояние.",
                "related": "[[hibernate-relationships-interview#Q1]] типы связей; [[hibernate-relationships-interview#Q10]] @JoinColumn vs @JoinTable; [[hibernate-relationships-interview#Q15]] best practices синхронизации."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "Unidirectional и `@OneToMany`-bidirectional — один и тот же маппинг, отличие лишь в стиле кода. На генерируемый `SQL` и на то, чья `entity` держит `@JoinColumn` и `mappedBy`, выбор не влияет.",
              "correct": false,
              "sections": {
                "what_actually": "Bidirectional требует `mappedBy` и определяет owning side, тогда как unidirectional знает о связи только с одной стороны — это разные модели.",
                "source_of_confusion": "Обе связи маппят одни и те же таблицы, поэтому кажется, что отличие чисто косметическое.",
                "if_it_were_true": "Тогда не нужны были бы helper-методы синхронизации обеих сторон, но без них коллекция и FK расходятся.",
                "how_it_should_be": "Unidirectional — одна сторона знает о другой; bidirectional — обе, и тогда нужен `mappedBy` для inverse side."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "В bidirectional `mappedBy` вешают на owning side `Order`, где стоит `@JoinColumn`, а не на inverse. Сторона `@OneToMany` остаётся без аннотации, а `mappedBy` отвечает за навигацию и FK не затрагивает.",
              "correct": false,
              "sections": {
                "what_actually": "`mappedBy` ставится именно на inverse side; owning side держит `@JoinColumn` и владеет FK, без `mappedBy`.",
                "source_of_confusion": "Owning side кажется «главной», поэтому интуитивно хочется навесить `mappedBy` именно на неё.",
                "if_it_were_true": "Тогда `Hibernate` создал бы две конкурирующие FK-колонки для одной связи, но `mappedBy` как раз убирает дублирование.",
                "how_it_should_be": "`mappedBy` на inverse side (`@OneToMany`), `@JoinColumn` без `mappedBy` — на owning side (`@ManyToOne`)."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "Односторонняя связь сводится к двунаправленной: любая `@OneToMany` по сути двусторонняя. Провайдер сам заводит обратное поле `mappedBy` на второй `entity` `Order`, даже если вы `@JoinColumn` не объявляли.",
              "correct": false,
              "sections": {
                "what_actually": "Unidirectional абсолютно валидна: `@ManyToOne` без обратного `@OneToMany` или `@OneToMany` с `@JoinColumn` — частые однонаправленные случаи.",
                "source_of_confusion": "В примерах часто показывают bidirectional ради навигации, отсюда впечатление, что односторонняя связь «не настоящая».",
                "if_it_were_true": "Тогда `Hibernate` сам бы добавлял поле `order` в `OrderItem`, но он не генерирует обратные ссылки автоматически.",
                "how_it_should_be": "Обратное поле появляется, только если вы его явно объявите; unidirectional связи полностью поддерживаются."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 3,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что такое `FetchType.LAZY` и `FetchType.EAGER`, чем они отличаются?",
          "options": [
            {
              "label": "A",
              "text": "`LAZY` — подсказка отложить связь до первого обращения, `EAGER` — требование грузить сразу, `JOIN`-ом либо отдельным запросом. По умолчанию коллекции ленивы, а `@ManyToOne` — нет.",
              "correct": true,
              "sections": {
                "explanation": "`LAZY` — лишь подсказка провайдеру (он вправе загрузить и сразу), `EAGER` — требование грузить немедленно; при `EAGER` `Hibernate` использует `JOIN` в `em.find`, но в `JPQL`-запросе без `JOIN FETCH` — отдельный `SELECT`.",
                "example": "`em.find(Order.class, 1L)` при `LAZY` `items` даёт один `SELECT` за заказом; обращение `order.getItems().size()` инициирует второй `SELECT` за `order_items`.",
                "when_to_apply": "Рекомендуется явно ставить `LAZY` на все связи (`@ManyToOne(fetch = LAZY)`), а нужные данные подтягивать `JOIN FETCH`/`@EntityGraph` в конкретных запросах.",
                "edge_cases": "`LAZY` вне открытой сессии бросает `LazyInitializationException`; `EAGER` от этого спасает, но не фильтруется под запрос и на списках сам порождает N+1.",
                "related": "[[hibernate-relationships-interview#Q4]] N+1 проблема; [[hibernate-relationships-interview#Q11]] @EntityGraph для загрузки; [[hibernate-relationships-interview#Q9]] LAZY в @OneToOne."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`LAZY` и `EAGER` отличаются лишь оборачиванием связи в прокси-объект. Данные в обоих режимах приходят тем же `SELECT` за parent, просто `LAZY` откладывает разворачивание в поля `entity`.",
              "correct": false,
              "sections": {
                "what_actually": "`EAGER` грузит связь сразу вместе с parent, `LAZY` — отдельным запросом при первом обращении; это разное число и время SQL-запросов.",
                "source_of_confusion": "Оба режима в итоге дают доступ к данным связи, поэтому разницу во времени загрузки недооценивают.",
                "if_it_were_true": "Тогда не было бы `LazyInitializationException` вне сессии, но при `LAZY` он возникает именно из-за отложенной загрузки.",
                "how_it_should_be": "`EAGER` подгружает связь немедленно (часто `JOIN`), `LAZY` откладывает загрузку до первого обращения к полю."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`@OneToMany`-коллекции по умолчанию грузятся `EAGER`, а `@ManyToOne` — `LAZY`. Поэтому именно коллекции чаще всего тянут лишние данные и раздувают `SELECT`, а не одиночные `@ManyToOne`.",
              "correct": false,
              "sections": {
                "what_actually": "Наоборот: дефолт для `*ToMany` — `LAZY`, а для `*ToOne` (`@ManyToOne`, `@OneToOne`) — `EAGER`.",
                "source_of_confusion": "Коллекции выглядят «тяжёлыми», поэтому кажется, что по умолчанию они должны грузиться сразу.",
                "if_it_were_true": "Тогда каждая выборка `Order` тянула бы все `items` без запроса, но `@OneToMany` ленив по умолчанию.",
                "how_it_should_be": "Дефолты обратные: `@OneToMany`/`@ManyToMany` — `LAZY`, `@ManyToOne`/`@OneToOne` — `EAGER`."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`EAGER` обычно выгоднее `LAZY`: один `JOIN` за parent и связью дешевле множества запросов. На часто читаемых `@ManyToOne`- и `@OneToMany`-связях его и стоит предпочитать.",
              "correct": false,
              "sections": {
                "what_actually": "`EAGER` грузит данные даже когда они не нужны и не фильтруется; на больших коллекциях это раздувает результат и порождает N+1 при списках.",
                "source_of_confusion": "Идея «один запрос лучше многих» верна локально, но игнорирует, что `EAGER` нельзя отключить для конкретного запроса.",
                "if_it_were_true": "Тогда не было бы рекомендации ставить `LAZY` на все связи, но именно её дают как best practice.",
                "how_it_should_be": "Универсально лучше `LAZY` + точечный `JOIN FETCH`/`@EntityGraph`: так данные грузятся только там, где реально нужны."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 4,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что такое N+1 проблема в `Hibernate` и как её решить?",
          "options": [
            {
              "label": "A",
              "text": "`N+1` — это лишние `JOIN`, что `Hibernate` подставляет для связей при выборке списка. Убираются переводом связей на `EAGER`, чтобы всё пришло единым `SELECT` за списком `entity`.",
              "correct": false,
              "sections": {
                "what_actually": "N+1 — это 1 запрос за родителями + по одному запросу за связью каждого из N родителей; `EAGER` на списках только усугубляет, тоже давая N+1.",
                "source_of_confusion": "Слово `JOIN` ассоциируется с проблемой, поэтому кажется, что лишние запросы — это и есть джойны.",
                "if_it_were_true": "Тогда замена на `EAGER` всё бы чинила, но `EAGER`-коллекция в выборке списка сама порождает N+1.",
                "how_it_should_be": "Это 1 + N отдельных запросов; решают `JOIN FETCH`, `@EntityGraph`, `@BatchSize` или `@Fetch(SUBSELECT)`, а не глобальным `EAGER`."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`N+1` упирается в пропускную способность пула соединений. Увеличьте размер `HikariCP`, и добавочные `SELECT` и `JOIN` за связями `entity` уйдут параллельно, а задержка списка `Order` сгладится.",
              "correct": false,
              "sections": {
                "what_actually": "Проблема в количестве запросов, а не в пропускной способности пула; пул не уменьшает число SQL-вызовов за связями.",
                "source_of_confusion": "Симптом — медленные списки, и его легко спутать с нехваткой ресурсов БД или пула.",
                "if_it_were_true": "Тогда на пустой нагрузке N+1 исчезала бы при большом пуле, но число запросов остаётся 1+N.",
                "how_it_should_be": "Надо сократить число запросов: `JOIN FETCH`, `@EntityGraph`, `@BatchSize`, `SUBSELECT` — а не масштабировать пул."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`N+1` — это один запрос за родителями плюс по отдельному `SELECT` за связью каждого из них. Лечат это `JOIN FETCH`, `@EntityGraph` и `@BatchSize`, а не глобальным переводом на `EAGER`.",
              "correct": true,
              "sections": {
                "explanation": "Причина — ленивая загрузка коллекции, к которой обращаются в цикле: каждый `order.getItems()` инициирует свой `SELECT`, и их количество растёт линейно с N.",
                "example": "`em.createQuery(\"FROM Order o JOIN FETCH o.items\", Order.class).getResultList()` тянет заказы вместе с `items` одним SQL вместо 1+N запросов.",
                "when_to_apply": "Чинят на этапе запроса: `JOIN FETCH o.items` тянет всё одним SQL, `@EntityGraph` делает это декларативно, `@BatchSize(20)` грузит связи пачками по 20.",
                "edge_cases": "`JOIN FETCH` двух `List`-коллекций бросает `MultipleBagFetchException` — тогда используют `Set` или несколько запросов; `@BatchSize` снижает 1+N до 1+ceil(N/size).",
                "related": "[[hibernate-relationships-interview#Q3]] LAZY vs EAGER как источник N+1; [[hibernate-relationships-interview#Q11]] @EntityGraph; [[hibernate-relationships-interview#Q8]] Set против MultipleBagFetchException."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`N+1` — по сути проблема схемы: повесьте индекс на FK-колонку `order_id` связи. Тогда `SELECT` за детьми станут дешёвыми, и деградация списков `Order` уйдёт без правки загрузки `entity`.",
              "correct": false,
              "sections": {
                "what_actually": "Индекс ускоряет каждый из N запросов, но их по-прежнему N; первопричина — стратегия загрузки, а не отсутствие индекса.",
                "source_of_confusion": "Индексы — частый ответ на «медленные запросы», поэтому их применяют и здесь по инерции.",
                "if_it_were_true": "Тогда после индексации `Hibernate Statistics` показывала бы 1 запрос, но останется 1+N.",
                "how_it_should_be": "Снижать нужно само число запросов через `JOIN FETCH`/`@EntityGraph`/`@BatchSize`; индекс — лишь точечная оптимизация каждого."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 5,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что такое `CascadeType` и какие у него варианты?",
          "options": [
            {
              "label": "A",
              "text": "`CascadeType` определяет, грузится ли связь как `LAZY` или `EAGER`, и подбирает нужный `JOIN`. К операциям `PERSIST`, `MERGE`, `REMOVE` над детьми `entity` он отношения не имеет.",
              "correct": false,
              "sections": {
                "what_actually": "`CascadeType` отвечает за распространение операций персистентности (`persist`/`remove`/...), а стратегию загрузки задаёт `FetchType`.",
                "source_of_confusion": "И `fetch`, и `cascade` пишутся в одной аннотации `@OneToMany`, поэтому их роли путают.",
                "if_it_were_true": "Тогда `CascadeType.PERSIST` менял бы поведение `getItems()`, но он влияет только на `em.persist`.",
                "how_it_should_be": "Загрузку определяет `FetchType.LAZY/EAGER`, а `CascadeType` — какие операции каскадируются на детей."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`CascadeType` определяет, какие именно операции `EntityManager` каскадируются с parent на связанных детей: `PERSIST`, `MERGE`, `REMOVE`, `REFRESH`, `DETACH`. Значение `ALL` объединяет весь этот набор сразу.",
              "correct": true,
              "sections": {
                "explanation": "При `em.persist(order)` с `CascadeType.PERSIST` `Hibernate` сам сделает `persist` для каждого `item`; то же логически для `MERGE`, `REMOVE`, `REFRESH`, `DETACH`.",
                "example": "`@OneToMany(mappedBy = \"order\", cascade = {PERSIST, REMOVE})`: `em.persist(order)` сохранит и заказ, и все `items`, а `em.remove(order)` удалит заказ вместе с ними.",
                "when_to_apply": "Каскады ставят на owned-связь родитель→дети (`Order` → `OrderItem`), где жизненный цикл детей привязан к родителю и их не нужно сохранять отдельно.",
                "edge_cases": "`CascadeType.ALL` не включает `orphanRemoval` — это отдельный атрибут; удаление item из коллекции при одном лишь `ALL` не удалит его из БД.",
                "related": "[[hibernate-relationships-interview#Q6]] orphanRemoval; [[hibernate-relationships-interview#Q15]] почему не ставить ALL по умолчанию; [[hibernate-relationships-interview#Q2]] owning side связи."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`CascadeType.ALL` включает в себя и `orphanRemoval`, поэтому убранный из `getItems()` элемент уходит `DELETE` сам. Отдельные `PERSIST`, `MERGE`, `REMOVE`, `REFRESH` для этого не нужны.",
              "correct": false,
              "sections": {
                "what_actually": "`ALL` объединяет `PERSIST/MERGE/REMOVE/REFRESH/DETACH`, но `orphanRemoval` в него не входит и задаётся отдельным флагом.",
                "source_of_confusion": "Оба механизма связаны с удалением дочерних записей, отсюда ложное объединение их в один.",
                "if_it_were_true": "Тогда `order.getItems().remove(item)` при `ALL` всегда удалял бы item, но без `orphanRemoval` он останется в БД.",
                "how_it_should_be": "`orphanRemoval = true` указывается отдельно от `cascade` и реагирует именно на удаление из коллекции, а не на `em.remove(parent)`."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`CascadeType.ALL` на практике хватает одного: он покрывает `PERSIST`, `MERGE`, `REMOVE`, `REFRESH`, `DETACH`. Перечислять их по отдельности над связью `@OneToMany` обычно не требуется.",
              "correct": false,
              "sections": {
                "what_actually": "Каждый `CascadeType` каскадирует свою конкретную операцию, и их комбинируют выборочно, например `{PERSIST, MERGE}` без `REMOVE`.",
                "source_of_confusion": "`ALL` встречается в примерах чаще всего, отсюда впечатление, что остальные не нужны.",
                "if_it_were_true": "Тогда нельзя было бы каскадировать `persist`, но не `remove`, а это типичная безопасная конфигурация.",
                "how_it_should_be": "Доступны независимые значения `PERSIST/MERGE/REMOVE/REFRESH/DETACH`; `ALL` — лишь их объединение, а не единственный вариант."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 6,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что такое `orphanRemoval` и чем он отличается от `CascadeType.REMOVE`?",
          "options": [
            {
              "label": "A",
              "text": "`orphanRemoval` и `CascadeType.REMOVE` работают одинаково: оба шлют `DELETE` по детям `@OneToMany` при `em.remove(parent)`. Триггеры совпадают, поэтому на практике их ставят как эквивалент.",
              "correct": false,
              "sections": {
                "what_actually": "`REMOVE` срабатывает при удалении самого parent, а `orphanRemoval` — при удалении ребёнка из коллекции parent; триггеры разные.",
                "source_of_confusion": "Оба про удаление дочерних записей, поэтому их легко счесть синонимами.",
                "if_it_were_true": "Тогда `order.getItems().remove(item)` без `orphanRemoval` удалял бы item, но он останется в БД.",
                "how_it_should_be": "`REMOVE` каскадирует `em.remove(order)`; `orphanRemoval` ловит именно вынос item из коллекции и удаляет «осиротевший» ребёнок."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`orphanRemoval = true` шлёт `DELETE` по ребёнку при выносе из коллекции: после `getItems().remove(item)` и `flush`. А `CascadeType.REMOVE` срабатывает лишь при `em.remove(order)` над parent.",
              "correct": true,
              "sections": {
                "explanation": "Без `orphanRemoval` удалённый из коллекции `item` остаётся в БД с `order_id = null` (или ловит FK-violation); с ним `Hibernate` выдаёт `DELETE` по этому item.",
                "example": "`@OneToMany(mappedBy = \"order\", cascade = ALL, orphanRemoval = true)`: после `order.getItems().remove(item); em.flush()` `Hibernate` выполнит `DELETE FROM order_items WHERE id = ?`.",
                "when_to_apply": "Флаг ставят на owned-связь, где parent полностью владеет жизненным циклом детей (`Order` → `OrderItem`), и «осиротевшая» строка не имеет смысла без родителя.",
                "edge_cases": "`orphanRemoval` реагирует на удаление из коллекции, `REMOVE` — на удаление parent; их часто включают вместе (`cascade = ALL, orphanRemoval = true`), но это разные триггеры.",
                "related": "[[hibernate-relationships-interview#Q5]] CascadeType и его варианты; [[hibernate-relationships-interview#Q14]] тест на удаление orphan; [[hibernate-relationships-interview#Q15]] orphanRemoval для owned children."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`orphanRemoval` срабатывает при `em.remove(order)` и каскадно шлёт `DELETE` по детям `@OneToMany`. На вынос через `getItems().remove(item)` из коллекции он не реагирует, оставляя строку `order_items`.",
              "correct": false,
              "sections": {
                "what_actually": "Описан механизм `CascadeType.REMOVE`: именно он каскадирует удаление при `em.remove(parent)`, а `orphanRemoval` ловит вынос ребёнка из коллекции.",
                "source_of_confusion": "Оба связаны с удалением, поэтому их триггеры легко поменять местами.",
                "if_it_were_true": "Тогда `order.getItems().remove(item)` не удалял бы item, но именно на это `orphanRemoval` и реагирует.",
                "how_it_should_be": "`orphanRemoval` срабатывает при удалении item из коллекции родителя; каскад при `em.remove(parent)` — это `CascadeType.REMOVE`."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`orphanRemoval` работает лишь в `@ManyToMany` и чистит строки junction-таблицы через `DELETE`. Саму дочернюю `entity` из её таблицы `order_items` он при этом не трогает.",
              "correct": false,
              "sections": {
                "what_actually": "`orphanRemoval` применяется к owned-связям (`@OneToMany`, `@OneToOne`) и удаляет именно дочернюю entity из её таблицы.",
                "source_of_confusion": "Junction-таблица и удаление связей в `@ManyToMany` создают ассоциацию с «удалением связи», но это другой механизм.",
                "if_it_were_true": "Тогда для `@OneToMany` `Order` → `OrderItem` флаг был бы бесполезен, но именно там его чаще всего и применяют.",
                "how_it_should_be": "`orphanRemoval` валиден для owned `@OneToMany`/`@OneToOne` и удаляет дочернюю запись целиком, а не строку junction."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 7,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Как моделировать `@ManyToMany` связь и что делать, когда нужны поля в junction-таблице?",
          "options": [
            {
              "label": "A",
              "text": "Сам `@ManyToMany` требует, чтобы вы объявили `entity` для junction-таблицы и вставляли строки через `EntityManager`. Автоматически `@JoinTable` провайдер не ведёт, `Set<Course>` не хватит.",
              "correct": false,
              "sections": {
                "what_actually": "Простой `@ManyToMany` с `@JoinTable` управляет junction-таблицей автоматически; ручная entity нужна лишь когда в связи появляются доп. поля.",
                "source_of_confusion": "Промежуточный entity действительно нужен в сложном случае, и это переносят на простой `@ManyToMany`.",
                "if_it_were_true": "Тогда нельзя было бы объявить `Set<Course> courses` с `@JoinTable` и сразу работать, но это штатный сценарий.",
                "how_it_should_be": "Для связи без доп. полей хватает `@ManyToMany` + `@JoinTable`; junction-таблицу `Hibernate` ведёт сам."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "Чтобы добавить в junction поле `grade`, достаточно перечислить его прямо в `@JoinTable` через атрибут `columns`. Отдельный `entity` вроде `Enrollment` с полем `enrolled_at` заводить не нужно.",
              "correct": false,
              "sections": {
                "what_actually": "`@JoinTable` описывает только join-колонки (FK); произвольные атрибуты в нём задать нельзя, для этого нужен отдельный entity.",
                "source_of_confusion": "`@JoinTable` параметризуется колонками, отсюда иллюзия, что туда можно дописать любые поля.",
                "if_it_were_true": "Тогда `enrolled_at`/`grade` хранились бы без отдельного класса, но `@ManyToMany` такие поля не поддерживает.",
                "how_it_should_be": "Доп. поля требуют замены `@ManyToMany` на промежуточный entity (`Enrollment`) с `@ManyToOne` к обеим сторонам."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "На обеих сторонах `@ManyToMany` надо указать `mappedBy`, иначе `Hibernate` заведёт две junction-таблицы. Общий `@JoinTable` без owning side связать `Set<Course>` не сможет.",
              "correct": false,
              "sections": {
                "what_actually": "`mappedBy` ставится только на одной (inverse) стороне; owning side держит `@JoinTable`, и junction-таблица одна.",
                "source_of_confusion": "Симметричность `@ManyToMany` создаёт ощущение, что и `mappedBy` должен быть симметричным.",
                "if_it_were_true": "Тогда не было бы owning side, и `Hibernate` не знал бы, кто управляет junction-таблицей.",
                "how_it_should_be": "`@JoinTable` — на owning side, `mappedBy` — на inverse; так обе стороны делят одну junction-таблицу."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "Простой `@ManyToMany` маппится через `@JoinTable` плюс `mappedBy` на обратной стороне. Когда в связи нужны поля, её заменяют на `entity` `Enrollment` с двумя `@ManyToOne` к сторонам.",
              "correct": true,
              "sections": {
                "explanation": "`Student` ↔ `Enrollment` ↔ `Course`: `Enrollment` держит `@EmbeddedId EnrollmentId`, `@ManyToOne @MapsId(\"studentId\")` и `@ManyToOne @MapsId(\"courseId\")`, плюс `enrolledAt` и `grade`.",
                "example": "Простой случай: `@ManyToMany @JoinTable(name = \"student_course\", joinColumns = @JoinColumn(name = \"student_id\"), inverseJoinColumns = @JoinColumn(name = \"course_id\")) Set<Course> courses`.",
                "when_to_apply": "Промежуточную entity вводят, как только junction-таблице нужны собственные атрибуты или поведение; для чистой связи N:M достаточно `@ManyToMany` с `@JoinTable`.",
                "edge_cases": "Удаление через `student.getCourses().remove(course)` при чистом `@ManyToMany` стирает строку junction вместе с её полями — поэтому при наличии `grade` нужен явный entity и управление им.",
                "related": "[[hibernate-relationships-interview#Q1]] @ManyToMany среди типов связей; [[hibernate-relationships-interview#Q10]] @JoinTable vs @JoinColumn; [[hibernate-relationships-interview#Q13]] потеря полей при удалении @ManyToMany."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 8,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какие коллекции использовать для `@OneToMany` и как выбирать между `Set` и `List`?",
          "options": [
            {
              "label": "A",
              "text": "Доступны `List`, `Set`, `Bag`, `SortedSet` и `Map` c `@MapKey`; конкретный выбор зависит от семантики самой связи. Порядок элементов диктует `List`, а от дублей и `MultipleBagFetchException` защищает `Set`.",
              "correct": true,
              "sections": {
                "explanation": "`@OrderBy(\"price DESC\") List<OrderItem> items` сортирует при загрузке; `Set<Comment>` исключает дубликаты; `@MapKey(name = \"type\") Map<String, Phone> phones` даёт доступ по ключу `phone.type`.",
                "example": "`@OneToMany Set<OrderItem> items` плюс `@OneToMany Set<Shipment> shipments` позволяют `JOIN FETCH` обеих коллекций сразу, тогда как две `List` дали бы `MultipleBagFetchException`.",
                "when_to_apply": "Выбор зависит от семантики: `List` — когда важен порядок (`@OrderBy`/`@OrderColumn`); `Set` — против дублей и для безопасного `JOIN FETCH` нескольких коллекций; `Bag` — самый дешёвый, без индекса.",
                "edge_cases": "`JOIN FETCH` двух `List` (двух bag) бросает `MultipleBagFetchException`; замена на `Set` снимает именно этот случай, а общий N+1 лечат `@BatchSize`, отдельными запросами или `@EntityGraph`.",
                "related": "[[hibernate-relationships-interview#Q4]] JOIN FETCH и N+1; [[hibernate-relationships-interview#Q13]] MultipleBagFetchException как типичная ошибка; [[hibernate-relationships-interview#Q15]] Set vs List в best practices."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "Сам `@OneToMany` рассчитан на `List<T>`; порядок `Hibernate` связывает с детьми через `@OrderColumn`. Коллекции `Set<T>`, `Bag` и `Map<K,V>` для ассоциаций он не применяет, ожидая именно `List<T>`.",
              "correct": false,
              "sections": {
                "what_actually": "`Hibernate` поддерживает `List`, `Set`, `Bag`, `SortedSet`/`TreeSet` и `Map` для `@OneToMany` — выбор зависит от семантики.",
                "source_of_confusion": "`List` — самый частый тип в примерах, отсюда ощущение, что других нет.",
                "if_it_were_true": "Тогда нельзя было бы объявить `Set<Comment> comments` или `Map<String, Phone>`, но это валидные маппинги.",
                "how_it_should_be": "Доступны `List`, `Set`, `Bag`, `SortedSet`, `Map`; `Set` особенно полезен для bidirectional-связей."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "Тип `List` почти всегда лучше `Set`; он держит порядок вставки, а `Set` тратит время на `hashCode`. На больших коллекциях `Set<Comment>` заметно медленнее по `contains`, чем `List`, `Bag` или `SortedSet`.",
              "correct": false,
              "sections": {
                "what_actually": "`Set` быстрее для `contains`, гарантирует уникальность и помогает избежать `MultipleBagFetchException` при `JOIN FETCH` двух коллекций.",
                "source_of_confusion": "`List` сохраняет порядок вставки, что кажется универсальным преимуществом для любых коллекций.",
                "if_it_were_true": "Тогда не было бы правила «`Set` предпочтительнее для bidirectional», но именно оно даётся как best practice.",
                "how_it_should_be": "`Set` предпочитают в bidirectional-связях; `List` берут, когда важен порядок (с `@OrderBy`/`@OrderColumn`)."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "Словарь `Map<K,V>` в `@OneToMany` не поддерживается; `Hibernate` работает здесь с линейными `List`, `Set` и `Bag` без ключей. Аннотация `@MapKey` для таких ассоциаций остаётся без эффекта.",
              "correct": false,
              "sections": {
                "what_actually": "`Map` поддерживается через `@MapKey(name = \"...\")`, где ключом служит поле дочерней entity (например `phone.type`).",
                "source_of_confusion": "Map ассоциируется со «странной» структурой для связи, отсюда сомнение в её поддержке.",
                "if_it_were_true": "Тогда нельзя было бы написать `Map<String, Phone> phones` с `@MapKey`, но это рабочий маппинг.",
                "how_it_should_be": "`Map` валиден: `@MapKey` задаёт ключ из поля child entity, давая индексированный доступ к связанным записям."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 9,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Как работает `@OneToOne` и какие у него варианты (shared PK vs FK)?",
          "options": [
            {
              "label": "A",
              "text": "`@OneToOne` при строгом один-к-одному держит обе стороны в одной таблице: он сливает колонки `User` и `UserProfile` в общую строку без `@JoinColumn` и `@MapsId`.",
              "correct": false,
              "sections": {
                "what_actually": "Даже shared-PK вариант оставляет две таблицы с общим значением `id`; FK-вариант — две таблицы со связью через `profile_id`.",
                "source_of_confusion": "Кардинальность 1:1 наводит на мысль, что данные логичнее держать в одной строке.",
                "if_it_were_true": "Тогда `@MapsId` и `@JoinColumn` были бы не нужны, но они как раз связывают две отдельные таблицы.",
                "how_it_should_be": "`@OneToOne` связывает две таблицы — через общий PK (`@MapsId`) или через FK-колонку (`@JoinColumn`)."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`fetch = LAZY` на `@OneToOne` соблюдается как в прочих связях, например `@ManyToOne`. Вторая сторона `UserProfile` подтягивается лишь при первом обращении к полю, без `EAGER`.",
              "correct": false,
              "sections": {
                "what_actually": "Для optional `@OneToOne` `Hibernate` не может узнать о наличии связи без запроса и часто грузит её `EAGER`, несмотря на `fetch = LAZY`.",
                "source_of_confusion": "`LAZY` объявлен в аннотации, отсюда вывод, что он гарантированно соблюдается.",
                "if_it_were_true": "Тогда `optional = false` + bytecode enhancement были бы не нужны для ленивости 1:1, но их рекомендуют именно поэтому.",
                "how_it_should_be": "Истинный `LAZY` в `@OneToOne` достигается только при `optional = false` и bytecode enhancement; иначе провайдер часто грузит `EAGER`."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`@OneToOne` бывает shared-PK через `@MapsId` и общий `id` с parent, либо FK с отдельной колонкой `@JoinColumn`. Для bidirectional inverse side берёт `mappedBy`, а owning держит `@JoinColumn`.",
              "correct": true,
              "sections": {
                "explanation": "Shared PK экономит колонку и гарантирует 1:1 на уровне ключа: `profile.id == user.id`; FK-вариант хранит отдельный `profile_id` и допускает `optional`-связь.",
                "example": "Shared PK: `@OneToOne @MapsId @JoinColumn(name = \"id\") User user` в `UserProfile`. FK: `@OneToOne @JoinColumn(name = \"profile_id\") UserProfile profile` в `User`.",
                "when_to_apply": "Shared PK выбирают, когда профиль не существует без пользователя и делит его identity; FK-вариант — когда связь опциональна или маппится на готовую схему с отдельной колонкой.",
                "edge_cases": "`LAZY` в `@OneToOne` часто не работает: при 1:1 `Hibernate` не знает, есть ли связь, без запроса, и делает `EAGER`; помогает `optional = false` + bytecode enhancement.",
                "related": "[[hibernate-relationships-interview#Q1]] @OneToOne среди типов связей; [[hibernate-relationships-interview#Q3]] почему LAZY проблемен; [[hibernate-relationships-interview#Q10]] @JoinColumn для FK-варианта."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`@OneToOne` в shared-PK варианте настраивают через `@JoinTable`: связь живёт в отдельной таблице с двумя FK. Общий `id` и `@MapsId` между `User` и `UserProfile` тут не нужны.",
              "correct": false,
              "sections": {
                "what_actually": "Shared PK реализуется через `@MapsId` + общий `id`, без junction-таблицы; `@JoinTable` для 1:1 не используется.",
                "source_of_confusion": "`@JoinTable` ассоциируется со связями вообще, и его ошибочно переносят на `@OneToOne`.",
                "if_it_were_true": "Тогда `profile.id` не совпадал бы с `user.id`, но смысл shared PK именно в общем значении ключа.",
                "how_it_should_be": "Shared PK — это `@MapsId` и одинаковый `id` в двух таблицах; отдельная таблица связей здесь не нужна."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 10,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Чем отличаются `@JoinColumn` и `@JoinTable` и когда что применять?",
          "options": [
            {
              "label": "A",
              "text": "`@JoinColumn` и `@JoinTable` дают один результат: оба выносят связь в отдельную таблицу с парой FK. Разница между `@JoinColumn` на `@ManyToOne` и `@JoinTable` сводится к синтаксису.",
              "correct": false,
              "sections": {
                "what_actually": "`@JoinColumn` кладёт FK прямо в таблицу самой entity, а `@JoinTable` создаёт отдельную таблицу связей — это разные схемы.",
                "source_of_confusion": "Оба относятся к маппингу связей, поэтому их роли смешивают.",
                "if_it_were_true": "Тогда `@ManyToOne` с `@JoinColumn(\"customer_id\")` рождал бы лишнюю таблицу, но FK уходит в колонку `orders`.",
                "how_it_should_be": "`@JoinColumn` — FK в таблице entity; `@JoinTable` — отдельная junction-таблица (обычно для `@ManyToMany`)."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`@JoinColumn` кладёт FK в таблицу самой `entity`: `@ManyToOne @JoinColumn(customer_id)` даёт колонку в `orders`. А `@JoinTable` создаёт отдельную таблицу связей — типично для `@ManyToMany`.",
              "correct": true,
              "sections": {
                "explanation": "`CREATE TABLE orders (id, customer_id, ...)` — это `@JoinColumn`; `CREATE TABLE student_course (student_id, course_id)` — это `@JoinTable` с `joinColumns`/`inverseJoinColumns`.",
                "example": "`@ManyToOne @JoinColumn(name = \"customer_id\") Customer customer` кладёт FK в `orders`; `@ManyToMany @JoinTable(name = \"student_course\", ...)` создаёт отдельную таблицу `student_course`.",
                "when_to_apply": "`@JoinColumn` берут, когда FK помещается в таблицу entity (`@ManyToOne`, FK-`@OneToOne`); `@JoinTable` — когда нужна отдельная таблица связей, обычно для N:M.",
                "edge_cases": "Технически `@JoinTable` можно навесить и на `@OneToMany`, чтобы вынести FK из таблицы детей в отдельную таблицу — но это редкий и обычно нежелательный приём.",
                "related": "[[hibernate-relationships-interview#Q2]] owning side и @JoinColumn; [[hibernate-relationships-interview#Q7]] @JoinTable для @ManyToMany; [[hibernate-relationships-interview#Q9]] @JoinColumn в FK-варианте @OneToOne."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`@JoinTable` рассчитан на `@ManyToMany`. В `@OneToMany` или `@ManyToOne` он приводит к ошибке маппинга при старте. Вынести FK `customer_id` в отдельную таблицу через `@JoinTable` так не выйдет.",
              "correct": false,
              "sections": {
                "what_actually": "`@JoinTable` чаще всего применяют в `@ManyToMany`, но он валиден и для `@OneToMany`/`@ManyToOne`, когда FK выносят в отдельную таблицу.",
                "source_of_confusion": "Junction-таблица прочно ассоциируется с N:M, отсюда вывод об исключительности.",
                "if_it_were_true": "Тогда нельзя было бы вынести связь `@OneToMany` в отдельную таблицу, но `@JoinTable` это позволяет.",
                "how_it_should_be": "`@JoinTable` чаще всего нужен для `@ManyToMany`, но технически допустим и в других связях."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`@JoinColumn` задаёт имя самой таблицы `entity`, а имя FK-колонки `customer_id` провайдер берёт сам. Переопределить его через `@JoinColumn(name)` вместо `@Table` не получится.",
              "correct": false,
              "sections": {
                "what_actually": "`@JoinColumn(name = ...)` задаёт именно имя FK-колонки, а не таблицы; таблица именуется через `@Table`.",
                "source_of_confusion": "Слово «column» рядом с «join» сбивает: кажется, что речь о таблице связи.",
                "if_it_were_true": "Тогда `@JoinColumn(name = \"customer_id\")` не влиял бы на имя колонки, но он его и определяет.",
                "how_it_should_be": "`@JoinColumn(name = ...)` управляет именем FK-колонки в таблице entity; имя таблицы — отдельно через `@Table`."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 11,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Как использовать `@EntityGraph` для оптимизации загрузки связей?",
          "options": [
            {
              "label": "A",
              "text": "`@EntityGraph` меняет стратегию загрузки всей `entity` разом: перечисленные связи становятся `EAGER` во всех запросах. Точечно держать их `LAZY` при этом не выйдет.",
              "correct": false,
              "sections": {
                "what_actually": "`@EntityGraph` действует на конкретный запрос/`find`, а не глобально: вне его связи остаются ленивыми, как объявлено.",
                "source_of_confusion": "Декларативность графа создаёт ощущение «постоянной» настройки на уровне entity.",
                "if_it_were_true": "Тогда нельзя было бы держать `LAZY` по умолчанию и подтягивать связь точечно, но именно в этом смысл графа.",
                "how_it_should_be": "`@EntityGraph` задаёт fetch-план для отдельного запроса; глобальная стратегия по-прежнему определяется `FetchType`."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`@EntityGraph` задаётся лишь программно через `em.createEntityGraph()` вместе с `fetchgraph`-hint у `find`. Аннотацией `@EntityGraph(attributePaths)` над методом репозитория `findById` его никак не объявить.",
              "correct": false,
              "sections": {
                "what_actually": "Граф задаётся и аннотацией (`@EntityGraph(attributePaths = ...)` или именованный `@NamedEntityGraph`), и программно — оба способа валидны.",
                "source_of_confusion": "Программный API выглядит «настоящим», и про декларативный вариант забывают.",
                "if_it_were_true": "Тогда нельзя было бы написать `@EntityGraph(\"Order.withItems\")` над `findById`, но это штатный способ в Spring Data.",
                "how_it_should_be": "`@EntityGraph` доступен и как аннотация над методом репозитория, и программно через `EntityManager`-hints."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`@EntityGraph` — лишь необязательный совет: связи из `@EntityGraph(attributePaths)` провайдер вправе проигнорировать. Тогда `LAZY`-поля грузятся по одному, и N+1 сохраняется.",
              "correct": false,
              "sections": {
                "what_actually": "`@EntityGraph` — не необязательный hint: для fetchgraph/loadgraph Hibernate гарантированно грузит перечисленные `attributePaths` одним `JOIN`, устраняя N+1.",
                "source_of_confusion": "JPA-спека формально зовёт это «hint», отсюда впечатление, будто провайдер волен его проигнорировать.",
                "if_it_were_true": "Если бы граф игнорировался, `@EntityGraph` не решал бы N+1 и был бы бесполезен как инструмент оптимизации.",
                "how_it_should_be": "Корректно: `@EntityGraph` надёжно подтягивает указанные связи за один запрос, оставляя прочие поля `LAZY`."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`@EntityGraph` — это декларативный fetch-план для одного конкретного запроса. Задаётся именованным `@NamedEntityGraph`, ad-hoc `@EntityGraph(attributePaths)` над методом или программно через `em.createEntityGraph()`, оставляя связи `LAZY`.",
              "correct": true,
              "sections": {
                "explanation": "`@EntityGraph(\"Order.withItems\") Optional<Order> findById(Long id)` подтянет `items` и `customer` одним планом, решая N+1 без `JOIN FETCH` в JPQL.",
                "example": "Программно: `EntityGraph<Order> g = em.createEntityGraph(Order.class); g.addAttributeNodes(\"items\", \"customer\"); em.find(Order.class, 1L, Map.of(\"jakarta.persistence.fetchgraph\", g))`.",
                "when_to_apply": "Граф применяют, чтобы переиспользовать fetch-план в нескольких запросах и держать связи `LAZY` по умолчанию, подтягивая нужное точечно.",
                "edge_cases": "В отличие от инлайнового `JOIN FETCH`, `@EntityGraph` декларативнее и переиспользуется, но для одной выборки оба дают одинаковый результат — это вопрос стиля.",
                "related": "[[hibernate-relationships-interview#Q4]] @EntityGraph как решение N+1; [[hibernate-relationships-interview#Q3]] LAZY по умолчанию; [[hibernate-relationships-interview#Q15]] @EntityGraph в best practices."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 12,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Как работает `@ElementCollection` и когда её применять вместо `@OneToMany`?",
          "options": [
            {
              "label": "A",
              "text": "`@ElementCollection` работает как `@OneToMany`: оба маппят коллекцию полноценных `entity` с собственным `id` и ключом. Сослаться на элемент `@CollectionTable` из другой `entity` можно так же.",
              "correct": false,
              "sections": {
                "what_actually": "`@ElementCollection` хранит value-типы или `@Embeddable` без собственной identity, тогда как `@OneToMany` связывает именно entity с PK.",
                "source_of_confusion": "Обе аннотации описывают коллекции в отдельной таблице, отсюда смешение.",
                "if_it_were_true": "Тогда на элемент `@ElementCollection` можно было бы сослаться из другой entity, но у него нет identity.",
                "how_it_should_be": "`@ElementCollection` — для коллекций значений/embeddable без PK; `@OneToMany` — для коллекций entity со своей identity."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "`@ElementCollection` хранит элементы прямо в таблице владельца как массив в одной строке. Отдельная `@CollectionTable` с `@JoinColumn(user_id)` под коллекцию `String` не заводится.",
              "correct": false,
              "sections": {
                "what_actually": "Элементы выносятся в отдельную таблицу, задаваемую `@CollectionTable` с `@JoinColumn` на владельца.",
                "source_of_confusion": "Идея «простые значения хранятся рядом» создаёт иллюзию массива в одной строке.",
                "if_it_were_true": "Тогда `@CollectionTable(name = \"user_emails\")` был бы не нужен, но он как раз и описывает отдельную таблицу.",
                "how_it_should_be": "`@ElementCollection` всегда использует отдельную таблицу (`@CollectionTable`) с FK на entity-владельца."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "`@ElementCollection` маппит коллекцию значений `String` или `@Embeddable`-объектов `Address` в отдельную таблицу `@CollectionTable` с `@JoinColumn`. Собственной `entity` и `id` у элементов при этом нет.",
              "correct": true,
              "sections": {
                "explanation": "`@ElementCollection @CollectionTable(name = \"user_addresses\", joinColumns = @JoinColumn(name = \"user_id\")) List<Address> addresses`, где `Address` — `@Embeddable` со `street`/`city`/`country`.",
                "example": "Для простых значений: `@ElementCollection @CollectionTable(name = \"user_emails\", joinColumns = @JoinColumn(name = \"user_id\")) @Column(name = \"email\") Set<String> emails`.",
                "when_to_apply": "Применяют для коллекций значений без собственной identity (теги, адреса, телефоны); если на элемент ссылаются другие сущности и нужна identity — берут `@OneToMany` с entity.",
                "edge_cases": "У элементов нет PK, поэтому обновление коллекции часто реализуется как delete-all + re-insert строк `@CollectionTable` — на больших коллекциях это дороже, чем у `@OneToMany`.",
                "related": "[[hibernate-relationships-interview#Q1]] @OneToMany среди типов связей; [[hibernate-relationships-interview#Q8]] коллекции для @OneToMany; [[hibernate-relationships-interview#Q9]] @Embeddable и @MapsId."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "`@ElementCollection` берёт примитивы вроде `String` или `Integer`, а сложнее — нет. Объекты `@Embeddable` (например тип `Address`) с полями `street`/`city` в `@CollectionTable` не поддерживаются.",
              "correct": false,
              "sections": {
                "what_actually": "`@ElementCollection` поддерживает и простые значения, и `@Embeddable` (например `List<Address>`), маппя каждое поле в колонки таблицы.",
                "source_of_confusion": "Базовые примеры показывают `Set<String>`, отсюда вывод об ограничении только примитивами.",
                "if_it_were_true": "Тогда `List<Address> addresses` с `@Embeddable Address` был бы невалиден, но это рабочий маппинг.",
                "how_it_should_be": "`@ElementCollection` хранит и примитивы, и `@Embeddable`-типы; для последних колонки берутся из полей embeddable."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 13,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какие типичные ошибки допускают при работе со связями в `Hibernate`?",
          "options": [
            {
              "label": "A",
              "text": "Частая ошибка — вешать `LAZY` на все связи: она плодит лишние `SELECT` и роняет `LazyInitializationException` вне сессии. Безопаснее держать `@ManyToOne` и коллекции на `EAGER`.",
              "correct": false,
              "sections": {
                "what_actually": "`LAZY` — как раз рекомендуемая стратегия; ошибкой считается бездумный `EAGER`, а `LazyInitializationException` лечат границами транзакции/`JOIN FETCH`.",
                "source_of_confusion": "`LazyInitializationException` пугает, и из этого делают вывод, что `LAZY` опасен сам по себе.",
                "if_it_were_true": "Тогда best practice советовал бы `EAGER` везде, но он рекомендует обратное — `LAZY` на всех связях.",
                "how_it_should_be": "Ошибка — ненужный `EAGER`; `LAZY` корректен, а исключение вне сессии решается правильными границами загрузки."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "Ошибка `MultipleBagFetchException` возникает при `JOIN FETCH` сразу двух `Set`-коллекций одного `@OneToMany`. Спасает переход на `List`, снимающий конфликт `bag` при выборке `items` и `shipments`.",
              "correct": false,
              "sections": {
                "what_actually": "Исключение бросают именно две `List` (bag); решение — заменить одну/обе на `Set` или разбить на два запроса.",
                "source_of_confusion": "Тип коллекции и исключение легко переставить местами в памяти.",
                "if_it_were_true": "Тогда переход на `List` чинил бы проблему, но он её как раз и вызывает.",
                "how_it_should_be": "`MultipleBagFetchException` — про две `List`; лечится `Set` или раздельными запросами, а не переходом на `List`."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "Типичные ошибки — ненужный `EAGER` и забытая синхронизация bidirectional (`setOrder` без `items`). Также `MultipleBagFetchException` на двух `List`, `@ManyToMany` с полями и `equals/hashCode` по всем полям.",
              "correct": true,
              "sections": {
                "explanation": "Например, `@EqualsAndHashCode` от `Lombok` по всем полям ломает entity в `Set`: `hashCode` меняется после `save` (генерится `id`), и объект «теряется» в коллекции.",
                "example": "`@Query(\"FROM Order o JOIN FETCH o.items JOIN FETCH o.shipments\")` с двумя `List` бросает `MultipleBagFetchException` — лечится заменой на `Set` или двумя отдельными запросами.",
                "when_to_apply": "Этот чек-лист применяют на код-ревью JPA-кода и при разборе багов с пустыми коллекциями, дублями в `Set` или внезапными лишними запросами.",
                "edge_cases": "`equals/hashCode` правильнее строить по natural ID или возвращать константный `hashCode`; `MultipleBagFetchException` лечится заменой `List` на `Set` или двумя запросами.",
                "related": "[[hibernate-relationships-interview#Q3]] EAGER vs LAZY; [[hibernate-relationships-interview#Q8]] MultipleBagFetchException и Set; [[hibernate-relationships-interview#Q2]] синхронизация bidirectional."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "Для `entity` в `Set` лучше `@EqualsAndHashCode` от `Lombok` по всем полям. Так `hashCode` и `equals` точнее различают объекты `Order`, чем сравнение по одному `id`.",
              "correct": false,
              "sections": {
                "what_actually": "`@EqualsAndHashCode` по всем полям ломает контракт: после `save` меняется `id`, `hashCode` уезжает, и entity «пропадает» из `Set`.",
                "source_of_confusion": "Lombok удобен и кажется безопасным выбором по умолчанию для `equals/hashCode`.",
                "if_it_were_true": "Тогда не было бы рекомендации делать `equals/hashCode` по ID или константный `hashCode`, но её дают как best practice.",
                "how_it_should_be": "`equals/hashCode` строят по стабильному ID (natural ID/UUID) или возвращают константу, а не по всем полям."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 14,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Как тестировать связи между entity и обнаруживать N+1 в тестах?",
          "options": [
            {
              "label": "A",
              "text": "Достаточно вызвать `repository.save(order)` и сразу читать `getItems()` без `flush` или `clear`. Данные всё равно перечитаются из БД, а не из L1-кэша `Session`.",
              "correct": false,
              "sections": {
                "what_actually": "Без `em.flush()` + `em.clear()` объект остаётся в L1-кэше, и тест читает то же состояние из памяти, не проверяя реальную персистентность.",
                "source_of_confusion": "L1-кэш незаметен, и кажется, что `findById` всегда идёт в базу.",
                "if_it_were_true": "Тогда `em.clear()` в примерах был бы лишним, но его ставят именно чтобы сбросить L1-кэш.",
                "how_it_should_be": "Перед перечитыванием делают `em.flush()` и `em.clear()`, иначе проверяется кэш сессии, а не БД."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "Связи проверяют срезом `@DataJpaTest` с `TestEntityManager`: сохранить parent, вызвать `flush` и `clear` для сброса L1-кэша, затем перечитать `findById` и число запросов через `Statistics`.",
              "correct": true,
              "sections": {
                "explanation": "`shouldSaveOrderWithItems`: после `flush`/`clear` `loaded.getItems()` имеет размер 2; `shouldDetectNPlusOne`: `stats.getPrepareStatementCount()` ожидается `<= 2`, а не `1 + N`.",
                "example": "`Statistics stats = sessionFactory.getStatistics(); stats.clear();` затем после выборки `assertThat(stats.getPrepareStatementCount()).isLessThanOrEqualTo(2)` фиксирует отсутствие N+1.",
                "when_to_apply": "Такие тесты пишут при настройке связей, каскадов и `orphanRemoval`, а Statistics-проверку — чтобы зафиксировать отсутствие регрессии в количестве запросов.",
                "edge_cases": "`@DataJpaTest` по умолчанию откатывает транзакцию и использует тестовый контекст; `stats.clear()` перед актом обязателен, иначе счётчик включит предыдущие запросы.",
                "related": "[[hibernate-relationships-interview#Q4]] N+1 проблема; [[hibernate-relationships-interview#Q6]] orphanRemoval в тесте; [[hibernate-relationships-interview#Q15]] Hibernate Statistics в best practices."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "Проблему `N+1` в тесте ловят по времени: замеряют суммарную длительность метода и сравнивают с бюджетом. Явный подсчёт запросов через `getPrepareStatementCount()` у `Statistics` в `@DataJpaTest` считают необязательным.",
              "correct": false,
              "sections": {
                "what_actually": "Надёжнее считать сами запросы через `Hibernate Statistics` (`getPrepareStatementCount()`); время нестабильно и не доказывает причину.",
                "source_of_confusion": "Медленные списки часто следствие N+1, отсюда соблазн мерить именно длительность.",
                "if_it_were_true": "Тогда на быстрой БД N+1 проходил бы тест, но число запросов всё равно 1+N.",
                "how_it_should_be": "Считают количество SQL через Statistics, а не миллисекунды: это детерминированный сигнал N+1."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "Для теста связей нужен полный `@SpringBootTest` со всем контекстом, а среза `@DataJpaTest` мало. `TestEntityManager` и проверка `getItems()` без полного контекста не работают.",
              "correct": false,
              "sections": {
                "what_actually": "`@DataJpaTest` специально поднимает только JPA-слой (репозитории, `TestEntityManager`) и идеально подходит для тестов связей.",
                "source_of_confusion": "`@SpringBootTest` кажется «более правильным», раз он самый полный.",
                "if_it_were_true": "Тогда `TestEntityManager` был бы недоступен, но он предоставляется именно срезом `@DataJpaTest`.",
                "how_it_should_be": "Связи тестируют срезом `@DataJpaTest` с `TestEntityManager`; полный `@SpringBootTest` тут избыточен."
              },
              "order": 3
            }
          ]
        }
      ]
    },
    {
      "q_number": 15,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какие best practices при работе с отношениями в `Hibernate`?",
          "options": [
            {
              "label": "A",
              "text": "Разумные умолчания: `LAZY`-связи с точечным `JOIN FETCH` или `@EntityGraph`, `mappedBy` на inverse и выборочные каскады вместо `CascadeType.ALL`. Плюс `equals/hashCode` по бизнес-ключу и промежуточный `entity` для `@ManyToMany` с полями.",
              "correct": true,
              "sections": {
                "explanation": "Это умолчания, а не догмы: `@ManyToOne(fetch = LAZY)` вместо дефолтного `EAGER`, helper-методы `addItem`/`removeItem` для синхронизации, `orphanRemoval = true` там, где parent владеет жизненным циклом детей.",
                "example": "На код-ревью ловят `@ManyToOne` без `fetch = LAZY`, `@EqualsAndHashCode` по всем полям и `@ManyToMany` с полями вроде `grade` — каждый пункт ведёт к конкретному багу в проде.",
                "when_to_apply": "Этот набор применяют как чек-лист при проектировании JPA-модели и на код-ревью, чтобы заранее исключить N+1, пустые коллекции и проблемы с `Set`.",
                "edge_cases": "Контекст решает: иногда инлайновый `JOIN FETCH` уместнее переиспользуемого `@EntityGraph`; `equals/hashCode` строят по бизнес-ключу, когда он есть, иначе по суррогатному `id`; `Hibernate Statistics` — диагностика, которую включают на время разбора, а не постоянная настройка prod.",
                "related": "[[hibernate-relationships-interview#Q3]] LAZY для всех связей; [[hibernate-relationships-interview#Q6]] orphanRemoval для owned children; [[hibernate-relationships-interview#Q13]] типичные ошибки, которых избегают."
              },
              "order": 0
            },
            {
              "label": "B",
              "text": "Лучшая практика — `EAGER` на все связи и `CascadeType.ALL` по умолчанию, чтобы ничего не забыть подгрузить и сохранить. Тогда `getItems()` и `persist` над `entity` работают предсказуемо.",
              "correct": false,
              "sections": {
                "what_actually": "Рекомендуют обратное: `LAZY` на всех связях и выбор конкретных каскадов, а не `ALL`; глобальный `EAGER` раздувает запросы.",
                "source_of_confusion": "«Чтобы точно всё загрузилось/сохранилось» звучит безопасно, отсюда тяга к EAGER+ALL.",
                "if_it_were_true": "Тогда best practices не предупреждали бы против `EAGER` и `CascadeType.ALL`, но они делают это явно.",
                "how_it_should_be": "Правильно: `LAZY` везде, точечные каскады вместо `ALL`, а нужные связи тянуть `JOIN FETCH`/`@EntityGraph`."
              },
              "order": 1
            },
            {
              "label": "C",
              "text": "Методы `equals` и `hashCode` для `entity` стоит строить по всем полям — так объекты точнее сравниваются в `Set` и `HashMap`. Стабильность `hashCode` после `save` и генерации `id` тут вторична и роли не играет.",
              "correct": false,
              "sections": {
                "what_actually": "Best practice — `equals/hashCode` по стабильному ID, а не по всем полям: иначе `hashCode` меняется после `save` и ломает `Set`.",
                "source_of_confusion": "Сравнение «по всем полям» интуитивно кажется самым корректным.",
                "if_it_were_true": "Тогда не предупреждали бы про поломку контракта в Set-коллекциях, но это одна из ключевых рекомендаций.",
                "how_it_should_be": "`equals/hashCode` строят по ID (natural ID/UUID), а не по всем полям — особенно для Set-коллекций."
              },
              "order": 2
            },
            {
              "label": "D",
              "text": "Главный совет — везде применять `@ManyToMany`, даже когда в связи есть поля `grade` и `enrolled_at`. Промежуточный `entity` `Enrollment` с двумя `@ManyToOne` только плодит лишние классы.",
              "correct": false,
              "sections": {
                "what_actually": "Best practice прямо советует избегать `@ManyToMany` с доп. полями и заменять её промежуточным entity (`Enrollment`).",
                "source_of_confusion": "Меньше классов кажется проще, отсюда тяга оставить `@ManyToMany` любой ценой.",
                "if_it_were_true": "Тогда поля `enrolled_at`/`grade` некуда было бы положить, но именно ради них вводят промежуточный entity.",
                "how_it_should_be": "При доп. полях `@ManyToMany` заменяют на промежуточный entity с двумя `@ManyToOne`; чистый N:M — только без атрибутов связи."
              },
              "order": 3
            }
          ]
        }
      ]
    }
  ]
}