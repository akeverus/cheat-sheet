{
  "topic_slug": "jmh-microbenchmarking-interview",
  "questions": [
    {
      "q_number": 1,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Почему наивный замер через `System.nanoTime()` врёт и какой механизм JMH спасает именно от dead-code elimination (а входные данные при этом передаёт через `@State`)?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Против dead-code elimination JMH использует `@Fork`, а входные данные передаёт через `@State`.",
              "correct": false,
              "sections": {
                "what_actually": "`@Fork` запускает бенчмарк в отдельной свежей JVM (или нескольких), чтобы изолировать замер от эффектов профиля компиляции и состояния текущей JVM и получить воспроизводимость между прогонами.",
                "source_of_confusion": "`@Fork` тоже входит в список механизмов, которыми JMH делает замер честным, поэтому его легко перепутать с защитой именно от мёртвого кода.",
                "if_it_were_true": "Тогда форк отдельной JVM каким-то образом заставлял бы JIT не выкидывать неиспользуемый результат — но изоляция процесса никак не влияет на то, считает ли JIT вычисление мёртвым.",
                "how_it_should_be": "От dead-code elimination защищает `Blackhole`; `@Fork` решает другую задачу — изоляцию JVM и устойчивость результата между запусками."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Против dead-code elimination JMH использует `@Warmup`, а входные данные передаёт через `@State`.",
              "correct": false,
              "sections": {
                "what_actually": "`@Warmup` задаёт прогревочные итерации, результаты которых не учитываются: за них «горячий» метод успевает скомпилироваться C2, и измеряется уже оптимизированный код, а не интерпретатор.",
                "source_of_confusion": "Прогрев — первый пункт в списке проблем наивного замера, поэтому `@Warmup` кажется универсальным «лекарством», в том числе и от мёртвого кода.",
                "if_it_were_true": "Тогда сколько ни прогревай, лишние прогревочные итерации делали бы неиспользуемый результат «нужным» — но прогрев меняет лишь профиль компиляции, а не факт, что результат никуда не идёт.",
                "how_it_should_be": "От dead-code elimination защищает `Blackhole`; `@Warmup` борется с эффектом холодного кода и недокомпилированного C1/интерпретатора."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Против dead-code elimination JMH использует `Blackhole`, а входные данные передаёт через `@State`.",
              "correct": true,
              "sections": {
                "explanation": "Если результат вычисления никуда не используется, JIT целиком выкидывает вычисление как мёртвый код, и наивный замер показывает «как быстро Java умеет ничего не делать». `Blackhole` — это объект-приёмник, который «потребляет» результат так, что JIT обязан его вычислить и не может удалить. Передача входных данных через `@State` отдельно закрывает constant folding.",
                "example": "Вместо `Math.log(42)` в цикле без использования результата метод объявляют как `public void bench(Blackhole bh) { bh.consume(Math.log(x)); }`, где `x` приходит из `@State`-объекта.",
                "when_to_apply": "Всегда, когда бенчмарк производит несколько результатов или значение, которое иначе не возвращается из метода; для одиночного результата достаточно просто вернуть его из `@Benchmark` (JMH сам заворачивает return в Blackhole).",
                "edge_cases": "`Blackhole` нужен именно для множественных значений внутри метода; нельзя «складывать» результаты в локальную переменную и думать, что это спасёт — JIT всё равно может доказать ненужность и выкинуть вычисление.",
                "related": "Связан с constant folding (Q12, защита через `@State`) и с вопросом «нужно ли возвращать значение из `@Benchmark`» (Q13) — это две стороны борьбы с устранением мёртвого кода."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Против dead-code elimination JMH использует `@Setup`, а входные данные передаёт через `@State`.",
              "correct": false,
              "sections": {
                "what_actually": "`@Setup` помечает метод фикстуры, который JMH вызывает до измерения (на уровне Trial/Iteration/Invocation), чтобы подготовить состояние `@State`-объекта перед бенчмарком.",
                "source_of_confusion": "`@Setup` работает в паре со `@State`, который уже фигурирует в правильном ответе, поэтому его легко достроить как «второй» защитный механизм.",
                "if_it_were_true": "Тогда подготовка данных перед замером сама бы запрещала JIT удалять неиспользуемый результат — но `@Setup` только инициализирует поля, он не потребляет результат вычисления.",
                "how_it_should_be": "От dead-code elimination защищает `Blackhole`; `@Setup` лишь готовит `@State` перед итерацией и к устранению мёртвого кода отношения не имеет."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 2,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какие два артефакта JMH нужно подключить к проекту, чтобы бенчмарки собрались в исполняемый вид?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "`jmh-core` (рантайм-движок) плюс `jmh-generator-annprocess` (annotation processor) в scope `provided`",
              "correct": true,
              "sections": {
                "explanation": "Нужны ровно оба: `jmh-core` даёт раннер, аннотации, `Blackhole` и профилировщики, а `jmh-generator-annprocess` на этапе компиляции генерирует обвязку — синтетические классы-раннеры вокруг `@Benchmark`-методов. Без annotation processor бенчмарки физически не соберутся в исполняемый вид.",
                "example": "В Maven обе зависимости с `groupId` `org.openjdk.jmh` и одной версией `1.37`; у `jmh-generator-annprocess` ставят `<scope>provided</scope>`, так как процессор нужен только при компиляции.",
                "when_to_apply": "При ручной настройке Maven/Gradle-модуля под бенчмарки, когда не используется готовый архетип и зависимости прописываются вручную.",
                "edge_cases": "Если забыть `jmh-generator-annprocess`, сборка пройдёт, но ни одного синтетического раннера не появится — `java -jar` просто не найдёт бенчмарков. Архетип `jmh-java-benchmark-archetype` подключает оба артефакта и `maven-shade-plugin` за вас.",
                "related": "Архетип настраивает fat-jar с `Main-Class: org.openjdk.jmh.Main`; бенчмарки держат в отдельном модуле, а не в `src/test`, чтобы процессор и shade-плагин не конфликтовали с обычной сборкой."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "`jmh-core` (рантайм-движок) плюс `jmh-generator-reflection` (annotation processor) в scope `provided`",
              "correct": false,
              "sections": {
                "what_actually": "`jmh-generator-reflection` — это реальный, но альтернативный генератор обвязки, работающий через рефлексию по уже скомпилированным классам, а не как annotation processor на этапе компиляции.",
                "source_of_confusion": "Оба генератора лежат в семействе `org.openjdk.jmh` и делают концептуально похожую вещь — порождают раннеры вокруг `@Benchmark`; легко перепутать annotation-processor-вариант с reflection-вариантом.",
                "if_it_were_true": "Стандартный compile-time-путь сборки через annotation processing работал бы без `annprocess`, что противоречит официальному способу из архетипа.",
                "how_it_should_be": "Каноничный артефакт для compile-time-генерации обвязки — `jmh-generator-annprocess`; reflection-генератор используют в особых нестандартных сборках."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "`jmh-runtime` (рантайм-движок) плюс `jmh-generator-annprocess` (annotation processor) в scope `provided`",
              "correct": false,
              "sections": {
                "what_actually": "Артефакта `jmh-runtime` в JMH не существует; рантайм-движок (раннер, аннотации, `Blackhole`, профилировщики) поставляется именно в `jmh-core`.",
                "source_of_confusion": "Слово «рантайм» в описании движка наводит на мысль, что и артефакт должен называться `jmh-runtime`; на деле имя — `jmh-core`.",
                "if_it_were_true": "Пришлось бы тянуть несуществующую зависимость, и Maven падал бы на разрешении артефакта, так и не собрав бенчмарки.",
                "how_it_should_be": "Рантайм-движок подключается как `jmh-core` той же версии, что и генератор; никакого `jmh-runtime` нет."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "`jmh-core` (рантайм-движок) плюс `jmh-generator-annprocess` (annotation processor) в scope `runtime`",
              "correct": false,
              "sections": {
                "what_actually": "Scope `runtime` означает, что зависимость недоступна при компиляции и подтягивается только в рантайме — ровно когда annotation processor уже бесполезен.",
                "source_of_confusion": "Слово «processor» ассоциируется с выполнением, а не с компиляцией, поэтому scope `runtime` кажется логичным; но процессор работает именно во время `javac`.",
                "if_it_were_true": "`javac` не увидел бы процессор на этапе компиляции, обвязка не сгенерировалась бы, и бенчмарки опять-таки не собрались бы в исполняемый вид.",
                "how_it_should_be": "Генератор подключают в scope `provided` — он нужен только при компиляции и не должен попадать в итоговый fat-jar."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 3,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какой аннотацией помечается метод, который JMH будет замерять как простейший бенчмарк?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Метод замеряемого бенчмарка помечается аннотацией `@State` в публичном классе.",
              "correct": false,
              "sections": {
                "what_actually": "`@State` объявляет класс-носитель данных, которые нужны бенчмарку, но не должны попадать в измеряемый код; JMH создаёт его экземпляр и передаёт в `@Benchmark`-метод как параметр.",
                "source_of_confusion": "`@State` тоже из JMH и тоже требует публичный класс с no-arg конструктором, поэтому его легко спутать с маркером самого бенчмарка.",
                "if_it_were_true": "Если бы `@State` помечала бенчмарк, JMH замерял бы классы-контейнеры данных, а не методы, и не нашёл бы, какой именно код прогонять.",
                "how_it_should_be": "Замеряемый метод помечается `@Benchmark`; `@State` лишь поставляет ему входные данные через параметр."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Метод замеряемого бенчмарка помечается аннотацией `@Setup` в публичном классе.",
              "correct": false,
              "sections": {
                "what_actually": "`@Setup` — fixture-метод внутри `@State`-класса: он готовит состояние перед замерами и вызывается с частотой по `Level` (Trial/Iteration/Invocation), но сам не замеряется.",
                "source_of_confusion": "`@Setup` стоит на методе и относится к жизненному циклу бенчмарка, поэтому кажется, будто именно он «запускает» замер.",
                "if_it_were_true": "Если бы `@Setup` помечала бенчмарк, JMH измерял бы подготовку данных вместо целевого кода, и результат отражал бы стоимость инициализации.",
                "how_it_should_be": "`@Setup` только готовит состояние; замеряемый код помечается `@Benchmark`."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Метод замеряемого бенчмарка помечается аннотацией `@Param` в публичном классе.",
              "correct": false,
              "sections": {
                "what_actually": "`@Param` ставится на публичное нефинальное поле `@State`-класса и параметризует прогон: JMH гоняет бенчмарк отдельно для каждого значения и выдаёт по строке результата.",
                "source_of_confusion": "`@Param` тоже задаёт, как именно гоняется бенчмарк, поэтому его роль путают с пометкой самого замеряемого метода.",
                "if_it_were_true": "Если бы `@Param` помечала бенчмарк, аннотация стояла бы на методе, а не на поле, и JMH не знал бы, какие методы вообще замерять.",
                "how_it_should_be": "`@Param` параметризует поле состояния; замеряемый метод по-прежнему помечается `@Benchmark`."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Метод замеряемого бенчмарка помечается аннотацией `@Benchmark` в публичном классе.",
              "correct": true,
              "sections": {
                "explanation": "`@Benchmark` — единственный маркер, который говорит JMH: «вот этот метод нужно замерять». Annotation processor находит такие методы и генерирует вокруг них синтетическую обвязку-раннер; метод обязан лежать в публичном классе.",
                "example": "public class StringBenchmark { @Benchmark public String concat() { return \"a\" + \"b\" + System.currentTimeMillis(); } } — concat() станет отдельной строкой в отчёте JMH.",
                "when_to_apply": "Ставится на каждый метод, чью производительность вы хотите измерить; класс с такими методами собирается в fat-jar и запускается через `java -jar target/benchmarks.jar` или программно через `Runner` + `OptionsBuilder`.",
                "edge_cases": "Метод должен быть в публичном классе; если несколько значений вычисляется внутри, лишние пропускают через `Blackhole`, а одно можно вернуть из метода.",
                "related": "Запуск идёт двумя путями: fat-jar с `Main-Class: org.openjdk.jmh.Main` или программно `new Runner(opt).run()`, где `include(...)` принимает регулярку по именам бенчмарков."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 4,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что задаёт `Scope.Benchmark` для `@State`-класса в JMH при многопоточном прогоне (`-t N`)?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "по одному экземпляру state на каждый рабочий поток",
              "correct": false,
              "sections": {
                "what_actually": "Это поведение `Scope.Thread`: JMH создаёт отдельный экземпляр state для каждого потока, состояние изолировано, гонок и contention на самих данных нет.",
                "source_of_confusion": "Оба — значения одного и того же `Scope`, и оба про многопоточный прогон; легко перепутать «один общий» с «по одному на поток».",
                "if_it_were_true": "Тогда `Scope.Benchmark` нельзя было бы использовать для замера shared-структур — каждый поток работал бы со своей копией, и contention в замер не попадал бы.",
                "how_it_should_be": "«по одному на каждый поток» — это `Scope.Thread`; `Scope.Benchmark` даёт один экземпляр на всех."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "один экземпляр state на весь бенчмарк, общий для всех потоков",
              "correct": true,
              "sections": {
                "explanation": "`Scope.Benchmark` создаёт ровно один экземпляр state-класса и шарит его между всеми рабочими потоками. Поэтому замер включает стоимость конкуренции (contention) и возможный false sharing на общих данных — это нужно, когда меряют именно shared-структуры.",
                "example": "`@State(Scope.Benchmark)` с полем `final Map<Integer,Integer> map = new ConcurrentHashMap<>()`: при `-t 4` все 4 потока бьют в одну и ту же мапу, и замер `map.put(...)` отражает реальную конкуренцию.",
                "when_to_apply": "Когда цель — измерить поведение разделяемой структуры под нагрузкой: конкурентные коллекции (`ConcurrentHashMap`), общий счётчик/`AtomicLong`, очереди producer/consumer.",
                "edge_cases": "Тот же `Scope.Benchmark` для обычного `HashMap` под несколькими потоками измерит ещё и contention/false sharing, исказив однопоточную стоимость операции; для чистой стоимости берут `Scope.Thread`.",
                "related": "Дополняет `Scope.Thread` (изолированное состояние на поток) и `Scope.Group` (экземпляр на группу потоков `@Group`); сам state готовится через `@Setup`/`@TearDown` с разными `Level`."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "по одному экземпляру state на каждую группу потоков",
              "correct": false,
              "sections": {
                "what_actually": "Это поведение `Scope.Group`: один экземпляр на каждую группу потоков, объявленную через `@Group`, — для асимметричных сценариев producer/consumer.",
                "source_of_confusion": "`Scope.Group` тоже шарит state между несколькими потоками, поэтому его путают с общим-на-всех `Scope.Benchmark`.",
                "if_it_were_true": "Тогда без объявления `@Group` поведение было бы неопределённым, а один общий счётчик на весь прогон задать было бы нельзя.",
                "how_it_should_be": "«на каждую группу» — это `Scope.Group`; `Scope.Benchmark` — один на весь бенчмарк независимо от групп."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "по одному экземпляру state на каждую итерацию измерения",
              "correct": false,
              "sections": {
                "what_actually": "Частоту пересоздания/сброса состояния по итерациям задаёт не `Scope`, а `@Setup`/`@TearDown` с `Level.Iteration`; `Scope` управляет шарингом между потоками, а не временем жизни по итерациям.",
                "source_of_confusion": "И `Scope`, и `Level` касаются жизненного цикла state, поэтому «на итерацию» ошибочно приписывают именно `Scope.Benchmark`.",
                "if_it_were_true": "Тогда общий state не переживал бы между итерациями, и накопленные за прогон данные (например, прогретый кэш) терялись бы на каждой итерации.",
                "how_it_should_be": "Частота по итерациям — это `Level.Iteration` у `@Setup`; `Scope.Benchmark` живёт один экземпляр на весь бенчмарк (фактически на форк)."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 5,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какой `Level` у `@Setup`/`@TearDown` означает «вызвать один раз на весь форк, до прогрева и измерений», и используется как дефолтный?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "`Level.Iteration` — fixture вызывается один раз на форк, до всего прогрева и измерений",
              "correct": false,
              "sections": {
                "what_actually": "`Level.Iteration` вызывает fixture перед и после каждой итерации (и прогревочной, и измеряемой), а не один раз на форк.",
                "source_of_confusion": "И `Trial`, и `Iteration` оперируют на уровне «фаз» прогона, а не отдельных вызовов, поэтому их легко перепутать по частоте.",
                "if_it_were_true": "Если бы `Level.Iteration` срабатывал раз на форк, между итерациями не было бы способа сбросить или пересоздать состояние — пропала бы целая ступень частоты.",
                "how_it_should_be": "Раз на форк — это `Level.Trial`; `Level.Iteration` нужен для сброса состояния между итерациями."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "`Level.Invocation` — fixture вызывается один раз на форк, до всего прогрева и измерений",
              "correct": false,
              "sections": {
                "what_actually": "`Level.Invocation` вызывает fixture перед и после каждого вызова метода `@Benchmark` — это самый частый и самый опасный уровень.",
                "source_of_confusion": "Все три значения — варианты одного enum `Level`, и без знания семантики `Invocation` звучит как «один раз на запуск».",
                "if_it_were_true": "Будь это так, документация не предупреждала бы об опасности: при разовом вызове setup не попадал бы в тайминг-окно и не искажал бы наносекундные замеры.",
                "how_it_should_be": "Раз на форк — это `Level.Trial`; `Level.Invocation` гоняет fixture на каждый вызов и для наносекундных методов доминирует над замером."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "`Level.Trial` — fixture вызывается один раз на форк, до всего прогрева и измерений",
              "correct": true,
              "sections": {
                "explanation": "`Level.Trial` — это уровень по умолчанию: `@Setup(Level.Trial)` отрабатывает ровно один раз за запуск форка, ещё до warmup-итераций, а `@TearDown(Level.Trial)` — после всех измерений. Поэтому он подходит для разовой дорогой подготовки состояния.",
                "example": "`@Setup(Level.Trial) void setUp() { arr = ThreadLocalRandom.current().ints(10_000).toArray(); }` — массив строится один раз на форк, а не в каждой итерации.",
                "when_to_apply": "Для дорогой разовой подготовки: построить большой массив или коллекцию, прогреть кэш, поднять mock-объекты — всё, что не нужно пересоздавать между итерациями.",
                "edge_cases": "Если состояние мутируется самим бенчмарком и должно быть свежим между итерациями, `Level.Trial` не подойдёт — нужен `Level.Iteration`. Для `@State(Scope.Thread)` setup на каждом потоке свой.",
                "related": "Уровень настраивается рядом со `@State`-классом и его полями; связан с `Scope` (Q4), который определяет, сколько экземпляров state создаётся на потоки."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "`Level.Fork` — fixture вызывается один раз на форк, до всего прогрева и измерений",
              "correct": false,
              "sections": {
                "what_actually": "Значения `Level.Fork` в JMH не существует: enum `Level` содержит только `Trial`, `Iteration` и `Invocation`.",
                "source_of_confusion": "Раз семантика именно «один раз на форк», напрашивается одноимённое значение `Fork` — но изоляцией форков управляет аннотация `@Fork`, а не `Level`.",
                "if_it_were_true": "Если бы `Level.Fork` существовал, он дублировал бы уже имеющийся `Level.Trial`, у которого как раз гранулярность форка.",
                "how_it_should_be": "Нужный уровень называется `Level.Trial`; `@Fork` — это отдельная аннотация про число процессов JVM, а не значение enum `Level`."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 6,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Если в одном бенчмарке объявить два поля с `@Param`, как JMH построит набор прогонов по их значениям?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "JMH прогонит декартово произведение всех комбинаций значений обоих полей",
              "correct": true,
              "sections": {
                "explanation": "Каждый `@Param` задаёт независимое измерение пространства параметров. JMH перебирает все сочетания значений, давая отдельную строку результата на каждую комбинацию — это и есть декартово произведение.",
                "example": "`@Param({\"10\",\"1000\"})` для size и `@Param({\"ArrayList\",\"LinkedList\"})` для типа дадут 2×2 = 4 прогона: (10, ArrayList), (10, LinkedList), (1000, ArrayList), (1000, LinkedList).",
                "when_to_apply": "Когда нужно снять кривую сразу по нескольким осям: например, зависимость операции от размера входа И от варианта реализации коллекции, не дублируя код бенчмарка.",
                "edge_cases": "Произведение растёт мультипликативно: три поля по 4 значения дадут 64 прогона, и с учётом warmup и форков время прогона взрывается — число значений держат минимальным.",
                "related": "Поле под `@Param` должно быть `public` и неконстантным, значения задаются строками и парсятся JMH в тип поля через рефлексию (`int`, `enum`, `boolean`)."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "JMH прогонит параллельные пары значений обоих полей по позициям в массивах",
              "correct": false,
              "sections": {
                "what_actually": "Позиционного спаривания значений у `@Param` нет; JMH трактует каждое поле как независимую ось и не привязывает i-е значение одного к i-му значению другого.",
                "source_of_confusion": "Интуитивно массивы одинаковой длины хочется «застегнуть» в пары (zip), как в некоторых параметризованных тестах с курсором по индексу.",
                "if_it_were_true": "Два массива по 3 значения дали бы лишь 3 прогона вместо 9, а массивы разной длины было бы непонятно как спаривать.",
                "how_it_should_be": "JMH строит полное декартово произведение всех комбинаций значений обоих полей, а не пары по индексу."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "JMH прогонит только первое значение остальных полей при переборе одного поля",
              "correct": false,
              "sections": {
                "what_actually": "JMH не фиксирует остальные поля на первом значении — он перебирает все их значения тоже, как отдельные оси.",
                "source_of_confusion": "Похоже на ручной «one-factor-at-a-time» подход, где варьируют один параметр, держа прочие на базовом значении.",
                "if_it_were_true": "Часть комбинаций (например, 1000 с LinkedList) никогда бы не замерилась, и кривая по второй оси осталась бы неполной.",
                "how_it_should_be": "JMH строит полное декартово произведение всех комбинаций значений обоих полей, ничего не фиксируя."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "JMH прогонит одну комбинацию из последних значений обоих полей",
              "correct": false,
              "sections": {
                "what_actually": "JMH не схлопывает массивы значений в один прогон и не берёт только последнее значение каждого поля.",
                "source_of_confusion": "Можно спутать с тем, как присваивание поля «перезаписывает» переменную, оставляя лишь последнее записанное значение.",
                "if_it_were_true": "Смысл `@Param` как способа снять кривую пропал бы: вы получили бы единственную точку вместо сетки замеров.",
                "how_it_should_be": "JMH строит полное декартово произведение всех комбинаций значений обоих полей, по строке результата на каждую."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 7,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какой режим `@BenchmarkMode` применяется по умолчанию, если аннотацию не указать?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "`AverageTime` — среднее время одного вызова, «меньше — лучше»",
              "correct": false,
              "sections": {
                "what_actually": "`AverageTime` действительно существует и меряет среднее время одного вызова в ns/op или us/op, но включается только при явном указании `@BenchmarkMode(Mode.AverageTime)`.",
                "source_of_confusion": "`AverageTime` — самый ходовой режим в примерах и статьях, поэтому его легко принять за дефолтный, хотя по умолчанию JMH считает пропускную способность.",
                "if_it_were_true": "Тогда метод без аннотации сразу печатал бы ns/op и «меньше — лучше», а строка результата шла бы в режиме `avgt`, а не `thrpt`.",
                "how_it_should_be": "По умолчанию работает `Throughput` (ops/s); `AverageTime` надо запрашивать явно через аннотацию или флаг `-bm avgt`."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "`SampleTime` — среднее время одного вызова, «меньше — лучше»",
              "correct": false,
              "sections": {
                "what_actually": "`SampleTime` сэмплирует время вызовов и строит распределение с перцентилями p50/p99/p999; он не печатает простое «среднее время» и не является режимом по умолчанию.",
                "source_of_confusion": "Его путают с `AverageTime`, потому что оба про время вызова в наносекундах, и кажется, что «время одного вызова» — это и есть дефолт JMH.",
                "if_it_were_true": "Тогда метод без аннотации сразу выдавал бы перцентили латентности, а не одно число пропускной способности в ops/s.",
                "how_it_should_be": "По умолчанию идёт `Throughput`; `SampleTime` включают явно, когда важны хвосты латентности, а не среднее."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "`SingleShotTime` — время одного вызова без прогрева, «меньше — лучше»",
              "correct": false,
              "sections": {
                "what_actually": "`SingleShotTime` меряет один-единственный вызов и сознательно не прогревает код многократно; он про cold-start и разовые операции, а не про дефолтный непрерывный замер.",
                "source_of_confusion": "Слово «single» наводит на мысль о простейшем базовом режиме, который якобы работает «из коробки», хотя по умолчанию JMH гоняет непрерывный `Throughput`.",
                "if_it_were_true": "Тогда обычный бенчмарк без аннотации не прогревался бы и мерил холодный запуск, а warmup-итерации вообще не имели бы смысла.",
                "how_it_should_be": "По умолчанию идёт `Throughput` с полным прогревом; `SingleShotTime` запрашивают явно для замера холодного старта."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "`Throughput` — число операций в единицу времени, «больше — лучше»",
              "correct": true,
              "sections": {
                "explanation": "Если `@BenchmarkMode` не задан явно, JMH использует `Throughput`: он считает, сколько вызовов `@Benchmark` укладывается в единицу времени и печатает результат в ops/s, где «больше — лучше».",
                "example": "Метод без `@BenchmarkMode` и `@OutputTimeUnit` выдаст строку вида `Mode=thrpt, Score=12345.6 ops/s`; чтобы получить среднее время, нужно явно поставить `@BenchmarkMode(Mode.AverageTime)`.",
                "when_to_apply": "Режим по умолчанию удобен, когда меряют пропускную способность: количество запросов, парсингов, операций структуры данных за секунду под непрерывной нагрузкой.",
                "edge_cases": "`Throughput` и `AverageTime` — по сути обратные величины, но считаются по-разному; для latency-хвостов p99/p999 ни один из них не подходит, там берут `SampleTime`.",
                "related": "В `@BenchmarkMode` можно передать массив режимов или `Mode.All`, а единицу вывода задаёт отдельная аннотация `@OutputTimeUnit` (Q8)."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 8,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что делает аннотация `@OutputTimeUnit` в JMH?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Задаёт единицу времени, в которой JMH выполняет замер, влияя на точность результата",
              "correct": false,
              "sections": {
                "what_actually": "Точность замера определяется числом итераций, форками и warmup, а не единицей вывода; JMH всегда меряет в наносекундах внутри и лишь конвертирует при печати.",
                "source_of_confusion": "Слово «единица времени» наводит на мысль, что она задаёт разрешение таймера, хотя речь идёт только о формате отчёта.",
                "if_it_were_true": "Тогда смена `@OutputTimeUnit` меняла бы score и `± error`, и одинаковый код давал бы разные числа лишь из-за выбранной единицы.",
                "how_it_should_be": "`@OutputTimeUnit` влияет только на представление; за точность отвечают `@Measurement`, `@Warmup` и `@Fork`."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Задаёт единицу времени, в которой JMH печатает результат, не влияя на сам замер",
              "correct": true,
              "sections": {
                "explanation": "`@OutputTimeUnit` управляет только форматом вывода: JMH пересчитывает уже измеренное значение в указанную единицу при печати отчёта. На точность, длительность или ход самого замера она не влияет — это чисто косметическая настройка читаемости.",
                "example": "С `@OutputTimeUnit(TimeUnit.NANOSECONDS)` для `AverageTime` результат печатается как `12.3 ns/op` вместо дефолтного `0.0000000123 s/op` — три порядка разницы только в представлении.",
                "when_to_apply": "Указывают явно почти всегда: для наносекундных операций берут `NANOSECONDS`, для I/O и крупных операций — `MICRO/MILLISECONDS`, чтобы числа в отчёте были читаемыми и сравнимыми между бенчмарками.",
                "edge_cases": "Дефолт — `SECONDS`, что для микробенчмарков даёт нечитаемые числа и риск промахнуться на три порядка. Единица применяется к строкам отчёта, но не меняет внутреннюю точность измерения.",
                "related": "Работает в паре с `@BenchmarkMode`: для `Throughput` единица идёт в знаменатель (`ops/ns`), для `AverageTime`/`SampleTime` — в числитель (`ns/op`)."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Задаёт единицу времени, в которой JMH ограничивает длительность одной итерации замера",
              "correct": false,
              "sections": {
                "what_actually": "Длительность итерации задаётся через `@Measurement(time=..., timeUnit=...)` и `@Warmup`, а не через `@OutputTimeUnit`.",
                "source_of_confusion": "И там, и там фигурирует `TimeUnit`, поэтому легко спутать единицу вывода с настройкой тайминга итераций.",
                "if_it_were_true": "Тогда `@OutputTimeUnit(MILLISECONDS)` обрывал бы каждую итерацию через миллисекунду, и замер был бы статистически ничтожным.",
                "how_it_should_be": "Длительностью итераций управляют `@Measurement` и `@Warmup`; `@OutputTimeUnit` лишь форматирует итоговый отчёт."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Задаёт единицу времени, в которой JMH выставляет таймаут на весь прогон бенчмарка",
              "correct": false,
              "sections": {
                "what_actually": "Глобальный таймаут задаётся опцией `@Timeout` (или `-to` в CLI), и его превышение прерывает прогон, тогда как `@OutputTimeUnit` ничего не прерывает.",
                "source_of_confusion": "Обе аннотации работают со временем, и кажется, что одна из них отвечает за лимиты выполнения.",
                "if_it_were_true": "Тогда слишком мелкая единица в `@OutputTimeUnit` валила бы долгие бенчмарки по таймауту ещё до получения результата.",
                "how_it_should_be": "За таймаут прогона отвечает `@Timeout`; `@OutputTimeUnit` только конвертирует единицы в выводе."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 9,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Чем `@Warmup` отличается от `@Measurement` в JMH по отношению к результатам прогоняемых итераций?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "`@Warmup` задаёт прогревочные итерации, результаты которых усредняются с замером, а `@Measurement` — итерации, чьи цифры идут в отчёт.",
              "correct": false,
              "sections": {
                "what_actually": "Прогревочные итерации запускаются именно для того, чтобы их результаты НЕ учитывались: JMH полностью отбрасывает их score и считает итог только по `@Measurement`.",
                "source_of_confusion": "Обе аннотации описывают итерации с одинаковыми опциями (`iterations`, `time`), поэтому легко решить, что все итерации вносят вклад в среднее.",
                "if_it_were_true": "Если бы warmup усреднялся с замером, в итоговый score протекали бы медленные интерпретируемые и C1-итерации, и число было бы систематически завышено по времени.",
                "how_it_should_be": "Warmup-итерации отбрасываются целиком; в отчёт идут только итерации `@Measurement`."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "`@Warmup` задаёт прогревочные итерации, результаты которых отбрасываются, а `@Measurement` — итерации, которые тоже отбрасываются для контроля разброса.",
              "correct": false,
              "sections": {
                "what_actually": "`@Measurement` задаёт именно те итерации, чьи цифры идут в отчёт и формируют `score ± error`; они не отбрасываются.",
                "source_of_confusion": "Может казаться, что для статистической чистоты JMH отбрасывает и замеряемые итерации, оставляя только агрегат, но агрегат как раз и считается ПО этим итерациям.",
                "if_it_were_true": "Если бы `@Measurement` тоже отбрасывался, в отчёте вообще не было бы данных для `score` — печатать было бы нечего.",
                "how_it_should_be": "Отбрасываются только warmup-итерации; цифры `@Measurement` идут в отчёт."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "`@Warmup` задаёт прогревочные итерации, результаты которых отбрасываются, а `@Measurement` — итерации, чьи цифры идут в отчёт.",
              "correct": true,
              "sections": {
                "explanation": "Прогрев существует, чтобы довести код до steady-state (C2 скомпилировал горячие методы, прогреты кэши, стабилизирован профиль ветвлений), и его цифры по определению выбрасываются — иначе в итог попали бы переходные эффекты интерпретатора и компиляции. В отчёт идут только итерации `@Measurement`.",
                "example": "`@Warmup(iterations = 5, time = 1)` прогонит 5 прогревочных итераций по 1 секунде (их score не учитывается), затем `@Measurement(iterations = 5, time = 1)` даст 5 замеряемых итераций, чей средний score и `± error` попадут в таблицу.",
                "when_to_apply": "Когда нужно отделить установившуюся производительность от стартовых эффектов: любой замер на JVM с JIT требует прогрева, иначе вместо C2-кода измеряется интерпретатор или C1.",
                "edge_cases": "Слишком мало warmup-итераций даёт бимодальное распределение и огромный `± error` (код не дошёл до C2). У обеих аннотаций общий набор опций: `iterations`, `time`, `timeUnit`, `batchSize`.",
                "related": "Связано с `@Fork` (изоляция profile pollution между бенчмарками) и с чтением `± error` (нестабильный замер часто означает нехватку прогрева)."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "`@Warmup` задаёт прогревочные итерации, результаты которых отбрасываются, а `@Measurement` — итерации, чьи цифры идут только в лог, но не в отчёт.",
              "correct": false,
              "sections": {
                "what_actually": "Итерации `@Measurement` агрегируются в финальный `score` и `± error` и попадают именно в итоговую таблицу отчёта, а не только в промежуточный лог.",
                "source_of_confusion": "JMH печатает построчный прогресс каждой измеряемой итерации в лог, поэтому легко спутать промежуточный вывод с тем, что эти данные не доходят до отчёта.",
                "if_it_were_true": "Если бы цифры `@Measurement` шли только в лог, итоговая строка `Benchmark Mode Cnt Score Error Units` оставалась бы пустой.",
                "how_it_should_be": "Промежуточные строки идут в лог, но именно итерации `@Measurement` агрегируются в итоговый отчёт."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 10,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что делает `@Fork` в JMH и почему форков должно быть хотя бы один?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "`@Fork` запускает бенчмарк в отдельном свежем процессе JVM, изолируя профиль JIT между бенчмарками",
              "correct": true,
              "sections": {
                "explanation": "`@Fork` задаёт, сколько раз бенчмарк запускается в отдельном свежем процессе JVM. Свежая JVM на каждый бенчмарк сбрасывает накопленный профиль JIT, поэтому один бенчмарк не отравляет оптимизации следующего. Поэтому `@Fork ≥ 1` — это условие корректности, а не перестраховка.",
                "example": "`@Fork(value = 2, jvmArgs = {\"-Xms2g\", \"-Xmx2g\"})` запустит два независимых процесса JVM; через `jvmArgs` фиксируют heap и GC, чтобы межпроцессная дисперсия была видна, а замер стабилен.",
                "when_to_apply": "Всегда в реальных замерах оставлять форк включённым (дефолт). Несколько форков (`@Fork(3)`) берут, когда нужно увидеть межпроцессную дисперсию — разные запуски JVM иногда выходят на разные стабильные состояния.",
                "edge_cases": "`@Fork(0)` отключает форк и гоняет в той же JVM — допустимо только для отладки самого бенчмарка (поставить брейкпойнт), цифрам в этом режиме доверять нельзя из-за profile pollution.",
                "related": "Связано с прогревом (`@Warmup`, Q9): если измерения «гуляют», причина — либо мало прогрева, либо отсутствие изоляции, которую как раз даёт форк."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "`@Fork` запускает бенчмарк в отдельном потоке JVM, изолируя профиль JIT между бенчмарками",
              "correct": false,
              "sections": {
                "what_actually": "Количеством потоков в JMH управляет флаг `-t N` (или `@Threads`), а не `@Fork`; потоки делят одну JVM и один общий профиль JIT.",
                "source_of_confusion": "И форки, и потоки — про «параллельность/множественность» прогона, и оба влияют на дисперсию результата, поэтому их легко перепутать.",
                "if_it_were_true": "Если бы форк был просто отдельным потоком, бенчмарки в одной JVM продолжали бы отравлять профиль JIT друг друга — изоляции не было бы вовсе.",
                "how_it_should_be": "Форк — это отдельный свежий процесс JVM; потоки задаются отдельно через `@Threads`/`-t` и работают внутри одного процесса."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "`@Fork` запускает бенчмарк в отдельном `ClassLoader` JVM, изолируя профиль JIT между бенчмарками",
              "correct": false,
              "sections": {
                "what_actually": "Отдельный `ClassLoader` изолирует загруженные классы внутри одной JVM, но не сбрасывает накопленный профиль JIT и не даёт свежий C2.",
                "source_of_confusion": "Оба механизма звучат как «изоляция», и новый `ClassLoader` действительно даёт «чистую» загрузку классов — кажется, что этого достаточно для чистого замера.",
                "if_it_were_true": "Если бы хватало нового `ClassLoader`, profile pollution всё равно сохранялся бы: JIT-профиль живёт на уровне процесса JVM, а не загрузчика классов.",
                "how_it_should_be": "Чистый профиль JIT даёт только новый процесс JVM (форк); смена `ClassLoader` внутри той же JVM этого не обеспечивает."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "`@Fork` запускает бенчмарк в отдельной итерации JVM, изолируя профиль JIT между бенчмарками",
              "correct": false,
              "sections": {
                "what_actually": "Итерациями управляют `@Warmup`/`@Measurement` внутри одного процесса; это окна замера, а не отдельные запуски JVM.",
                "source_of_confusion": "И форки, и итерации — это про повторяемость прогона и про разброс цифр между прогонами, поэтому их роли путают.",
                "if_it_were_true": "Если бы форк был просто ещё одной итерацией в той же JVM, все прогоны делили бы один профиль JIT — межпроцессной изоляции не возникло бы.",
                "how_it_should_be": "Итерации (`@Measurement`/`@Warmup`) идут внутри одного форка; сам форк — это отдельный процесс JVM поверх этих итераций."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 11,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Что такое dead-code elimination в JMH и как `Blackhole` защищает от него результат бенчмарка?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Если результат вычисления нигде не наблюдается, C2 вправе выкинуть всё вычисление; `Blackhole.consume(...)` помечает поле как `volatile`, и компилятор не может доказать бесполезность потребления.",
              "correct": false,
              "sections": {
                "what_actually": "`volatile` обеспечивает видимость и упорядочивание записей между потоками через барьеры памяти, но не является механизмом, на котором держится защита `Blackhole` от dead-code elimination.",
                "source_of_confusion": "И `volatile`, и `Blackhole` мешают компилятору «упрощать» код, поэтому их легко перепутать как один и тот же приём против оптимизаций.",
                "if_it_were_true": "Тогда любую защиту от dead-code сводили бы к одному `volatile`-полю, а отдельный класс `Blackhole` с его логикой потребления был бы лишним.",
                "how_it_should_be": "Защита держится на нетривиальной логике внутри `consume(...)`, которую компилятор не может статически признать бесполезной, а не на ключевом слове `volatile`."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Если результат вычисления нигде не наблюдается, C2 вправе выкинуть всё вычисление; `Blackhole.consume(...)` выводит значение в `System.out`, и компилятор не может доказать бесполезность потребления.",
              "correct": false,
              "sections": {
                "what_actually": "`Blackhole.consume(...)` ничего не печатает: вывод в `System.out` на каждый вызов наносекундного метода добавил бы огромный I/O-overhead и полностью исказил бы замер.",
                "source_of_confusion": "Печать результата — это «наблюдаемый» побочный эффект, который тоже спасает от dead-code, поэтому кажется правдоподобным механизмом для `Blackhole`.",
                "if_it_were_true": "Тогда микробенчмарк наносекундного метода тратил бы основное время на форматирование и запись в поток вывода, а не на сам алгоритм.",
                "how_it_should_be": "`consume(...)` потребляет значение без побочных эффектов вроде I/O, опираясь на логику, которую JIT не может признать бесполезной."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Если результат вычисления нигде не наблюдается, C2 вправе выкинуть всё вычисление; `Blackhole.consume(...)` отключает JIT для метода, и компилятор не может доказать бесполезность потребления.",
              "correct": false,
              "sections": {
                "what_actually": "Отключением JIT-компиляции метода занимается `@CompilerControl(Mode.EXCLUDE)`, а не `Blackhole`; `Blackhole` работает именно с JIT-скомпилированным кодом, не выключая его.",
                "source_of_confusion": "И то и другое противодействует «вредным» оптимизациям JIT, поэтому роль `Blackhole` легко спутать с управлением компиляцией.",
                "if_it_were_true": "Тогда любой бенчмарк с `Blackhole` гонялся бы в интерпретаторе, и вы измеряли бы медленный неоптимизированный код вместо реальной производительности.",
                "how_it_should_be": "`Blackhole` не трогает компиляцию метода — он лишь потребляет результат так, чтобы скомпилированный C2-код не был выкинут как мёртвый."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Если результат вычисления нигде не наблюдается, C2 вправе выкинуть всё вычисление; `Blackhole.consume(...)` написан так, что компилятор не может доказать бесполезность потребления.",
              "correct": true,
              "sections": {
                "explanation": "Dead-code elimination — это право JIT удалить вычисление, чей результат не используется. `Blackhole.consume(...)` содержит нетривиальную логику, и компилятор не может статически доказать, что потребление значения ни на что не влияет, поэтому вычисление сохраняется и реально замеряется.",
                "example": "`bh.consume(compute1()); bh.consume(compute2());` — оба значения потреблены, поэтому ни одно из вычислений не будет выкинуто, даже если метод объявлен как `void`.",
                "when_to_apply": "Когда внутри метода `@Benchmark` вычисляется несколько значений и вернуть можно только одно: остальные пропускают через `Blackhole.consume(...)`, чтобы JIT не удалил их.",
                "edge_cases": "В современных JDK JMH задействует компиляторные blackholes на уровне C1/C2 (`-XX:+UseCompilerBlackhole`), делая потребление почти бесплатным и надёжнее ручной реализации.",
                "related": "Парный механизм — возврат значения из метода: JMH-обвязка сама прогоняет его через `Blackhole` (Q13). Constant folding (Q12) убирает «вход» бенчмарка, а dead-code elimination — «выход»."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 12,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Чтобы constant folding не свернул вход бенчмарка в готовую константу, как нужно объявить поле с входными данными внутри `@State`-класса?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Как `public static final` поле с инициализатором",
              "correct": false,
              "sections": {
                "what_actually": "`static final`-поле с константным инициализатором — это class:constant, который JIT гарантированно сворачивает: значение известно на этапе компиляции и подставляется в место использования.",
                "source_of_confusion": "Поле всё ещё находится «в state-классе», и кажется, что любого поля достаточно; на деле решает не местоположение, а модификаторы.",
                "if_it_were_true": "Если бы `static final` спасал от folding, можно было бы хардкодить константы и всё равно мерить алгоритм — но тогда сам механизм constant folding не существовал бы.",
                "how_it_should_be": "Поле должно быть нефинальным экземплярным полем `@State`-объекта, чтобы JIT не доказал константность."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Как обычное `public` поле без модификатора `final`",
              "correct": true,
              "sections": {
                "explanation": "Constant folding срабатывает, когда JIT может доказать, что значение известно на этапе компиляции. Нефинальное поле `@State`-объекта JIT не вправе считать константой, поэтому он честно компилирует вычисление, а не подставляет готовый результат.",
                "example": "`public double x = 42.0;` внутри `@State(Scope.Thread)`; метод `Math.sqrt(in.x)` реально вычисляется, тогда как `Math.sqrt(42.0)` с литералом свернулся бы в константу.",
                "when_to_apply": "Всегда, когда у бенчмарка есть входные данные: размер коллекции, число для арифметики, ключ для поиска — их кладут в `@State` нефинальными полями вместо литералов в теле метода.",
                "edge_cases": "Даже значение, выглядящее «случайным», но присвоенное литералом в коде, свернётся; важна именно невозможность для JIT доказать константность, а не визуальная «непредсказуемость» числа.",
                "related": "Симметрично Q11: dead-code elimination убирает «выход» бенчмарка (защита — `Blackhole`/return), а constant folding убирает «вход» (защита — нефинальное `@State`-поле)."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Как `public final` поле, присвоенное в конструкторе",
              "correct": false,
              "sections": {
                "what_actually": "`final`-поле, даже инициализированное в конструкторе, JIT в горячем коде часто трактует как стабильное и может свернуть выражение над ним — `final` подсказывает компилятору неизменность.",
                "source_of_confusion": "Кажется, что раз значение задаётся в рантайме (в конструкторе), оно «динамическое»; но модификатор `final` сам по себе разрешает агрессивные оптимизации.",
                "if_it_were_true": "Будь `final` безопасным, защита от folding сводилась бы к запрету только литералов в теле метода — но проблема именно в доказуемой константности значения.",
                "how_it_should_be": "Поле оставляют без `final`, чтобы JIT не считал его неизменной константой."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Как `public` поле, помеченное `@CompilerControl`",
              "correct": false,
              "sections": {
                "what_actually": "`@CompilerControl` управляет инлайнингом и компиляцией методов (`DONT_INLINE`/`INLINE`/`EXCLUDE`), а не «константностью» полей — на constant folding входных данных он не влияет.",
                "source_of_confusion": "Обе вещи про «вмешательство в JIT», и легко решить, что аннотация над полем отключит сворачивание; но `@CompilerControl` применяется к методам и борется с другой оптимизацией.",
                "if_it_were_true": "Если бы `@CompilerControl` защищал вход, не понадобилось бы правило про нефинальные `@State`-поля — folding выключался бы одной аннотацией.",
                "how_it_should_be": "От constant folding защищает нефинальное поле в `@State`, а `@CompilerControl` оставляют для тонкой настройки инлайнинга."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 13,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Зачем возвращать значение из метода `@Benchmark` и что с ним делает JMH?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Возврат значения — идиоматичная защита: обвязка JMH сама прогоняет его через `Blackhole`, и constant folding не свернёт вычисление в константу",
              "correct": false,
              "sections": {
                "what_actually": "Возврат значения через `Blackhole` защищает именно от dead-code elimination (устранение неиспользуемого результата), а не от constant folding. Constant folding убирает «вход» бенчмарка, а не «выход».",
                "source_of_confusion": "Обе оптимизации JIT часто перечисляют рядом как причины вранья наивных замеров, и обе устраняют лишнюю работу, поэтому их легко перепутать.",
                "if_it_were_true": "Тогда хватило бы одного return, чтобы гонять `Math.sqrt(42.0)` с литералом и получать честный замер — но JIT всё равно свернёт `sqrt(42.0)` в константу ещё до возврата.",
                "how_it_should_be": "От constant folding защищает хранение входа в `@State` как нефинального поля (Q12); return c `Blackhole` закрывает только dead-code elimination."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Возврат значения — идиоматичная защита: обвязка JMH сама прогоняет его через `Blackhole`, и инлайнинг метода больше не искажает замер",
              "correct": false,
              "sections": {
                "what_actually": "Возврат через `Blackhole` не управляет инлайнингом вовсе — он лишь не даёт выбросить результат как мёртвый код. Инлайнингом управляет отдельная аннотация `@CompilerControl`.",
                "source_of_confusion": "И инлайнинг, и dead-code elimination — оптимизации C2, влияющие на итоговый замер, поэтому защиту от одного легко приписать другому.",
                "if_it_were_true": "Тогда `@CompilerControl(DONT_INLINE)` был бы не нужен — один return фиксировал бы границы инлайнинга, но он на инлайнинг никак не влияет.",
                "how_it_should_be": "Инлайнингом управляют через `@CompilerControl` (Q14: `DONT_INLINE`/`INLINE`/`EXCLUDE`); return c `Blackhole` решает другую задачу — защиту от dead-code elimination."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Возврат значения — идиоматичная защита: обвязка JMH сама прогоняет его через `Blackhole`, и dead-code elimination не выкинет вычисление",
              "correct": true,
              "sections": {
                "explanation": "Если результат вычисления нигде не наблюдается, C2 имеет право выбросить всё вычисление целиком (dead-code elimination). Возврат значения делает его «наблюдаемым»: сгенерированная JMH обвязка автоматически пропускает возвращённое значение через `Blackhole`, поэтому компилятор не вправе соптимизировать вычисление.",
                "example": "`@Benchmark public long compute(State s) { return s.a * 31 + s.b; }` — одно значение просто возвращается, и JMH потребляет его за вас; ничего дополнительно писать не нужно.",
                "when_to_apply": "Когда метод вычисляет ровно одно значение — это самый дешёвый способ защититься от самой частой ошибки микробенчмаркинга. Если значений несколько, вернуть можно только одно, а остальные пропустить через `Blackhole.consume(...)`.",
                "edge_cases": "Метод с типом `void` без `Blackhole` и без побочных эффектов почти всегда баг: вы измеряете пустоту. Присвоить результат локальной переменной недостаточно — она тоже мёртвая и будет удалена.",
                "related": "Прямое продолжение Q11: return и `Blackhole.consume(...)` — два штатных способа «потребить» результат. От constant folding (Q12) это не защищает — там нужен нефинальный вход через `@State`."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Возврат значения — идиоматичная защита: обвязка JMH сама прогоняет его через `Blackhole`, и результат усредняется между форками автоматически",
              "correct": false,
              "sections": {
                "what_actually": "`Blackhole` только потребляет значение, чтобы его нельзя было выкинуть; он не усредняет и не агрегирует результаты. Усреднение и доверительный интервал считает раннер по таймингам итераций и форков.",
                "source_of_confusion": "В отчёте JMH есть агрегированный `Score ± Error` по форкам, и кажется, будто это связано с тем, что вернул сам бенчмарк.",
                "if_it_were_true": "Тогда возвращаемое значение влияло бы на `Score`, и бенчмарк с другим return давал бы другую цифру — но `Score` зависит от времени вызовов, а не от самого значения.",
                "how_it_should_be": "Усреднение и `± error` (Q15) считает раннер по длительностям итераций/форков; роль возврата — только сделать вычисление наблюдаемым против dead-code elimination."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 14,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Какой набор режимов `CompilerControl.Mode` управляет инлайнингом и компиляцией метода в JMH через `@CompilerControl`?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "`DONT_INLINE` (запретить инлайн), `INLINE` (форсировать), `EXCLUDE` (не компилировать метод)",
              "correct": true,
              "sections": {
                "explanation": "`@CompilerControl` точечно управляет тем, как JIT обращается с конкретным методом, и реальные режимы перечисления `CompilerControl.Mode` — это `DONT_INLINE`, `INLINE` и `EXCLUDE`. `EXCLUDE` исключает метод из компиляции, оставляя его исполняться в интерпретаторе.",
                "example": "`@CompilerControl(CompilerControl.Mode.DONT_INLINE)` над приватным `hotPath()` запрещает инлайн, и метод виден в профиле как отдельный фрейм, а не растворяется в вызывающем коде.",
                "when_to_apply": "Достают в тонких сценариях: когда инлайнинг искажает замер, мешает разделить вклад частей метода или нужно воспроизвести реальный вызов через границу метода. В большинстве бенчмарков трогать не надо — JMH меряет код так, как его реально оптимизирует JVM.",
                "edge_cases": "`EXCLUDE` оставляет метод в интерпретаторе — это крайний диагностический случай для исследования поведения JIT, а не способ ускорения. По умолчанию инлайнинг включён, и это правильное поведение для большинства замеров.",
                "related": "Дополняет другие средства борьбы с артефактами JIT: `Blackhole` против dead-code elimination (Q11) и `@State` без `final` против constant folding (Q12). `@CompilerControl` же управляет именно компиляцией/инлайнингом конкретного метода."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "`DONT_INLINE` (запретить инлайн), `INLINE` (форсировать), `DONT_COMPILE` (не компилировать метод)",
              "correct": false,
              "sections": {
                "what_actually": "Режима `DONT_COMPILE` в `CompilerControl.Mode` нет. Запрет компиляции (оставить метод в интерпретаторе) выражается режимом `EXCLUDE`.",
                "source_of_confusion": "Название `DONT_COMPILE` звучит симметрично рядом с реальным `DONT_INLINE` и буквально описывает нужный эффект «не компилировать», поэтому легко принять за настоящий режим.",
                "if_it_were_true": "Если бы режим назывался `DONT_COMPILE`, код `@CompilerControl(Mode.DONT_COMPILE)` компилировался бы; на самом деле он не скомпилируется — такой константы в enum нет.",
                "how_it_should_be": "Чтобы исключить метод из компиляции, используют `CompilerControl.Mode.EXCLUDE`."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "`DONT_INLINE` (запретить инлайн), `FORCE_INLINE` (форсировать), `EXCLUDE` (не компилировать метод)",
              "correct": false,
              "sections": {
                "what_actually": "Режима `FORCE_INLINE` в `CompilerControl.Mode` нет: принудительный инлайн выражается режимом `INLINE`.",
                "source_of_confusion": "`FORCE_INLINE` — узнаваемый термин из C/C++ и нативных компиляторов, и он точно описывает смысл «форсировать инлайн», поэтому его легко спутать с реальным именем режима.",
                "if_it_were_true": "Будь режим назван `FORCE_INLINE`, аннотация `@CompilerControl(Mode.FORCE_INLINE)` компилировалась бы; в JMH такой константы нет, и код не соберётся.",
                "how_it_should_be": "Чтобы форсировать инлайнинг метода, используют `CompilerControl.Mode.INLINE`."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "`DONT_INLINE` (запретить инлайн), `INLINE` (форсировать), `INTERPRET_ONLY` (не компилировать метод)",
              "correct": false,
              "sections": {
                "what_actually": "Режима `INTERPRET_ONLY` в `CompilerControl.Mode` нет. Оставить метод только в интерпретаторе (исключить из компиляции) делает режим `EXCLUDE`.",
                "source_of_confusion": "`INTERPRET_ONLY` буквально описывает результат `EXCLUDE` — метод остаётся в интерпретаторе, — и потому выглядит правдоподобным именем режима.",
                "if_it_were_true": "Если бы существовал `INTERPRET_ONLY`, его эффект совпал бы с `EXCLUDE`; но такой константы в enum нет, и обращение к ней не скомпилируется.",
                "how_it_should_be": "Чтобы метод не компилировался и исполнялся интерпретатором, используют `CompilerControl.Mode.EXCLUDE`."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 15,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "В выводе JMH число после `±` (колонка `Error`) — это что по отношению к доверительному интервалу?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Полная ширина доверительного интервала, по умолчанию для уровня доверия 99.9%",
              "correct": false,
              "sections": {
                "what_actually": "Полная ширина интервала — это удвоенный `error`: от `score - error` до `score + error`. JMH печатает именно половину, а не всю ширину.",
                "source_of_confusion": "Интуитивно кажется, что «±error» задаёт сразу весь интервал, тогда как знак ± означает отступ в обе стороны, то есть половину ширины.",
                "if_it_were_true": "Если бы `error` был полной шириной, запись `42.318 ± 1.204` давала бы интервал [42.318; 43.522] только в одну сторону, что противоречит смыслу символа ±.",
                "how_it_should_be": "Правильно: `error` — половина ширины, полный интервал равен `2 × error` и симметричен вокруг `score`."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Половина доверительного интервала, по умолчанию для уровня доверия 95%",
              "correct": false,
              "sections": {
                "what_actually": "95% — это распространённый дефолт в общей статистике и многих инструментах, но не дефолт JMH; JMH использует более строгие 99.9%.",
                "source_of_confusion": "95% — самый узнаваемый уровень доверия из учебников статистики, поэтому его легко приписать и JMH по привычке.",
                "if_it_were_true": "При 95% интервал был бы уже (меньше `error`) при тех же данных, и перекрытие интервалов двух бенчмарков срабатывало бы реже, давая больше ложных «значимых» различий.",
                "how_it_should_be": "Правильно: дефолтный уровень доверия в JMH — 99.9%, что даёт более широкий и консервативный интервал."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Половина диапазона между минимальной и максимальной итерацией, по уровню доверия 99.9%",
              "correct": false,
              "sections": {
                "what_actually": "Половина размаха (max−min)/2 — это другая статистика, чувствительная к единичным выбросам; `error` же считается из дисперсии по итерациям и форкам.",
                "source_of_confusion": "И размах, и доверительный интервал описывают «разброс», поэтому половину размаха легко спутать с половиной доверительного интервала.",
                "if_it_were_true": "Если бы `error` был половиной размаха, один аномальный прогон (GC-пауза) раздувал бы его произвольно, и уровень доверия 99.9% к нему было бы нельзя применить.",
                "how_it_should_be": "Правильно: `error` — половина доверительного интервала, выводимая из дисперсии выборки, а не из крайних значений."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Половина доверительного интервала, по умолчанию для уровня доверия 99.9%",
              "correct": true,
              "sections": {
                "explanation": "JMH печатает `score ± error`, где `error` — это половина ширины доверительного интервала (отступ в обе стороны от точечной оценки). Дефолтный уровень доверия — 99.9%, поэтому это разброс между итерациями и форками, а не «погрешность прибора».",
                "example": "Строка `StringBench.concat avgt 5 42.318 ± 1.204 ns/op` означает интервал примерно [41.114; 43.522] ns/op: 42.318 — точечная оценка, 1.204 — половинная ширина.",
                "when_to_apply": "При сравнении двух бенчмарков смотрят, перекрываются ли интервалы `score ± error`: если да — разница статистически незначима и нельзя говорить «A быстрее B».",
                "edge_cases": "Большой `error` относительно `score` (например ±50%) — сигнал нестабильного замера: мало прогрева, мешает GC или бимодальное поведение JIT; цифрам верить рано.",
                "related": "Связано с `@OutputTimeUnit` (единицы в колонке `Units`) и с `Mode.SampleTime`, где вместо одного интервала печатаются перцентили p0.50/p0.99/p0.999."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 16,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Профилировщик async-profiler подключают к JMH через `-prof async`. Чем он принципиально точнее встроенного `-prof stack`?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Обходит safepoint bias, сэмплируя стек по таймеру safepoint-поллинга",
              "correct": false,
              "sections": {
                "what_actually": "Сэмплирование «по таймеру safepoint-поллинга» — это и есть механизм обычных safepoint-профайлеров вроде `-prof stack`: стек снимается, когда поток дошёл до safepoint, что как раз и создаёт смещение.",
                "source_of_confusion": "Формулировка почти дословно совпадает с правильной и тоже упоминает safepoint bias, но подменяет несмещающий механизм (`AsyncGetCallTrace`) ровно тем механизмом, который смещение и порождает.",
                "if_it_were_true": "Если бы async-profiler сэмплировал по safepoint-поллингу, он унаследовал бы тот же safepoint bias, что и `-prof stack`, и никакого преимущества по точности не давал бы.",
                "how_it_should_be": "async-profiler снимает стек через `AsyncGetCallTrace` по асинхронному сигналу вне safepoint'ов — именно это и убирает safepoint bias."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Обходит safepoint bias, сэмплируя стек через `AsyncGetCallTrace`",
              "correct": true,
              "sections": {
                "explanation": "Встроенный `-prof stack` снимает стек только в safepoint'ах, поэтому сэмплы смещены к точкам, где поток успевает остановиться (safepoint bias), и горячий код между ними недооценивается. async-profiler сэмплирует через `AsyncGetCallTrace`, который снимает стек в произвольный момент по сигналу, а не на safepoint, и потому даёт несмещённую картину при низком overhead.",
                "example": "Запуск: `-prof async:libPath=/opt/async-profiler/libasyncProfiler.so;output=flamegraph;event=cpu`. На выходе — flame-graph, где видно реальное распределение CPU, а не только safepoint-кадры.",
                "when_to_apply": "Когда нужно понять, где именно горит CPU в бенчмарке, а грубый `-prof stack` показывает смазанную из-за safepoint bias картину; также для alloc/lock-профилирования с выводом во flame-graph или JFR.",
                "edge_cases": "Требует нативной библиотеки `libasyncProfiler.so` и обычно прав на perf-события ОС (Linux); это внешний профилировщик, а не часть `jmh-core`.",
                "related": "Дополняет `-prof gc` (аллокации, `B/op`) и `-prof perfasm` (горячий ассемблер): gc отвечает «сколько аллоцируем», async-profiler — «где горит CPU»."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Обходит safepoint bias, сэмплируя стек через `GetStackTrace` (JVMTI)",
              "correct": false,
              "sections": {
                "what_actually": "`GetStackTrace` из JVMTI снимает стек только тогда, когда поток приведён в safepoint, поэтому он подвержен ровно тому же safepoint bias, что и `-prof stack`.",
                "source_of_confusion": "`GetStackTrace` и `AsyncGetCallTrace` похожи по имени и оба возвращают стек потока, но первый — safepoint-зависимый API JVMTI, а второй — асинхронный и safepoint-независимый.",
                "if_it_were_true": "Будь точность async-profiler основана на `GetStackTrace`, он замирал бы на safepoint'ах и не мог бы устранить смещение — преимущество исчезло бы.",
                "how_it_should_be": "Несмещённость даёт именно `AsyncGetCallTrace`, снимающий стек вне safepoint'ов; `GetStackTrace` (JVMTI) для этого не подходит."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Обходит safepoint bias, сэмплируя стек через `Blackhole.consume`",
              "correct": false,
              "sections": {
                "what_actually": "`Blackhole.consume` — это API JMH для защиты от dead-code elimination: оно «потребляет» результат, чтобы JIT не выкинул вычисление. К профилированию и снятию стека оно отношения не имеет.",
                "source_of_confusion": "`Blackhole` — центральное понятие JMH и легко всплывает по ассоциации, но это инструмент против оптимизаций компилятора, а не способ сэмплировать стек.",
                "if_it_were_true": "Если бы `Blackhole.consume` снимал стеки, любой бенчмарк с потреблением результата автоматически профилировался бы — но `consume` лишь делает значение наблюдаемым для JIT.",
                "how_it_should_be": "Стек async-profiler снимает через `AsyncGetCallTrace`; `Blackhole.consume` решает совсем другую задачу — защиту от dead-code elimination."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 17,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "Среди типичных ошибок микробенчмаркинга — ситуация, когда на вход бенчмарка подали литералы, и JIT посчитал результат на этапе компиляции, заменив метод возвратом готового значения. Как называется эта ошибка?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Dead-code elimination — подали литералы вместо `@State`, измерили возврат константы",
              "correct": false,
              "sections": {
                "what_actually": "Dead-code elimination — выкидывание вычисления, чьи результаты нигде не наблюдаются (не потреблены): JIT убирает «выход» бенчмарка, и вы измеряете пустоту.",
                "source_of_confusion": "Обе оптимизации C2 приводят к тому, что измеряется «ничто», и обе перечислены рядом в списке типичных ошибок, поэтому их легко перепутать местами.",
                "if_it_were_true": "Если бы причиной была dead-code elimination, проблема решалась бы потреблением результата (`return` или `Blackhole.consume`), а не выносом входа в `@State`.",
                "how_it_should_be": "Описанная в вопросе подмена входов литералами — это constant folding; dead-code elimination относится к непотреблённому выходу."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Profile pollution — подали литералы вместо `@State`, измерили возврат константы",
              "correct": false,
              "sections": {
                "what_actually": "Profile pollution — это взаимное «отравление» профиля JIT при прогоне нескольких бенчмарков в одной JVM, лечится форком (`@Fork ≥ 1`), а `@Fork(0)` его как раз провоцирует.",
                "source_of_confusion": "И profile pollution, и constant folding — про искажения со стороны JIT-компилятора, и оба попадают в список типичных ошибок замера.",
                "if_it_were_true": "Если бы дело было в profile pollution, лекарством был бы запуск в свежей JVM на бенчмарк, а не вынос входных данных в `@State`.",
                "how_it_should_be": "Подмена входов литералами и сворачивание результата на компиляции — это constant folding; profile pollution касается межбенчмарочной изоляции через форк."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Constant folding — подали литералы вместо `@State`, измерили возврат константы",
              "correct": true,
              "sections": {
                "explanation": "Constant folding — это сворачивание выражения над константами на этапе компиляции: если вход известен заранее (литерал или `final`-поле), C2 вычислит ответ один раз и подставит его, и бенчмарк измерит лишь возврат готового значения, а не работу алгоритма. Именно про подмену реальных входов литералами и идёт речь в формулировке.",
                "example": "`return Math.sqrt(42.0)` свернётся в константу; правильно — хранить вход в `@State` как нефинальное поле `public double x = 42.0` и писать `return Math.sqrt(in.x)`, тогда JIT не докажет константность.",
                "when_to_apply": "Помнить при любом бенчмарке, где есть числовой/строковый вход: все входные данные выносить в `@State`-объект нефинальными полями, не хардкодить значения в тело метода.",
                "edge_cases": "Опасны не только литералы, но и `final`-поля и значения, которые JIT может доказать константными по потоку данных; даже инициализация в `@Setup` не спасёт, если поле объявлено `final`.",
                "related": "Симметрична dead-code elimination: constant folding убирает вход бенчмарка, а dead-code elimination — выход; от первого защищает `@State` без `final`, от второго — `Blackhole`/return."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Insufficient warmup — подали литералы вместо `@State`, измерили возврат константы",
              "correct": false,
              "sections": {
                "what_actually": "Insufficient warmup — это нехватка прогревочных итераций, из-за чего меряется интерпретатор или C1 вместо стационарного C2-кода; лечится увеличением `@Warmup`.",
                "source_of_confusion": "Оба дефекта искажают результат из-за поведения JIT и стоят в одном списке типичных ошибок, поэтому название легко подставить не то.",
                "if_it_were_true": "Если бы причиной была нехватка прогрева, замер чинился бы добавлением warmup-итераций, а не выносом входа в нефинальное поле `@State`.",
                "how_it_should_be": "Описанная подмена входов литералами — это constant folding; нехватка прогрева — отдельная ошибка про необходимость довести код до steady-state."
              }
            }
          ]
        }
      ]
    },
    {
      "q_number": 18,
      "blocks": [
        {
          "block_idx": 0,
          "question_text": "На какой вопрос отвечает JMH, в отличие от связки JFR + JMC?",
          "options": [
            {
              "order": 0,
              "label": "A",
              "text": "Какой из двух вариантов реализации быстрее и на сколько",
              "correct": true,
              "sections": {
                "explanation": "JMH — это микробенчмарк-харнесс: он изолированно сравнивает конкретные реализации одного фрагмента (синтетический бенчмарк, прогрев, форк) и выдаёт `score ± error` для каждого варианта. Его вопрос — сравнение «A против B», а не диагностика живой системы.",
                "example": "Сравнить `stream().mapToInt().sum()` против обычного `for`-цикла на размерах входа 10/1000/100000: JMH прогонит оба варианта и покажет, что у стримов score хуже на малых N и сравним на больших.",
                "when_to_apply": "Когда уже известна горячая точка и нужно выбрать между двумя конкретными способами её оптимизации перед тем, как катить изменение в прод.",
                "edge_cases": "JMH не скажет, какой метод в вашем сервисе горячий: если гонять бенчмарк, не зная узкого места, легко оптимизировать код, который в проде вообще не нагружен.",
                "related": "Симметрично JFR/JMC, которые отвечают на макро-вопрос «что тормозит в реальном приложении»; правильный порядок — сначала профайлер находит точку, затем JMH сравнивает варианты её правки."
              }
            },
            {
              "order": 1,
              "label": "B",
              "text": "Какой метод в реальном приложении самый горячий и почему",
              "correct": false,
              "sections": {
                "what_actually": "На этот вопрос отвечает связка JFR + JMC (или async-profiler): low-overhead запись событий рантайма прямо в проде даёт timeline аллокаций, GC, lock contention и список hot methods всей живой JVM.",
                "source_of_confusion": "И JMH, и JFR относятся к performance-инструментам JVM и оба оперируют временем выполнения, поэтому их роли легко смешать — но JMH работает на микро-уровне отдельного фрагмента, а не всей системы.",
                "if_it_were_true": "Тогда JMH был бы профайлером и его можно было бы навесить на работающий сервис, чтобы найти узкое место — но синтетический бенчмарк ничего не знает о реальной нагрузке приложения.",
                "how_it_should_be": "Поиск горячего метода — задача JFR/JMC или async-profiler на реальном приложении; JMH подключают уже после, чтобы сравнить варианты правки найденной точки."
              }
            },
            {
              "order": 2,
              "label": "C",
              "text": "Сколько аллокаций и пауз GC создаёт работающий сервис под нагрузкой",
              "correct": false,
              "sections": {
                "what_actually": "Это поле JFR: он с минимальными накладными записывает события рантайма — аллокации, GC-паузы, lock contention — на живом сервисе, а JMC потом визуализирует эту запись.",
                "source_of_confusion": "У JMH есть `-prof gc`, который тоже показывает аллокации (`gc.alloc.rate`, `B/op`), поэтому кажется, что он измеряет GC реального сервиса — но он считает их только внутри синтетического бенчмарка.",
                "if_it_were_true": "Тогда JMH можно было бы постоянно держать на проде как монитор аллокаций и GC — но он не пишет события рантайма работающего приложения, а гоняет изолированный фрагмент.",
                "how_it_should_be": "Аллокации и GC живого сервиса под нагрузкой смотрят через JFR + JMC прямо на проде/стейдже, а не через микробенчмарк."
              }
            },
            {
              "order": 3,
              "label": "D",
              "text": "Какие события рантайма JVM накопились на проде за последний час",
              "correct": false,
              "sections": {
                "what_actually": "Это делает JFR: встроенный в JVM механизм записи событий с минимальными накладными, который пишет timeline рантайма на проде непрерывно или по триггеру для последующего разбора в JMC.",
                "source_of_confusion": "Оба инструмента работают с JVM и временем, поэтому запись событий рантайма легко спутать с тем, что меряет JMH — но JMH не пишет историю живой JVM, а изолированно прогоняет бенчмарк.",
                "if_it_were_true": "Тогда JMH запускали бы постоянно на проде для записи timeline событий — но он по дизайну гоняется в отдельном модуле на тихой машине, а не как фоновый рекордер сервиса.",
                "how_it_should_be": "Историю событий рантайма за период собирает JFR на проде, а анализирует JMC; JMH к записи живой JVM отношения не имеет."
              }
            }
          ]
        }
      ]
    }
  ]
}