# ===========================
# Продакшн-профиль
# ===========================
spring:
  thymeleaf:
    cache: true

app:
  # В production токен обязателен: без него приложение не стартует.
  admin-token: ${APP_ADMIN_TOKEN}
  security:
    cors:
      allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:https://localhost}
