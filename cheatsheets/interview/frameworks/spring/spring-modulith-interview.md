---
title: "Вопросы на собеседовании: Spring Modulith"
description: "Spring Modulith для модульных монолитов: определение модулей, изоляция, @ApplicationModuleListener, персистентность событий, тестирование, переход к микросервисам"
tags:
  - interview
  - spring
  - spring-modulith-interview
type: "interview"
difficulty: "intermediate"
aliases:
  - "Вопросы на собеседовании"
  - "Spring Modulith"
  - "Spring Modulith interview"
  - "Spring Modulith собеседование"
prerequisites:
  - "[[spring-modulith]]"
next: []
updated: "2026-04-25"
---
# Вопросы на собеседовании: `Spring Modulith`

`Spring Modulith` — библиотека для построения модульных монолитов на Spring Boot. Обеспечивает явные границы между модулями, верифицирует зависимости и предоставляет event-driven взаимодействие через `ApplicationEvents`.

Дата последнего обновления: 2026-04-20

## Полезные ссылки

### Официальная документация

- [Spring Modulith Docs](https://docs.spring.io/spring-modulith/docs/current/reference/html/) — официальная документация
- [Baeldung: Spring Modulith](https://www.baeldung.com/spring-modulith) — практическое введение

## Содержание

- [Полезные ссылки](#полезные-ссылки)
- [See also](#see-also)

**Основы**
- [Q1. Что такое Spring Modulith и какую проблему он решает?](#q1-что-такое-spring-modulith-и-какую-проблему-он-решает)
- [Q2. Как Spring Modulith определяет модуль?](#q2-как-spring-modulith-определяет-модуль)
- [Q3. Как проверить соблюдение архитектурных правил Spring Modulith в CI?](#q3-как-проверить-соблюдение-архитектурных-правил-spring-modulith-в-ci)
- [Q4. Как модули в Spring Modulith должны взаимодействовать между собой?](#q4-как-модули-в-spring-modulith-должны-взаимодействовать-между-собой)
- [Q5. Что такое @ApplicationModuleListener и чем отличается от @EventListener?](#q5-что-такое-applicationmodulelistener-и-чем-отличается-от-eventlistener)

**Персистентность и тестирование**
- [Q6. Как работает персистентность событий в Spring Modulith?](#q6-как-работает-персистентность-событий-в-spring-modulith)
- [Q7. Как тестировать отдельный модуль Spring Modulith в изоляции?](#q7-как-тестировать-отдельный-модуль-spring-modulith-в-изоляции)
- [Q8. Какие режимы bootstrap существуют в @ApplicationModuleTest?](#q8-какие-режимы-bootstrap-существуют-в-applicationmoduletest)
- [Q9. Что такое Scenarios API и для чего он нужен?](#q9-что-такое-scenarios-api-и-для-чего-он-нужен)

**Архитектура и продвинутые темы**
- [Q10. Как визуализировать зависимости между модулями?](#q10-как-визуализировать-зависимости-между-модулями)
- [Q11. Когда использовать Spring Modulith, а когда — микросервисы?](#q11-когда-использовать-spring-modulith-а-когда--микросервисы)
- [Q12. Как Spring Modulith облегчает переход к микросервисам?](#q12-как-spring-modulith-облегчает-переход-к-микросервисам)
- [Q13. Что такое Named Interface в Spring Modulith и зачем он нужен?](#q13-что-такое-named-interface-в-spring-modulith-и-зачем-он-нужен)
- [Q14. Как включить Spring Modulith в существующий Spring Boot проект?](#q14-как-включить-spring-modulith-в-существующий-spring-boot-проект)
- [Q15. Как Spring Modulith соотносится с Hexagonal Architecture и DDD?](#q15-как-spring-modulith-соотносится-с-hexagonal-architecture-и-ddd)

## Q1. Что такое Spring Modulith и какую проблему он решает?

Spring Modulith — библиотека для построения **модульных монолитов** на Spring Boot: она даёт коду явные внутренние границы и не позволяет им размываться.

**Какую боль лечит.** В обычном монолите все классы технически видны друг другу. Со временем это вырождается в «большой ком грязи» (Big Ball of Mud): один модуль начинает дёргать внутренние классы другого, появляются циклические зависимости, и сделать это можно случайно — компилятор не возражает. В итоге монолит невозможно ни понять, ни безопасно распилить.

**Что предлагает Spring Modulith.** Модульный монолит — это по-прежнему один деплоируемый артефакт (одна сборка, одна БД, один процесс), но с явными границами между модулями, изоляцией внутренней реализации и зависимостями, которые можно **проверить тестом**. Нарушил границу — упал тест, а не «узнали через год».

**Почему это ценно:**
- Дисциплина границ, как у микросервисов, но без их инфраструктурной цены (сеть, отдельные деплои, распределённые транзакции).
- Архитектура не деградирует незаметно: правила проверяются автоматически.
- Готовый плацдарм для будущей декомпозиции — модуль превращается в кандидата на отдельный сервис.

**Сценарий применения.** Промежуточный шаг между монолитом и микросервисами: когда хочется навести порядок в монолите, не платя сразу за распределённую систему.


## Q2. Как Spring Modulith определяет модуль?

Модуль определяется **по структуре пакетов, без какой-либо аннотации на самом модуле**. Каждый пакет первого уровня (top-level) рядом с классом `@SpringBootApplication` Spring Modulith считает отдельным модулем — границы выводятся из layout-а, а не настраиваются вручную.

```
com.example.shop
├── ShopApplication.java       ← корневой пакет
├── order/                     ← модуль Order
│   ├── OrderService.java      ← public API (доступен другим модулям)
│   └── internal/              ← закрытая реализация
│       └── OrderRepository.java
├── inventory/                 ← модуль Inventory
└── notification/              ← модуль Notification
```

**Как устроена видимость:**
- Классы прямо в корне пакета модуля (`order/OrderService.java`) — это его **публичный API**, видимый другим модулям.
- Всё во вложенных пакетах модуля (`order/internal/...`) — закрытая реализация: технически это обычные `public`-классы Java, но Spring Modulith считает их приватными для модуля.

**Почему это важно.** Если из `inventory/` обратиться к `order/internal/OrderRepository`, код скомпилируется (для JVM это публичный класс), но `verify()` распознает это как нарушение границы и завалит проверку. Так модель «публичный API против внутренностей» становится исполняемым правилом, а не договорённостью на словах.


## Q3. Как проверить соблюдение архитектурных правил Spring Modulith в CI?

Правила проверяются **обычным JUnit-тестом** — одной строкой `verify()`. Под капотом Spring Modulith использует ArchUnit, поэтому нарушение архитектуры падает так же, как падает любой unit-тест.

```java
@Test
void modulesAreCompliant() {
    ApplicationModules.of(ShopApplication.class).verify();
}
```

`verify()` проверяет:
- **Нет обращений к `internal/` другого модуля** — никто не лезет во внутренности соседа.
- **Нет циклических зависимостей** между модулями — граф зависимостей остаётся ацикличным.

**Что происходит при нарушении.** Метод выбрасывает исключение с человекочитаемым описанием: какой модуль, какой класс и какую границу нарушает. Тест краснеет, сборка останавливается.

**Почему держать это в CI.** Проверка ловит деградацию архитектуры в момент её появления — в пул-реквесте, а не спустя месяцы. Это дёшево (один тест) и превращает «договорённость о границах» в защиту, которую нельзя обойти по невнимательности.


## Q4. Как модули в Spring Modulith должны взаимодействовать между собой?

Есть два разрешённых способа, и оба не позволяют залезать во внутренности соседа:

- **Прямой вызов публичного API** — обращение к классу/интерфейсу в корне пакета модуля (не в `internal/`). Синхронно, в той же транзакции. Подходит, когда вызывающему нужен ответ прямо сейчас (например, запросить данные).
- **Spring Application Events** — модуль публикует событие, заинтересованные модули его слушают. Асинхронно и без связи по типам реализации. Подходит для уведомления «случилось вот это» — публикатор не знает и не должен знать, кто отреагирует.

**Почему предпочитают события.** Прямой вызов API создаёт жёсткую зависимость «A знает про B». Событие разворачивает связь: `Order` просто сообщает «заказ размещён», а `Inventory` сам решает подписаться. Связанность падает, и впоследствии такой модуль проще вынести в отдельный сервис (событие заменяется на сообщение в брокере).

В примере ниже `Order` публикует факт, а `Inventory` реагирует — между ними нет прямой ссылки на классы друг друга:

```java
// Модуль Order публикует событие
@Service
public class OrderService {
    private final ApplicationEventPublisher events;

    public void placeOrder(Order order) {
        orderRepository.save(order);
        events.publishEvent(new OrderPlaced(order.getId()));
    }
}

// Событие — простой record
public record OrderPlaced(UUID orderId) {}

// Модуль Inventory подписывается
@ApplicationModuleListener
public class InventoryListener {
    public void on(OrderPlaced event) {
        inventoryService.decreaseStock(event.orderId());
    }
}
```


## Q5. Что такое @ApplicationModuleListener и чем отличается от @EventListener?

`@ApplicationModuleListener` — это **составная аннотация-«мета»**, которая собирает в себе три настройки, нужные для надёжной связи между модулями. Под ней лежат:

- `@TransactionalEventListener(phase = AFTER_COMMIT)` — обработчик срабатывает **только после успешного коммита** транзакции публикатора.
- `@Async` — обработка идёт в **отдельном потоке**, не блокируя публикатора.
- `@Transactional` — сам обработчик выполняется **в своей транзакции**.

**Зачем такая комбинация.** Она разводит две операции по разным транзакциям. Если бы слушатель работал внутри транзакции `Order` (как делает обычный `@EventListener`), то откат заказа откатил бы и побочный эффект в `Inventory` — модули оказались бы связаны на уровне транзакции. Здесь же `Inventory` реагирует только на **зафиксированный** факт и обрабатывает его независимо; падение слушателя не роняет публикатора.

**Чем отличается от `@EventListener`:**

| | `@EventListener` | `@ApplicationModuleListener` |
|---|---|---|
| Когда срабатывает | сразу при публикации | после коммита (`AFTER_COMMIT`) |
| Поток | тот же (синхронно) | отдельный (`@Async`) |
| Транзакция | та же, что у публикатора | своя |
| Изоляция модулей | низкая | высокая |

**Подводный камень.** Раз обработка асинхронная и после коммита, при падении слушателя или рестарте приложения событие может потеряться — если не включить персистентность событий (см. Q6), которая как раз закрывает этот разрыв.


## Q6. Как работает персистентность событий в Spring Modulith?

Персистентность событий решает главную слабость in-memory событий: если слушатель упал или приложение перезапустилось до обработки, событие теряется. Spring Modulith закрывает этот разрыв, **записывая каждое событие в БД** в той же транзакции, что и доменное изменение.

Включается стартером (для JPA):

```xml
<dependency>
  <groupId>org.springframework.modulith</groupId>
  <artifactId>spring-modulith-starter-jpa</artifactId>
</dependency>
```

**Как это работает по шагам:**
1. При публикации события Spring Modulith создаёт запись в таблице `event_publication` — это происходит **в одной транзакции** с доменной операцией (заказ и факт его публикации фиксируются атомарно).
2. После того как слушатель успешно отработал, запись помечается как завершённая (проставляется дата выполнения).
3. Незавершённые записи — это события, до которых обработка не дошла.

При рестарте приложения такие «зависшие» события можно переиграть:

```yaml
spring:
  modulith:
    republish-outstanding-events-on-restart: true
```

**Что это даёт.** Гарантию доставки **at-least-once** прямо внутри монолита, без внешнего брокера (Kafka/RabbitMQ). По сути это локальный паттерн Transactional Outbox: факт и обещание его обработать сохраняются атомарно, поэтому ничего не теряется.

**Важный нюанс.** Гарантия именно *at-least-once*, не *exactly-once* — после рестарта событие может быть обработано повторно. Поэтому слушатели должны быть **идемпотентными**.


## Q7. Как тестировать отдельный модуль Spring Modulith в изоляции?

Для этого есть аннотация `@ApplicationModuleTest`: она поднимает **только контекст текущего модуля**, а не всё приложение. Тест становится быстрым и по-настоящему изолированным — он проверяет логику одного модуля, не таща за собой соседей.

```java
@ApplicationModuleTest
class OrderModuleTests {

    @Test
    void placingOrderPublishesEvent(
            @Autowired OrderService orderService,
            ApplicationEvents events) {

        orderService.placeOrder(new Order(...));

        assertThat(events.ofType(OrderPlaced.class)).hasSize(1);
    }
}
```

**Что здесь происходит:**
- `@ApplicationModuleTest` сам определяет, в каком модуле лежит тест, и загружает бины только этого модуля.
- Параметр `ApplicationEvents` (из Spring Test) перехватывает опубликованные события — можно ассертить *что* событие было выпущено, не дёргая реальный модуль-подписчик.
- Зависимости от других модулей не поднимаются: их при необходимости мокируют (например, через `@MockBean`).

**Почему это правильный уровень теста.** Вы проверяете контракт модуля — «при размещении заказа публикуется `OrderPlaced`» — в отрыве от того, как на это событие отреагирует `Inventory`. Реакцию соседнего модуля и сквозной сценарий проверяют отдельно (см. Scenarios API, Q9).

**Сценарий применения.** Объём загружаемого контекста регулируется режимом bootstrap (см. Q8): по умолчанию — строго один модуль, при необходимости можно добавить его зависимости.


## Q8. Какие режимы bootstrap существуют в @ApplicationModuleTest?

Режим bootstrap управляет тем, **какую часть графа модулей** поднимать для теста. Это компромисс между скоростью/изоляцией и полнотой проверки: чем больше грузим, тем реалистичнее, но медленнее и менее изолированно.

| Режим | Что загружается | Когда выбирать |
|-------|-----------------|----------------|
| `STANDALONE` | Только текущий модуль (по умолчанию) | unit-уровень модуля, максимальная изоляция и скорость |
| `DIRECT_DEPENDENCIES` | Модуль + его прямые зависимости | модулю реально нужны бины соседей, от которых он зависит |
| `ALL_DEPENDENCIES` | Весь транзитивный граф зависимостей | проверка цепочки модулей целиком |

```java
@ApplicationModuleTest(mode = BootstrapMode.DIRECT_DEPENDENCIES)
class OrderIntegrationTests { ... }
```

**Эмпирическое правило.** Начинайте со `STANDALONE` (по умолчанию) и поднимайтесь по уровням только когда тесту не хватает реальных бинов соседних модулей. Чем шире bootstrap — тем медленнее тест и тем меньше остаётся той самой модульной изоляции, ради которой всё затевалось.


## Q9. Что такое Scenarios API и для чего он нужен?

Scenarios API — высокоуровневый **DSL для тестирования событийных сценариев** между модулями. Он решает конкретную проблему: события из `@ApplicationModuleListener` обрабатываются *асинхронно* и *после коммита*, поэтому наивный тест («дёрнули метод — сразу проверили результат») гоняется с реальностью и флакает. Scenarios API умеет **дождаться** нужного события, прежде чем проверять.

Читается как фраза: «**подай** стимул → **дождись** события такого-то типа → **проверь** его»:

```java
@ApplicationModuleTest
class OrderScenarios {

    @Test
    void orderPlacedTriggersNotification(Scenario scenario) {
        scenario
            .stimulate(() -> orderService.placeOrder(order))
            .andWaitForEventOfType(NotificationSent.class)
            .toArrive()
            .andVerify(event ->
                assertThat(event.orderId()).isEqualTo(order.getId()));
    }
}
```

**Чем он лучше ручного теста.** Без него пришлось бы вручную писать `Awaitility` или `Thread.sleep`, ловить таймауты и бороться с флакающими прогонами. Scenarios API инкапсулирует это ожидание: `andWaitForEventOfType(...).toArrive()` блокирует тест ровно до прибытия события (с разумным таймаутом). Поэтому он — основной инструмент для сквозной проверки связки «публикация → асинхронный `@ApplicationModuleListener` → результат».


## Q10. Как визуализировать зависимости между модулями?

Spring Modulith умеет **генерировать диаграммы и документацию из самой структуры кода** — модель модулей у него уже есть, нужно лишь её отрисовать. Это держит карту архитектуры в синхроне с реальностью: диаграмма строится по коду, а не рисуется руками и не устаревает.

Простейший вариант — распечатать модель в консоль; полноценный — сгенерировать PlantUML:

```java
// Вывод в консоль
ApplicationModules modules = ApplicationModules.of(ShopApplication.class);
modules.forEach(System.out::println);

// Генерация PlantUML-диаграмм (требует spring-modulith-docs)
new Documenter(modules)
    .writeModulesAsPlantUml()
    .writeIndividualModulesAsPlantUml();
```

- `writeModulesAsPlantUml()` — общая диаграмма всех модулей и их связей.
- `writeIndividualModulesAsPlantUml()` — по диаграмме на каждый модуль (его зависимости крупным планом).

Для этого нужен отдельный артефакт (помечается `optional`, чтобы не тянуть его в рантайм-сборку):

```xml
<dependency>
  <groupId>org.springframework.modulith</groupId>
  <artifactId>spring-modulith-docs</artifactId>
  <optional>true</optional>
</dependency>
```

**Сценарий применения.** Запускать генерацию из теста или в CI — тогда актуальная диаграмма архитектуры всегда лежит в репозитории и обновляется вместе с кодом.


## Q11. Когда использовать Spring Modulith, а когда — микросервисы?

Ключевой вопрос: **нужна ли вам независимость на уровне процесса/деплоя/данных, или достаточно дисциплины границ внутри одного приложения?** Если хватает второго — модульный монолит даёт почти всю пользу микросервисов без их цены.

**Spring Modulith подходит, если:**
- Команда небольшая (2–10 разработчиков) и работает над одним деплоем — координация дешевле, чем сетевые контракты.
- Нужны явные границы, но не хочется платить за накладные расходы микросервисов: сеть и её отказы, независимый деплой, распределённые транзакции.
- Планируется *постепенный* переход к микросервисам — модули станут готовыми линиями реза для будущей декомпозиции.

**Микросервисы нужны, если:**
- Компоненты надо **масштабировать независимо** (один грузит CPU, другой — память).
- Разные команды требуют **независимого цикла деплоя** — не хотят релизиться через общий артефакт.
- Нужна **изоляция данных на уровне БД** (отдельные схемы или базы), а не просто разделение кода.

**Компромисс.** Микросервисы покупают независимость ценой распределённой системы (сеть, согласованность, эксплуатация). Модульный монолит оставляет всё в одном процессе и одной БД, но взамен требует дисциплины, чтобы границы не размылись, — её как раз и обеспечивает `verify()`.


## Q12. Как Spring Modulith облегчает переход к микросервисам?

Главная идея: хорошо изолированный модуль **уже имеет ту же форму, что и сервис** — публичный контракт снаружи, скрытая реализация внутри, явные зависимости. Поэтому декомпозиция превращается из рискованного распутывания клубка в механическую замену механизма связи.

Соответствие «модуль → микросервис»:
- **Публичный API модуля** → REST/gRPC-контракт сервиса (граница для вызова уже определена — её нужно лишь вынести за процесс).
- **`ApplicationEvents`** → сообщения в Kafka/RabbitMQ (модель «публикую факт — кто-то слушает» сохраняется, меняется только транспорт).
- **Пакет `internal/`** уже скрыт — приватную реализацию не придётся выпутывать из чужих обращений.
- **Зависимости между модулями** уже явны и проверены `verify()` — вы заранее знаете весь граф связей и не наткнётесь на скрытую ссылку при распиле.

**Почему это снимает главный риск.** Самое дорогое при распиле монолита — обнаружить, что модули переплетены сильнее, чем казалось. Spring Modulith не даёт этой запутанности накопиться: границы чёткие с самого начала, поэтому при декомпозиции нет неприятных сюрпризов.


## Q13. Что такое Named Interface в Spring Modulith и зачем он нужен?

Named Interface — это способ **дать модулю несколько именованных точек входа** вместо одной общей.

По умолчанию публичный API модуля — это весь корень его пакета: всё, что лежит прямо в `order/`, видно соседям. Для маленького модуля это нормально, но у большого появляются разные грани — публичный API, типы событий, конфигурация — и хочется, чтобы соседи зависели только от нужной грани, а не от модуля целиком.

Named Interface объявляется на пакете и даёт этой части модуля имя:

```java
@org.springframework.modulith.NamedInterface("api")
package com.example.shop.order.api;
```

**Что это даёт:**
- Другой модуль может объявить зависимость от конкретного интерфейса (`order :: api`), а не от всего `order`.
- Остальные публичные пакеты модуля для него остаются недоступными — поверхность зависимости сужается до объявленного контракта.

**Зачем это нужно.** Чем уже точка входа, тем меньше способов на неё опереться «не туда» и тем проще потом эволюционировать или выносить модуль. Для крупных модулей с несколькими аспектами (API, events, config) это превращает один большой публичный пакет в набор аккуратных, явно поименованных контрактов.


## Q14. Как включить Spring Modulith в существующий Spring Boot проект?

Внедрять можно **инкрементально** — не нужно переписывать проект, прежде чем получить пользу. Порядок шагов:

1. **Добавить зависимость** `spring-modulith-starter-core`.
2. **Запустить `ApplicationModules.of(App.class).verify()`** — он покажет текущую картину: какие нарушения границ уже есть в коде (обращения к внутренностям, циклы).
3. **Чинить нарушения итеративно** — переносить детали реализации в `internal/`, убирать нежелательные зависимости и циклы. Делать это можно по одному модулю за раз.
4. **Зафиксировать `verify()` как тест в CI** — чтобы новые нарушения не появлялись.

**Почему такой порядок ценен.** Даже до того, как вы вычистили *все* старые нарушения, поставленная в CI проверка уже работает как храповик: архитектура перестаёт деградировать дальше. Вы стопорите ухудшение сразу, а долг разгребаете постепенно — это и есть главное преимущество перед «большим переписыванием».


## Q15. Как Spring Modulith соотносится с Hexagonal Architecture и DDD?

Spring Modulith — это **технический механизм для границ**, который сам по себе не диктует стиль, но хорошо ложится и на DDD, и на Hexagonal: он даёт исполняемое воплощение их идей на уровне пакетов Java.

**С DDD.** Модуль естественно отображается на **Bounded Context**: публичный API — это контракт контекста, `internal/` — его собственная модель, скрытая от остальных. Разница в том, что у DDD bounded context — концепция на словах, а Spring Modulith делает границу **проверяемой** (`verify()` не даст одному контексту залезть во внутренности другого).

**С Hexagonal Architecture (Ports & Adapters).** Аналогия на уровне модуля: публичный API играет роль **портов** (через них с модулем общаются), а классы в `internal/` — **адаптеров и реализаций** за этими портами.

**Важная оговорка.** Spring Modulith **не навязывает** конкретную внутреннюю структуру модуля — он следит только за границами *между* модулями. Хотите ли вы внутри модуля раскладывать код по гексагональным слоям или нет — ваше дело; библиотека с этим совместима, но не требует.

## See also

- [Spring Events](spring-events-interview.md) — ApplicationEventPublisher/Listener, основа взаимодействия модулей
- [Spring Boot](spring-boot-interview.md) — auto-configuration, основа для Spring Modulith
- [Microservices](../../architecture/microservices-interview.md) — переход модульного монолита к микросервисам
- [Domain-Driven Design](../../architecture/ddd-interview.md) — bounded contexts, aggregate roots — концепции модулей
- [Hexagonal Architecture](../../architecture/hexagonal-architecture-interview.md) — internal/ как адаптеры и реализации
- [Spring Testing](spring-testing-interview.md) — @ApplicationModuleTest изоляция при тестировании
- [Clean Architecture](../../architecture/clean-architecture-interview.md) — separation of concerns на уровне пакетов
- [Spring Kafka](spring-kafka-interview.md) — Kafka как замена ApplicationEvents при переходе к микросервисам
