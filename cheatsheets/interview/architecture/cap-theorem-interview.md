---
title: "Вопросы на собеседовании: CAP-теорема"
description: "Краткое введение: ответы по CAP-теореме для собеседований — ограничения распределённых систем, выбор между согласованностью и доступностью. CAP-теорема описывает ограничения распределённых систем: нельзя одновременно гарантировать согласованность (Consistency), доступность (Avail"
tags: ["interview", "architecture", "cap-theorem-interview"]
difficulty: "intermediate"
prerequisites: []
next: []
updated: "2026-02-11"
---
# Вопросы на собеседовании: `CAP`-теорема

Краткое введение: ответы по `CAP`-теореме для собеседований — ограничения распределённых систем, выбор между согласованностью и доступностью. **CAP-теорема** описывает ограничения распределённых систем: нельзя одновременно гарантировать согласованность (`Consistency`), доступность (`Availability`) и устойчивость к разделению (`Partition tolerance`). На интервью часто спрашивают про этот компромисс и примеры `CP`/`AP`-систем.

Дата последнего обновления: 2026-02-11

## Полезные ссылки

### Официальная документация

- [CAP Theorem](https://en.wikipedia.org/wiki/CAP_theorem) — CAP-теорема

### См. также

- [`distributed-systems-interview.md`](distributed-systems-interview.md) — распределённые системы
- [`consistency-patterns-interview.md`](consistency-patterns-interview.md) — паттерны согласованности

## Содержание

- [Полезные ссылки](#полезные-ссылки)

**Основы CAP**
- [Q1. (!) Что такое CAP-теорема?](#q1-что-такое-cap-теорема)
- [Q2. Какие три свойства описывает CAP?](#q2-какие-три-свойства-описывает-cap)
- [Q3. Почему на практике выбирают между C и A?](#q3-почему-на-практике-выбирают-между-c-и-a)
- [Q4. (!) Что такое CP-системы, приведите примеры](#q4-что-такое-cp-системы-приведите-примеры)
- [Q5. (!) Что такое AP-системы, приведите примеры](#q5-что-такое-ap-системы-приведите-примеры)
- [Q6. Что такое CA-системы и почему они редки?](#q6-что-такое-ca-системы-и-почему-они-редки)

**Детали и критика**
- [Q7. Что такое сетевое разделение (partition)?](#q7-что-такое-сетевое-разделение-partition)
- [Q8. Почему CAP описывает поведение при partition, а не в норме?](#q8-почему-cap-описывает-поведение-при-partition-а-не-в-норме)
- [Q9. В чём ограниченность CAP-теоремы?](#q9-в-чём-ограниченность-cap-теоремы)
- [Q10. Что такое PACELC и как связано с CAP?](#q10-что-такое-pacelc-и-как-связано-с-cap)
- [Q11. Можно ли систему переключать между CP и AP?](#q11-можно-ли-систему-переключать-между-cp-и-ap)
- [Q12. Как выбирать CP vs AP для бизнес-системы?](#q12-как-выбирать-cp-vs-ap-для-бизнес-системы)
- [Q13. Redis — CP или AP?](#q13-redis-cp-или-ap)
- [Q14. PostgreSQL с репликацией — CP или AP?](#q14-postgresql-с-репликацией-cp-или-ap)
- [Q15. Kafka и CAP: какой trade-off?](#q15-kafka-и-cap-какой-trade-off)

## Q1. (!) Что такое CAP-теорема?

**CAP-теорема** (`Consistency`, `Availability`, `Partition tolerance`) утверждает: в распределённой системе нельзя одновременно обеспечить все три свойства. Придётся пожертвовать одним из них. Сформулирована Эриком Брюером в 2000 году, формально доказана в 2002 году.

На практике **Partition tolerance** считают обязательным: сети падают, дата-центры изолируются. Поэтому выбор делается между **согласованностью** и **доступностью**: либо система при разделении блокирует запросы ради согласованных данных (`CP`), либо продолжает отвечать, допуская временную несогласованность (`AP`). CAP — это модель для понимания trade-offs, а не жёсткая классификация систем.

## Q2. Какие три свойства описывает CAP?

**Consistency** (согласованность) — все узлы видят одни и те же данные в один момент времени; чтение после записи возвращает последнее записанное значение. **Availability** (доступность) — каждый запрос получает ответ (успех или ошибка), система не блокирует клиентов. **Partition tolerance** (устойчивость к разделению) — система продолжает работать при разрыве связи между узлами, когда сообщения теряются или задерживаются.

В реальных распределённых системах сетевое разделение неизбежно, поэтому **Partition tolerance** не отбрасывают. Остаётся выбор между C и A: при partition нельзя иметь и то, и другое одновременно.

## Q3. Почему на практике выбирают между C и A?

**Partition tolerance** в распределённой системе нельзя отбросить: сети ненадёжны, дата-центры изолируются, задержки непредсказуемы. Любая система с репликацией или несколькими узлами должна учитывать возможность разделения.

Если мы хотим, чтобы система переживала partition, остаётся выбор: при разделении либо блокировать запросы до восстановления связи с кворумом (жертвуем **Availability**, получаем **CP**), либо продолжать обслуживать запросы и допускать устаревшие данные (жертвуем **Consistency**, получаем **AP**). Поэтому говорят: «на практике выбор между C и A».

## Q4. (!) Что такое CP-системы, приведите примеры

**CP-системы** жертвуют доступностью ради согласованности. При разделении сети они блокируют запись (и часто чтение), пока не восстановится связь с кворумом реплик. Цель — гарантировать, что все узлы видят одинаковые данные; клиент не получит устаревшую информацию, но может получить таймаут или отказ.

Примеры: **PostgreSQL** с синхронной репликацией, **MongoDB** с majority writes, **HBase**, **Zookeeper** (координация, конфигурация). Подходят там, где критична строгая согласованность — финансы, учёт, инвентарь — а кратковременная недоступность допустима.

## Q5. (!) Что такое AP-системы, приведите примеры

**AP-системы** жертвуют согласованностью ради доступности. При разделении сети они продолжают принимать запросы и отвечать, но могут отдавать устаревшие данные до схождения реплик. Репликация обычно асинхронная.

Примеры: **Cassandra**, **DynamoDB**, **CouchDB**, **Riak**. Многие поддерживают настраиваемые уровни согласованности: `ONE`, `QUORUM`, `ALL` в Cassandra — можно усилить C за счёт latency. Подходят для лент, счётчиков, кэшей, сценариев, где допустима eventual consistency.

## Q6. Что такое CA-системы и почему они редки?

**CA-системы** теоретически дают согласованность и доступность, но не устойчивы к разделению сети. В распределённой среде partition неизбежны, поэтому чисто CA в реальности почти не бывает.

CA по сути — одиночный узел или нераспределённая БД без репликации: одна база на одном сервере, локальная ФС. Как только появляются реплики и несколько узлов, приходится учитывать разделение и выбирать CP или AP. Единственный «CA» — это система, которая при partition просто перестаёт быть распределённой.

## Q7. Что такое сетевое разделение (partition)?

**Partition** (сетевое разделение) — ситуация, когда группа узлов перестаёт получать сообщения от другой группы из-за сетевого сбоя, задержки или потери пакетов. Узлы в каждой группе могут общаться между собой, но не с узлами «по ту сторону» partition.

Типичные причины: разрыв кабеля, отказ маршрутизатора, изоляция дата-центра (например, при проблемах с upstream-провайдером). Partition может быть временным (секунды) или длительным (минуты, часы). CAP говорит о поведении системы именно в момент partition.

## Q8. Почему CAP описывает поведение при partition, а не в норме?

CAP фокусируется на **момент partition**, потому что в нормальных условиях (без разделения сети) распределённая система может обеспечивать и согласованность, и доступность одновременно. Проблема возникает только когда сеть разделяется: тогда нельзя гарантировать и C, и A.

При partition нужно принять решение: блокировать запросы (CP) или допустить устаревшие данные (AP). В повседневной работе большинство систем работают без partition, поэтому CAP — это не про «всегда», а про «когда сеть сломалась».

## Q9. В чём ограниченность CAP-теоремы?

CAP описывает бинарный выбор при partition, но реальность сложнее. **Гранулярность**: система может быть CP для одних операций и AP для других (разные consistency levels). **Задержки**: CAP не учитывает latency — «доступная» система с 30-секундным ответом бесполезна. **Частичные отказы**: partition — не единственный тип сбоя; есть ещё отказы узлов, задержки.

**PACELC** расширяет CAP: при partition — выбор P (C или A), при отсутствии partition (E — Else) — выбор между Latency и Consistency. На практике многие системы настраиваются под сценарий, а не жёстко привязаны к CP или AP.

## Q10. Что такое PACELC и как связано с CAP?

**PACELC** — расширение CAP: при **P**artition выбор между **A** и **C** (как в CAP), при **E**lse (отсутствии partition) — выбор между **L**atency и **C**onsistency. То есть даже без partition система может жертвовать согласованностью ради более низкой задержки.

Пример: Cassandra с `read_repair` — при нормальной работе можно читать с `ONE` (быстро, но возможна устаревшая копия) или с `QUORUM` (медленнее, но свежее). PACELC лучше отражает реальные trade-offs: не только «при сбое», но и «в норме» — latency vs consistency.

## Q11. Можно ли систему переключать между CP и AP?

Да, многие системы позволяют настраивать поведение. **Cassandra**: `ONE`/`LOCAL_ONE` — скорее AP (быстрый ответ, возможна устаревшая копия), `QUORUM`/`ALL` — ближе к CP (читаем с большинства, ждём подтверждения записи). **MongoDB**: `majority` writes — CP, `w:1` — AP.

Для конкретного запроса можно выбирать уровень: критичные операции — сильнее consistency, некритичные — выше availability. Система не «чисто» CP или AP, а конфигурируемая по сценарию.

## Q12. Как выбирать CP vs AP для бизнес-системы?

Задайте вопрос: **допустима ли устаревшая информация?** Банковский баланс, инвентарь, место бронирования — нет, нужен CP. Лента постов, счётчики лайков, рекомендации — да, допустима eventual consistency, выбираем AP.

Учитывайте **влияние недоступности**: если при partition система «висит» 30 секунд — приемлемо ли для пользователей? Для платежей — часто да. Для real-time чата — нет. Также смотрите на **частоту partition**: в облаке с одной зоной — реже; в мульти-регионе — чаще, AP может быть предпочтительнее.

## Q13. Redis — CP или AP?

**Redis** в кластерном режиме (Redis Cluster) — в терминах CAP ближе к **AP**. При partition секции кластера продолжают обслуживать запросы; данные могут расходиться между секциями до восстановления связи. Redis не гарантирует strong consistency при partition.

Одиночный Redis (standalone) — по сути CA (один узел, нет partition между репликами в смысле CAP). Redis Sentinel с репликацией: при failover реплика асинхронная, возможна потеря данных при сбое мастера — тоже ближе к AP по поведению.

## Q14. PostgreSQL с репликацией — CP или AP?

Зависит от режима репликации. **Синхронная репликация** (`synchronous_commit=on`): запись подтверждается только после получения данных на реплику(и). При partition с потерей кворума реплик — запись блокируется или отказывает. Это **CP**.

**Асинхронная репликация**: мастер подтверждает запись сразу, репликация идёт в фоне. При partition мастер продолжает принимать запись — **AP**. Но при падении мастера возможна потеря данных (если реплика не успела получить изменения). По умолчанию PostgreSQL — синхронная для одной реплики в типичных настройках.

## Q15. Kafka и CAP: какой trade-off?

**Kafka** ориентирована на **доступность и отказоустойчивость**: при partition брокеры продолжают принимать и отдавать сообщения. Репликация асинхронная по умолчанию (можно настроить `acks=all` для сильнее guarantees). В терминах CAP — ближе к **AP**.

При `acks=1` или `acks=0` producer получает подтверждение до репликации — выше throughput, но возможна потеря при сбое. При `acks=all` producer ждёт подтверждения от in-sync реплик — ближе к CP, но выше latency. Kafka — это система обмена сообщениями, а не OLTP-БД; для неё важнее throughput и durability, чем строгая согласованность в смысле CAP.