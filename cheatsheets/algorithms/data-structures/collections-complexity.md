---
title: "Временная сложность коллекций Java"
description: "Руководство по производительности различных коллекций из Java Collection API с анализом временной сложности операций и результатами бенчмарков."
tags:
  - algorithms
  - data-structures
  - collections-complexity
difficulty: "intermediate"
prerequisites: []
next: []
updated: "2026-02-11"
---
# Временная сложность коллекций Java

Руководство по производительности различных коллекций из **Java Collection API** с анализом временной сложности операций и результатами бенчмарков.

## Полезные ссылки

### Официальная документация
- [Java Collections (Java SE 8)](https://docs.oracle.com/javase/8/docs/api/java/util/package-summary.html)
- [JMH (OpenJDK)](https://github.com/openjdk/jmh)

### См. также
- [[collections-big-o|Практические примеры Big O]]
- [[collections-lock-free|Структуры данных без блокировок]]
- [Коллекции Java](../../languages/java/README.md)

## Содержание

- [Обзор](#обзор)
- [Реализация на Java](#реализация-на-java)
- [Сравнительная таблица](#сравнительная-таблица)
- [Рекомендации по выбору коллекции](#рекомендации-по-выбору-коллекции)
- [Лучшие практики](#лучшие-практики)
- [Решение проблем](#решение-проблем)
- [Частые вопросы](#частые-вопросы)
- [Резюме](#резюме)
- [Реализация на Kotlin](#реализация-на-kotlin)


## Обзор

В этом туториале мы поговорим о производительности разных коллекций из **Java Collection API**. Когда мы говорим о коллекциях, мы обычно думаем о структурах данных **List**, **Map** и **Set**, а также об их общих реализациях.

Во-первых, мы рассмотрим понимание сложности **Big-O** для общих операций. Затем мы покажем реальные цифры времени выполнения некоторых операций сбора.

Обычно, когда мы говорим о временной сложности, мы имеем в виду нотацию **Big-O**. Проще говоря, нотация описывает, как время выполнения алгоритма растет с увеличением размера входных данных.

## Реализация на Java

### List коллекции

Начнем с простого списка, который представляет собой упорядоченную коллекцию.

Здесь мы рассмотрим обзор производительности реализаций **ArrayList**, **LinkedList** и **CopyOnWriteArrayList**.

#### ArrayList

**ArrayList** в **Java** поддерживается массивом. Это помогает понять внутреннюю логику его реализации.

**Итак, давайте сначала сосредоточимся на временной сложности общих операций на высоком уровне:**

1. **add()** - занимает `O(1)` времени; однако в худшем случае, когда необходимо создать новый массив и скопировать в него все элементы, это `O(n)`
2. **add(**index**, **element**)** - в среднем выполняется за время `O(n)`
3. **get()** - всегда является операцией `O(1)` с постоянным временем
4. **remove()** - работает за линейное время `O(n)`. Мы должны перебрать весь массив, чтобы найти элемент, подходящий для удаления
5. **indexOf()** - также работает в линейном времени. Он перебирает внутренний массив и проверяет каждый элемент один за другим, поэтому временная сложность этой операции всегда требует `O(n)` времени
6. **contains()** - реализация основана на **indexOf**(), поэтому она также будет выполняться за время `O(n)`

### Бенчмарк ArrayList

**Для тестирования производительности мы используем **JMH** (**Java `Microbenchmark` Harness**):**

```java
// Бенчмарк ArrayList через JMH: среднее время операций add/get/remove для выбора коллекции.
@BenchmarkMode(Mode.AverageTime)
@OutputTimeUnit(TimeUnit.MICROSECONDS)
@Warmup(iterations = 10)
public class ArrayListBenchmark {
    
    @State(Scope.Thread)
    public static class MyState {
        List<Employee> employeeList = new ArrayList<>();
        long iterations = 100000;
        Employee employee = new Employee(100L, "Harry");
        int employeeIndex = -1;
        
        @Setup(Level.Trial)
        public void setUp() {
            for (long i = 0; i < iterations; i++) {
                employeeList.add(new Employee(i, "John"));
            }
            employeeList.add(employee);
            employeeIndex = employeeList.indexOf(employee);
        }
    }
    
    @Benchmark
    public void testAdd(MyState state) {
        state.employeeList.add(new Employee(state.iterations + 1, "John"));
    }
    
    @Benchmark
    public void testAddAt(MyState state) {
        state.employeeList.add((int) (state.iterations), 
            new Employee(state.iterations, "John"));
    }
    
    @Benchmark
    public boolean testContains(MyState state) {
        return state.employeeList.contains(state.employee);
    }
    
    @Benchmark
    public int testIndexOf(MyState state) {
        return state.employeeList.indexOf(state.employee);
    }
    
    @Benchmark
    public Employee testGet(MyState state) {
        return state.employeeList.get(state.employeeIndex);
    }
    
    @Benchmark
    public boolean testRemove(MyState state) {
        return state.employeeList.remove(state.employee);
    }
}
```

**Результаты бенчмарка (**в микросекундах**):**

| Операция | Время | Сложность |
|----------|-------|-----------|
| **testAdd** | `2.296` ± `0.007` | `O(1)` |
| **testGet** | `0.007` ± `0.001` | `O(1)` |
| **testAddAt** | `101.092` ± `14.145` | `O(n)` |
| **testContains** | `709.404` ± `64.331` | `O(n)` |
| **testIndexOf** | `717.158` ± `58.782` | `O(n)` |
| **testRemove** | `624.856` ± `51.101` | `O(n)` |

Из результатов мы узнаем, что методы **testContains**() и **testIndexOf**() запускаются примерно в одно и то же время. Мы также можем ясно видеть огромную разницу между оценками методов **testAdd**() и **testGet**() по сравнению с остальными результатами.

#### CopyOnWriteArrayList

Такая реализация интерфейса **List** удобна при работе с многопоточными приложениями. Это потокобезопасно.

**Вот обзор производительности нотации **Big-O** для **CopyOnWriteArrayList**:**

1. **add()** - зависит от позиции, в которую мы добавляем значение, поэтому сложность составляет `O(n)`
2. **get()** - это `O(1)` операция с постоянным временем
3. **remove()** - занимает `O(n)` времени
4. **contains()** - аналогично, сложность `O(n)`

Как мы видим, использование этой коллекции очень затратно из-за характеристик производительности метода **add**().

**Результаты бенчмарка `CopyOnWriteArrayList`:**

| Операция | Время | Сложность |
|----------|-------|-----------|
| **testGet** | `0.006` ± `0.001` | `O(1)` |
| **testAdd** | `652.189` ± `36.641` | `O(n)` |
| **testAddAt** | `897.258` ± `35.363` | `O(n)` |
| **testContains** | `537.098` ± `54.235` | `O(n)` |
| **testIndexOf** | `547.207` ± `48.904` | `O(n)` |
| **testRemove** | `648.162` ± `138.379` | `O(n)` |

Здесь, опять же, цифры подтверждают теорию. Как мы видим, **testGet**() в среднем выполняется за `0.006` мс, что можно считать `O(1)`. Сравнивая с **ArrayList**, мы также замечаем значительную разницу между результатами метода **testAdd**(), так как здесь у нас есть сложность `O(n)` для метода **add**() по сравнению с `O(1)` **ArrayList**.

## LinkedList

**LinkedList** - это линейная структура данных, состоящая из узлов, содержащих поле данных и ссылку на другой узел.

**Приведем среднюю оценку времени, необходимого для выполнения некоторых основных операций:**

1. **add()** - добавляет элемент в конец списка. Он обновляет только хвост, и, следовательно, его сложность `O(1)` с постоянным временем
2. **add(**index**, **element**)** - в среднем выполняется за время `O(n)`
3. **get()** - поиск элемента занимает `O(n)` времени
4. **remove(**element**)** - чтобы удалить элемент, нам сначала нужно его найти. Эта операция `O(n)`
5. **remove(**index**)** - чтобы удалить элемент по индексу, нам сначала нужно перейти по ссылкам с самого начала; следовательно, общая сложность равна `O(n)`
6. **contains()** - также имеет временную сложность `O(n)`

**Результаты бенчмарка `LinkedList`:**

| Операция | Время | Сложность |
|----------|-------|-----------|
| **testAdd** | `2.580` ± `0.003` | `O(1)` |
| **testRemove** | `0.006` ± `0.001` | `O(1)` |
| **testGet** | `1561.831` ± `70.876` | `O(n)` |
| **testContains** | `1808.102` ± `68.155` | `O(n)` |

Из оценок видно, что добавление и удаление элементов в **LinkedList** происходит довольно быстро. Кроме того, существует значительный разрыв в производительности между операциями добавления/удаления и получения/содержания.

## Map коллекции

В последних версиях **JDK** мы наблюдаем значительное улучшение производительности для реализаций карт, например замену **LinkedList** сбалансированной структурой узлов дерева в **HashMap** и внутренних реализаций **LinkedHashMap**. Это сокращает время поиска элемента в наихудшем случае с `O(n)` до `O(**log(n**)`) во время коллизий **HashMap**.

Однако, если мы реализуем правильные методы **equals**() и **hashCode**(), коллизии маловероятны.

## HashMap и его варианты

Сохранение и извлечение элементов из **HashMap** занимает постоянное время `O(1)`.

**Результаты бенчмарка `HashMap`:**

| Операция | Время | Сложность |
|----------|-------|-----------|
| **testContainsKey** | `0.009` ± `0.002` | `O(1)` |
| **testGet** | `0.011` ± `0.001` | `O(1)` |
| **testPut** | `0.019` ± `0.002` | `O(1)` |
| **testRemove** | `0.010` ± `0.001` | `O(1)` |

Для всех перечисленных методов у нас есть `O(1)` для **HashMap**, **LinkedHashMap**, **IdentityHashMap**, **WeakHashMap**, **EnumMap** и **ConcurrentHashMap**.

**Сравнительная таблица Map реализаций:**

| Операция | **LinkedHashMap** | **IdentityHashMap** | **WeakHashMap** | **ConcurrentHashMap** |
|----------|---------------|------------------|-------------|-------------------|
| **testContainsKey** | `0.008` | `0.009` | `0.014` | `0.011` |
| **testGet** | `0.011` | `0.109` | `0.019` | `0.012` |
| **testPut** | `0.020` | `0.013` | `0.020` | `0.031` |
| **testRemove** | `0.011` | `0.115` | `0.021` | `0.019` |

Из выходных чисел мы можем подтвердить требования временной сложности `O(1)`.

## TreeMap и ConcurrentSkipListMap

Для древовидной структуры **TreeMap** и **ConcurrentSkipListMap** время операций **put**(), **get**(), **remove**() и **containsKey**() равно `O(**log(n**)`).

**Результаты для различных размеров данных:**

| Размер (n) | Время выполнения |
|------------|------------------|
| 1,`000` | 00:03:17 мс |
| 10,`000` | 00:03:17 мс |
| `100`,`000` | 00:03:30 мс |
| 1,`000`,`000` | 00:05:27 мс |

После сравнения чисел времени выполнения с функцией **log**(n) каждого n мы можем подтвердить, что корреляция обеих функций совпадает.

## Set коллекции

Как правило, **Set** представляет собой набор уникальных элементов. Здесь мы рассмотрим реализации **HashSet**, **LinkedHashSet**, **EnumSet**, **TreeSet**, **CopyOnWriteArraySet** и **ConcurrentSkipListSet** интерфейса **Set**.

### Временная сложность Set операций

Для **HashSet**, **LinkedHashSet** и **EnumSet** операции **add**(), **remove**() и **contains**() стоят постоянно `O(1)` времени благодаря внутренней реализации **HashMap**.

Точно так же **TreeSet** имеет временную сложность `O(**log(n**)`) для операций, перечисленных в предыдущей группе. Это связано с реализацией **TreeMap**. Временная сложность для **ConcurrentSkipListSet** также составляет `O(**log(n**)`) времени, так как она основана на структуре данных списка пропуска.

Для **CopyOnWriteArraySet** методы **add**(), **remove**() и **contains**() имеют среднюю временную сложность `O(n)`.

**Результаты бенчмарка `HashSet`:**

| Размер | **add**() | **remove**() | **contains**() |
|--------|-------|----------|------------|
| 1,`000` | `0.026` | `0.009` | `0.009` |
| 10,`000` | `0.023` | `0.009` | `0.009` |
| `100`,`000` | `0.024` | `0.009` | `0.010` |

**Результаты бенчмарка `LinkedHashSet`:**

| Размер | **add**() | **remove**() | **contains**() |
|--------|-------|----------|------------|
| 1,`000` | `0.022` | `0.008` | `0.008` |
| 10,`000` | `0.026` | `0.012` | `0.013` |
| `100`,`000` | `0.027` | `0.009` | `0.009` |

Как мы видим, оценки остаются практически одинаковыми для каждой операции. Когда мы сравниваем их с результатами теста **HashMap**, они также выглядят одинаково.

В результате мы подтверждаем, что все протестированные методы работают с постоянным временем `O(1)`.

## Сравнительная таблица

### List операции

| Коллекция | **add**() | **get**() | **remove**() | **contains**() | **indexOf**() |
|-----------|-------|-------|-----------|------------|-----------|
| **ArrayList** | `O(1)` | `O(1)` | `O(n)` | `O(n)` | `O(n)` |
| **LinkedList** | `O(1)` | `O(n)` | `O(n)` | `O(n)` | `O(n)` |
| **CopyOnWriteArrayList** | `O(n)` | `O(1)` | `O(n)` | `O(n)` | `O(n)` |

### Map операции

| Коллекция | **put**() | **get**() | **remove**() | **containsKey**() |
|-----------|-------|-------|-----------|----------------|
| **HashMap** | `O(1)` | `O(1)` | `O(1)` | `O(1)` |
| **LinkedHashMap** | `O(1)` | `O(1)` | `O(1)` | `O(1)` |
| **TreeMap** | `O(**log n**)` | `O(**log n**)` | `O(**log n**)` | `O(**log n**)` |
| **ConcurrentHashMap** | `O(1)` | `O(1)` | `O(1)` | `O(1)` |

### Set операции

| Коллекция | **add**() | **remove**() | **contains**() |
|-----------|-------|-----------|------------|
| **HashSet** | `O(1)` | `O(1)` | `O(1)` |
| **LinkedHashSet** | `O(1)` | `O(1)` | `O(1)` |
| **TreeSet** | `O(**log n**)` | `O(**log n**)` | `O(**log n**)` |
| **CopyOnWriteArraySet** | `O(n)` | `O(n)` | `O(n)` |

## Рекомендации по выбору коллекции

### Когда использовать ArrayList:
- Нужен быстрый доступ по индексу
- Часто выполняются операции **get**()
- Редко вставляются/удаляются элементы в середину

### Когда использовать LinkedList:
- Часто вставляются/удаляются элементы в начале или конце
- Не нужен доступ по индексу
- Важна эффективность операций **add**() и **remove**()

### Когда использовать HashMap:
- Нужен быстрый поиск по ключу
- Порядок элементов не важен
- Нет требований к потокобезопасности

### Когда использовать TreeMap:
- Нужна сортировка по ключам
- Важны операции диапазона
- Готовы пожертвовать производительностью ради упорядоченности

### Когда использовать HashSet:
- Нужна уникальность элементов
- Порядок не важен
- Быстрый поиск элементов

## Лучшие практики

Для доступа по индексу и добавления в конец предпочтителен ArrayList; для частых вставок/удалений в начале — LinkedList. Для быстрого поиска по ключу O(1) — HashMap; для упорядоченного обхода — TreeMap O(log n). Замеры делайте через JMH (Java Microbenchmark Harness), избегая ручных циклов без разогрева JVM. Выбор коллекции основывайте на доминирующих операциях (add/get/remove/contains) и требованиях к порядку и потокобезопасности.

## Решение проблем

| Симптом | Возможная причина | Решение |
|--------|-------------------|---------|
| Медленный get по индексу в списке | Использование LinkedList (get O(n)) | Заменить на ArrayList при частом доступе по индексу |
| Медленная вставка в середину списка | ArrayList add(index, e) O(n) | Рассмотреть LinkedList или пересмотреть структуру данных |
| Нужен порядок итерации по ключам | HashMap не гарантирует порядок | LinkedHashMap (порядок вставки) или TreeMap (сортировка по ключу) |
| Высокая конкуренция при записи | CopyOnWriteArrayList/Set — копирование при каждой записи | Рассмотреть ConcurrentHashMap, очереди или разбиение по сегментам |

## Частые вопросы

**ArrayList или LinkedList?** В большинстве случаев ArrayList: кэш-дружественность и O(1) доступ по индексу. LinkedList — когда доминируют вставки/удаления в начале (очередь) или нужна структура без копирования при вставке в середину (редко выгодно по суммарному времени).

**Когда использовать TreeMap вместо HashMap?** Когда нужен упорядоченный обход по ключам, операции subMap/headMap/tailMap или предсказуемый порядок; готовы к O(log n) вместо O(1) на операции.

**Как правильно замерять производительность коллекций?** Использовать JMH с разогревом JVM, фиксированным размером данных и изоляцией теста; не полагаться на один прогон или ручной цикл.

## Резюме

В этой статье представлена временная сложность наиболее распространенных реализаций структур данных **Java**.

Мы видели реальную производительность каждого типа коллекции во время выполнения тестов производительности **JVM**. Мы также сравнили производительность одних и тех же операций в разных коллекциях. В результате мы научились выбирать правильную коллекцию, соответствующую нашим потребностям.

**Ключевые выводы:**
- **ArrayList** и **HashMap** обеспечивают лучшую производительность для большинства операций
- **LinkedList** эффективен для операций вставки/удаления в начале/конце
- **TreeMap** и **TreeSet** обеспечивают упорядоченность за счет производительности
- **CopyOnWriteArrayList** и **CopyOnWriteArraySet** дороги для операций записи, но безопасны для чтения
- Выбор коллекции должен основываться на конкретных требованиях к производительности

## Реализация на Kotlin

### List Collections

#### ArrayList

```kotlin
fun arrayListOperationsK() {
    val list = mutableListOf<Int>()
    
    // O(1) - добавление в конец
    list.add(1)
    list.add(2)
    
    // O(1) - получение по индексу
    val element = list[0]
    
    // O(n) - вставка в середину
    list.add(1, 3)
    
    // O(n) - поиск элемента
    val contains = list.contains(2)
    
    // O(n) - удаление элемента
    list.remove(2)
}
```

#### LinkedList

```kotlin
import java.util.LinkedList

fun linkedListOperationsK() {
    val list = LinkedList<Int>()
    
    // O(1) - добавление в начало/конец
    list.addFirst(1)
    list.addLast(2)
    
    // O(1) - получение первого/последнего
    val first = list.first
    val last = list.last
    
    // O(1) - удаление первого/последнего
    list.removeFirst()
    list.removeLast()
}
```

### Map Collections

#### HashMap

```kotlin
fun hashMapOperationsK() {
    val map = hashMapOf<String, Int>()
    
    // O(1) - вставка
    map["key1"] = 1
    map["key2"] = 2
    
    // O(1) - получение
    val value = map["key1"]
    
    // O(1) - удаление
    map.remove("key1")
    
    // O(1) - проверка наличия ключа
    val contains = map.containsKey("key2")
}
```

#### TreeMap

```kotlin
import java.util.TreeMap

fun treeMapOperationsK() {
    val map = TreeMap<String, Int>()
    
    // O(log n) - вставка
    map["key1"] = 1
    map["key2"] = 2
    
    // O(log n) - получение
    val value = map["key1"]
    
    // O(log n) - удаление
    map.remove("key1")
}
```

### Set Collections

#### HashSet

```kotlin
fun hashSetOperationsK() {
    val set = hashSetOf<Int>()
    
    // O(1) - добавление
    set.add(1)
    set.add(2)
    
    // O(1) - проверка наличия
    val contains = set.contains(1)
    
    // O(1) - удаление
    set.remove(1)
}
```

#### TreeSet

```kotlin
import java.util.TreeSet

fun treeSetOperationsK() {
    val set = TreeSet<Int>()
    
    // O(log n) - добавление
    set.add(1)
    set.add(2)
    
    // O(log n) - проверка наличия
    val contains = set.contains(1)
    
    // O(log n) - удаление
    set.remove(1)
}
```
