---
title: "Задача коммивояжера (Traveling Salesman Problem)"
description: "Кратко: задача коммивояжера (TSP) ищет кратчайший маршрут, проходящий через все города ровно один раз и возвращающийся в исходный. Здесь показан эвристический подход имитации отжига: он не гарантирует оптимум, но способен находить хорошие маршруты за разумное время на больших гра"
tags:
  - algorithms
  - problems
  - traveling-salesman-problem
difficulty: "intermediate"
prerequisites: []
next: []
updated: "2026-04-20"
---
# Задача коммивояжера (Traveling Salesman Problem)

Кратко: задача коммивояжера (`TSP`) ищет кратчайший маршрут, проходящий через все города ровно один раз и возвращающийся в исходный. Здесь показан эвристический подход имитации отжига: он не гарантирует оптимум, но способен находить хорошие маршруты за разумное время на больших графах.

## Полезные ссылки

### Официальная документация
- [Baeldung: Simulated Annealing](https://www.baeldung.com/)

### См. также
- [[knapsack-problem|Задача о рюкзаке]] — **knapsack**
- [[a-star-pathfinding|Поиск пути A*]] — **A\*** pathfinding
- [[dijkstra|Алгоритм Дейкстры]] — **Dijkstra**

## Содержание

- [Описание алгоритма](#описание-алгоритма)
  - [Формулировка задачи](#формулировка-задачи)
- [Алгоритм имитации отжига](#алгоритм-имитации-отжига)
  - [Механизм работы](#механизм-работы)
  - [Параметры алгоритма](#параметры-алгоритма)
- [Модели данных](#модели-данных)
- [Реализация на Java](#реализация-на-java)
  - [Реализация](#реализация)
  - [Объяснение алгоритма](#объяснение-алгоритма)
  - [Пример использования (Java)](#пример-использования-java)
- [Реализация на Kotlin](#реализация-на-kotlin)
  - [Классы City и Travel](#классы-city-и-travel)
  - [Алгоритм имитации отжига](#алгоритм-имитации-отжига-1)
  - [Пример использования (Kotlin)](#пример-использования-kotlin)
- [Параметры алгоритма](#параметры-алгоритма-1)
  - [Рекомендуемые значения](#рекомендуемые-значения)
- [Решение проблем](#решение-проблем)
- [Частые вопросы](#частые-вопросы)
- [Глоссарий](#глоссарий)
- [Дополнительные примеры использования](#дополнительные-примеры-использования)
  - [Пример 1: 2-opt как соседство](#пример-1-2-opt-как-соседство)
  - [Пример 2: Матрица расстояний](#пример-2-матрица-расстояний)
  - [Пример 3: Остановка по времени](#пример-3-остановка-по-времени)
  - [Пример 4: Быстрый расчёт дистанции](#пример-4-быстрый-расчёт-дистанции)
- [Заключение](#заключение)
  - [Временная сложность](#временная-сложность)
  - [Пространственная сложность](#пространственная-сложность)
- [Особенности](#особенности)
- [Применение](#применение)
- [Варианты задачи](#варианты-задачи)
  - [Вариант 1: TSP с ограничениями](#вариант-1-tsp-с-ограничениями)
  - [Вариант 2: TSP с временными окнами](#вариант-2-tsp-с-временными-окнами)
- [Когда использовать](#когда-использовать)
- [Лучшие практики](#лучшие-практики)
- [Тестирование](#тестирование)
  - [JUnit](#junit)
  - [Kotlin](#kotlin)
  - [Property-based (kotest)](#property-based-kotest)
- [Заключение](#заключение-1)

## Описание алгоритма

Имитация отжига — стохастическая эвристика, вдохновлённая процессом охлаждения металла: при высокой температуре система свободно «прыгает» между состояниями, а по мере охлаждения всё реже принимает ухудшающие решения. Для `TSP` это означает: мы постепенно уменьшаем вероятность принятия маршрутов длиннее текущего лучшего, чтобы выйти из локальных минимумов и приблизиться к глобальному.

### Формулировка задачи

Коммивояжер должен посетить `n` городов, начиная и заканчивая в одном и том же городе, при этом каждый город посещается ровно один раз. Цель — найти маршрут с минимальной суммарной длиной (или временем, стоимостью и т.п.).

## Алгоритм имитации отжига

Алгоритм имитации отжига — это эвристика для решения задач с большим пространством поиска.

Вдохновение и название пришли из отжига в металлургии; это метод, который включает нагрев и контролируемое охлаждение материала.

В общем, имитация отжига снижает вероятность принятия худших решений, поскольку исследует пространство решений и снижает температуру системы.

### Механизм работы

Алгоритм использует более широкий диапазон решений при высокой температуре системы, ища глобальный оптимум. При понижении температуры диапазон поиска становится меньше, пока не будет найден глобальный оптимум.

### Параметры алгоритма

**Алгоритм имеет несколько параметров для работы:**

1. **Количество итераций** — условие остановки для моделирования
2. **Начальная температура** — начальная энергия системы
3. **Параметр скорости охлаждения** — процент, на который мы снижаем температуру системы
4. **Минимальная температура** — опциональное условие остановки
5. **Время симуляции** — необязательное условие остановки

Значения этих параметров должны быть тщательно выбраны, так как они могут иметь существенное влияние на производительность процесса.

## Модели данных

**Чтобы решить задачу **TSP**, нам понадобятся два класса моделей, а именно **City** и **Travel**. В первом мы будем хранить координаты узлов графа:**

```java
// Город (узел TSP) с координатами и методом расстояния до другого города
public class City {
    private final int x; // координата X
    private final int y; // координата Y

    // Случайное расположение города в квадрате 0..500
    public City() {
        this((int) (Math.random() * 500), (int) (Math.random() * 500));
    }

    // Явно заданные координаты
    public City(int x, int y) {
        this.x = x;
        this.y = y;
    }

    public int getX() {
        return x;
    }

    public int getY() {
        return y;
    }

    // Эвклидово расстояние до другого города
    public double distanceToCity(City city) {
        int dx = Math.abs(x - city.getX());
        int dy = Math.abs(y - city.getY());
        return Math.sqrt(dx * dx + dy * dy);
    }
}
```

Конструктор класса **City** позволяет нам создавать случайные расположения городов. Логика `distanceToCity()` отвечает за расчеты расстояния между городами.

**Следующий код отвечает за моделирование тура коммивояжера. Начнем с генерации начального порядка городов в путешествии:**

```java
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

// Маршрут коммивояжера: последовательность городов и операции над ним
public class Travel {
    private List<City> travel = new ArrayList<>();       // текущий маршрут
    private List<City> previousTravel = new ArrayList<>(); // маршрут до последней перестановки

    public Travel(int numberOfCities) {
        for (int i = 0; i < numberOfCities; i++) {
            travel.add(new City()); // создаём случайные города
        }
    }

    // Генерация начального случайного маршрута
    public void generateInitialTravel() {
        if (travel.isEmpty()) {
            for (int i = 0; i < 10; i++) {
                travel.add(new City());
            }
        }
        Collections.shuffle(travel); // перемешиваем порядок городов
    }

    // Меняем местами две случайные точки маршрута (кандидат-сосед)
    public void swapCities() {
        int a = generateRandomIndex();
        int b = generateRandomIndex();

        previousTravel = new ArrayList<>(travel); // сохраняем копию для возможного отката

        City first = travel.get(a);
        City second = travel.get(b);

        travel.set(a, second);
        travel.set(b, first);
    }

    // Откат последней перестановки, если шаг не принят
    public void revertSwap() {
        travel = previousTravel;
    }

    private int generateRandomIndex() {
        return (int) (Math.random() * travel.size());
    }

    public City getCity(int index) {
        return travel.get(index);
    }

    // Полная длина маршрута с возвратом в стартовый город
    public int getDistance() {
        int distance = 0;
        for (int index = 0; index < travel.size(); index++) {
            City start = getCity(index);
            City destination = (index + 1 < travel.size()) ? getCity(index + 1) : getCity(0);
            distance += start.distanceToCity(destination);
        }
        return distance;
    }
}
```

## Реализация на Java

### Реализация

Теперь давайте сосредоточимся на основной части, реализации алгоритма имитации отжига.

В следующей реализации имитации отжига мы собираемся решить проблему **TSP**. Напомню, что цель состоит в том, чтобы найти кратчайшее расстояние для путешествия по всем городам.

Чтобы запустить процесс, нам нужно указать три основных параметра, а именно: **startTemperature**, **numberOfIterations** и **coolRate**:

```java
// Имитация отжига для TSP: принимает ухудшения с некоторой вероятностью, охлаждая температуру
public class SimulatedAnnealing {
    private final Travel travel;

    public SimulatedAnnealing(Travel travel) {
        this.travel = travel;
    }

    public double simulateAnnealing(
        double startingTemperature,
        int numberOfIterations,
        double coolingRate
    ) {
        double t = startingTemperature;          // текущая температура
        travel.generateInitialTravel();          // стартовый маршрут

        double bestDistance = travel.getDistance(); // текущее лучшее расстояние

        for (int i = 0; i < numberOfIterations; i++) {
            if (t <= 0.1) {                      // минимальная температура: можно завершить
                break;
            }
            travel.swapCities();                 // генерируем соседа маршрута
            double currentDistance = travel.getDistance(); // длина нового маршрута

            if (currentDistance < bestDistance) {
                bestDistance = currentDistance;  // улучшаем лучшее решение
            } else {
                // Вероятностно принимаем ухудшение по Больцману
                double acceptChance = Math.exp((bestDistance - currentDistance) / t);
                if (acceptChance < Math.random()) {
                    travel.revertSwap();         // откатываем, если не приняли
                }
            }
            t *= coolingRate;                    // охлаждаем температуру
        }
        return bestDistance;
    }
}
```

### Объяснение алгоритма

Перед началом симуляции мы генерируем начальный (случайный) порядок городов и вычисляем общее расстояние для проезда. Поскольку это первое рассчитанное расстояние, мы сохраняем его в переменной `bestDistance` вместе с `currentSolution`.

На каждом шаге моделирования мы случайным образом меняем местами два города в порядке следования.

Кроме того, мы вычисляем `currentDistance`. Если вновь вычисленное значение `currentDistance` меньше значения `bestDistance`, мы сохраняем его как лучшее.

В противном случае мы проверяем, не меньше ли функция распределения вероятностей Больцмана, чем случайно выбранное значение в диапазоне от 0 до 1. Если да, то отменяем обмен городами. Если нет, сохраняем новый порядок городов, так как это может помочь нам избежать локальных минимумов.

Наконец, на каждом этапе симуляции мы уменьшаем температуру на заданную скорость охлаждения.

### Пример использования (Java)

```java
Travel travel = new Travel(10);
SimulatedAnnealing sa = new SimulatedAnnealing(travel);

double bestDistance = sa.simulateAnnealing(10000, 1000, 0.9995);
System.out.println("Best distance: " + bestDistance);
```

## Реализация на Kotlin

### Классы City и Travel

```kotlin
// Город в TSP: координаты и метод расстояния
data class CityK(val x: Int, val y: Int) {
    constructor() : this((Math.random() * 500).toInt(), (Math.random() * 500).toInt()) // случайные координаты

    fun distanceToCity(city: CityK): Double {
        val dx = (x - city.x).absoluteValue
        val dy = (y - city.y).absoluteValue
        return kotlin.math.sqrt((dx * dx + dy * dy).toDouble()) // Эвклидово расстояние
    }
}

// Маршрут и операции над ним
class TravelK(private val numberOfCities: Int) {
    private val travel = mutableListOf<CityK>()          // текущий маршрут
    private var previousTravel = mutableListOf<CityK>()  // предыдущий маршрут для отката

    init {
        repeat(numberOfCities) { travel.add(CityK()) }   // создаём случайные города
    }

    // Случайный стартовый маршрут
    fun generateInitialTravel() {
        if (travel.isEmpty()) {
            repeat(10) { travel.add(CityK()) }
        }
        travel.shuffle()
    }

    // Перестановка двух случайных городов
    fun swapCities() {
        val a = (Math.random() * travel.size).toInt()
        val b = (Math.random() * travel.size).toInt()
        previousTravel = ArrayList(travel)               // сохраняем для отката
        Collections.swap(travel, a, b)                   // меняем местами
    }

    // Откат перестановки
    fun revertSwap() {
        travel.clear()
        travel.addAll(previousTravel)
    }

    fun getCity(index: Int): CityK = travel[index]

    // Полная длина маршрута с возвратом в исходную точку
    fun getDistance(): Double {
        var distance = 0.0
        for (index in travel.indices) {
            val start = getCity(index)
            val dest = if (index + 1 < travel.size) getCity(index + 1) else getCity(0)
            distance += start.distanceToCity(dest)
        }
        return distance
    }
}
```

### Алгоритм имитации отжига

```kotlin
// Имитация отжига для TSP в Kotlin
class SimulatedAnnealingK(private val travel: TravelK) {
    fun simulateAnnealing(
        startingTemperature: Double,
        numberOfIterations: Int,
        coolingRate: Double
    ): Double {
        var t = startingTemperature           // текущая температура
        travel.generateInitialTravel()        // стартовый маршрут

        var bestDistance = travel.getDistance() // лучшее расстояние

        repeat(numberOfIterations) {
            if (t <= 0.1) return bestDistance // ранний выход при низкой температуре

            travel.swapCities()               // генерируем сосед
            val currentDistance = travel.getDistance() // измеряем длину

            if (currentDistance < bestDistance) {
                bestDistance = currentDistance // улучшение найдено
            } else {
                val acceptChance = kotlin.math.exp((bestDistance - currentDistance) / t)
                if (acceptChance < Math.random()) {
                    travel.revertSwap()        // откатываем ухудшение
                }
            }
            t *= coolingRate                   // охлаждаем
        }
        return bestDistance
    }
}
```

### Пример использования (Kotlin)

```kotlin
fun main() {
    val travel = TravelK(10)
    val sa = SimulatedAnnealingK(travel)

    val bestDistance = sa.simulateAnnealing(10000.0, 1000, 0.9995)
    println("Best distance: $bestDistance")
}
```

## Параметры алгоритма

**Обратите внимание на несколько советов о том, как выбрать наилучшие параметры моделирования:**

1. **Для небольших пространств решения:** лучше понизить начальную температуру и увеличить скорость охлаждения, так как это сократит время моделирования без потери качества

2. **Для больших пространств решения:** выберите более высокую начальную температуру и меньшую скорость охлаждения, так как будет больше локальных минимумов

3. **Всегда предоставляйте достаточно времени:** для имитации перехода от высокой к низкой температуре системы

Не забудьте потратить некоторое время на настройку алгоритма с меньшим экземпляром задачи, прежде чем приступить к основному моделированию, так как это улучшит окончательные результаты.

### Рекомендуемые значения

- **Начальная температура:** 1000-10000 (зависит от размера задачи)
- **Количество итераций:** 1000-10000
- **Скорость охлаждения:** 0.95-0.999
- **Минимальная температура:** `0.1`

## Решение проблем

- Результат хуже ожидаемого: снизьте скорость охлаждения, увеличьте число итераций, используйте более «сильный» оператор соседства (2-opt).
- Алгоритм застревает на плато: на ранних итерациях увеличьте вероятность принятия худших решений (выше стартовая температура).
- Переполнение стека/памяти: избегайте хранения истории маршрутов; держите только текущее и предыдущее состояние.
- Слишком долгие прогоны: добавьте таймаут или остановку по температуре; оптимизируйте вычисление расстояний (кешируйте матрицу расстояний).
- Нестабильные результаты: фиксируйте seed при отладке, логируйте параметры каждого прогона.

## Частые вопросы

**Нужен ли точный оптимум?** Имитация отжига — эвристика, оптимум не гарантирован; используйте точные методы для малых графов.
**Можно ли комбинировать с 2-opt?** Да, 2-opt как оператор соседства обычно улучшает качество и скорость сходимости.
**Как выбрать температуру?** Берите стартовую температуру такой, чтобы на первых шагах принималось ~80–90% ухудшений; дальше регулируйте cooling rate.
**Работает ли с ограничениями (time windows, forbidden edges)?** Да, но оператор соседства и проверка валидности должны учитывать ограничения, иначе маршрут будет недопустимым.
**Что делать с большими графами (1000+ городов)?** Уменьшайте число итераций, используйте агрессивное охлаждение, ограничивайте соседство (локальный swap), или переходите к гибридам (SA + 2-opt/lin-kernighan).

## Глоссарий

`Simulated Annealing` — имитация отжига, стохастическая эвристика оптимизации.
`Cooling schedule` — правило уменьшения температуры (`t *= coolingRate`).
`Neighbor` — соседнее решение, например перестановка двух городов или 2-opt.
`Acceptance probability` — вероятность принять ухудшение, обычно `exp((best — current)/t)`.
`2-opt` — локальный оператор, переворачивающий подотрезок маршрута для сокращения длины.

## Дополнительные примеры использования

### Пример 1: 2-opt как соседство

```java
// 2-opt: разворот сегмента между индексами i и j
public void twoOptSwap(int i, int j) {
    previousTravel = new ArrayList<>(travel); // сохраняем для отката
    while (i < j) {
        Collections.swap(travel, i, j);
        i++; j--;
    }
}
```

### Пример 2: Матрица расстояний

```java
// Предварительный расчёт матрицы расстояний для ускорения
double[][] buildDistanceMatrix(List<City> cities) {
    int n = cities.size();
    double[][] dist = new double[n][n];
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            dist[i][j] = cities.get(i).distanceToCity(cities.get(j));
        }
    }
    return dist;
}
```

### Пример 3: Остановка по времени

```java
// Прерываем, если превысили лимит времени
public double simulateWithTimeout(double startT, int maxIter, double coolingRate, long timeoutMs) {
    long end = System.currentTimeMillis() + timeoutMs;
    double t = startT;
    double best = travel.getDistance();
    for (int i = 0; i < maxIter && System.currentTimeMillis() < end; i++) {
        if (t <= 0.1) break;
        travel.swapCities();
        double current = travel.getDistance();
        if (current < best) {
            best = current;
        } else {
            double accept = Math.exp((best - current) / t);
            if (accept < Math.random()) travel.revertSwap();
        }
        t *= coolingRate;
    }
    return best;
}
```

### Пример 4: Быстрый расчёт дистанции

```java
// При использовании матрицы расстояний расстояние маршрута считается без Math.sqrt
int routeLength(List<Integer> route, int[][] dist) {
    int total = 0;
    for (int i = 0; i < route.size(); i++) {
        int from = route.get(i);
        int to = route.get((i + 1) % route.size());
        total += dist[from][to];
    }
    return total;
}
```

## Заключение

Имитация отжига даёт баланс между качеством решения и затратами на вычисления для `TSP`, когда точные методы слишком дороги. Ключ к хорошему результату — грамотный выбор параметров охлаждения, оператор соседства (2-opt, swap, вставка) и валидация на малых графах перед масштабированием. Поддерживайте воспроизводимость (seed), измеряйте качество на эталонных задачах и не забывайте про тесты и наблюдаемость — это снижает риск регрессий при доработках.

Для дальнейшего чтения: изучите «медиана медиан» для детерминированного выбора, гибриды `SA + 2-opt/lin-kernighan`, а также генетические алгоритмы и табу-поиск — они хорошо комбинируются с имитацией отжига в задачах маршрутизации.

### Временная сложность

На каждой итерации мы считаем длину маршрута за `O(n)`, где `n` — число городов. При `k` итерациях итоговая сложность `O(k · n)`. В реальности стоимость может быть выше, если сосед генерируется не простым обменом, а более дорогими оператором (например, 2-opt).

### Пространственная сложность

Основная память уходит на хранение маршрута `O(n)` и его копии для отката `O(n)`. Дополнительные структуры (температура, параметры) занимают `O(1)`. Если нужно логировать историю, это увеличит память до `O(k)`, что нужно оговаривать отдельно.

## Особенности

Имитация отжига — эвристика: она не гарантирует оптимальный ответ, но предоставляет управляемый компромисс между временем и качеством. Метод гибко настраивается параметрами охлаждения и оператором соседства (перестановки, 2-opt и т.п.), что позволяет адаптировать его под разные варианты `TSP` и смежные задачи маршрутизации.

## Применение

`TSP` встречается в логистике (планирование доставок), производстве (маршруты сверления/резки), сетевых задачах (обход узлов, роуминг), биоинформатике (анализ последовательностей), а также в аналитических задачах, где нужно найти короткий обход набора точек. Имитация отжига применима там, где точное решение слишком дорого, а хорошее приближение устраивает бизнес.

## Варианты задачи

### Вариант 1: TSP с ограничениями

```java
public class ConstrainedTSP extends Travel {
    private Map<City, List<City>> constraints;

    public boolean isValidRoute() {
        for (int i = 0; i < travel.size(); i++) {
            City current = travel.get(i);
            City next = (i + 1 < travel.size()) ?
                travel.get(i + 1) : travel.get(0);

            if (constraints.containsKey(current)) {
                if (!constraints.get(current).contains(next)) {
                    return false;
                }
            }
        }
        return true;
    }
}
```

### Вариант 2: TSP с временными окнами

```java
public class TSPWithTimeWindows extends Travel {
    private Map<City, TimeWindow> timeWindows;

    public double getTotalTime() {
        double time = 0;
        for (int i = 0; i < travel.size(); i++) {
            City current = travel.get(i);
            City next = (i + 1 < travel.size()) ?
                travel.get(i + 1) : travel.get(0);

            time += current.distanceToCity(next);
            time += getServiceTime(current);
        }
        return time;
    }
}
```

## Когда использовать

Имитация отжига стоит применять, когда пространство решений велико и точные методы (полный перебор, динамика) неподъёмны, а бизнес готов принять хорошее приближение. Алгоритм даёт ценность, если есть время на подбор параметров и на прогоны с разными `seed`. Не выбирайте его, если требуется гарантированный оптимум, жёсткие SLA по времени или задача мала настолько, что полная оптимизация возможна быстрее.

## Лучшие практики

- **Параметры отжига.** Подбирайте начальную температуру и скорость охлаждения на малых графах. Экспоненциальное охлаждение обычно стабильнее линейного, но проверяйте на своих данных.
- **Оператор соседства.** Простая перестановка двух городов даёт медленную сходимость; 2-opt или вставка сегмента ускоряют поиск, хотя дороже по шагу.
- **Критерий останова.** Комбинируйте минимальную температуру, число итераций и таймаут по времени; фиксируйте это в контракте, чтобы повторять результаты.
- **Рандомизация и seed.** Для отладки фиксируйте `seed`; для продакшена используйте системный источник, но логируйте seed проблемных прогона.
- **Сравнение.** На маленьких графах сверяйтесь с точным решением (например, ветвление и отсечение); на больших сравнивайте с жадным и генетическим алгоритмом, чтобы оценить качество приближения.

## Тестирование

### JUnit

```java
@Test
void simulatedAnnealing_improvesRouteOnSmallGraph() {
    Travel travel = new Travel(5);
    SimulatedAnnealing sa = new SimulatedAnnealing(travel);

    double initial = travel.getDistance(); // исходный маршрут
    double best = sa.simulateAnnealing(5000, 500, 0.995); // запускаем отжиг

    assertTrue(best <= initial); // ожидаем не хуже исходного
}
```

### Kotlin

```kotlin
@Test
fun `annealing improves small route`() {
    val travel = TravelK(5)
    val sa = SimulatedAnnealingK(travel)

    val initial = travel.getDistance()
    val best = sa.simulateAnnealing(5000.0, 500, 0.995)

    assertTrue(best <= initial)
}
```

### Property-based (kotest)

```kotlin
@Property
fun `distance never negative`(@ForAll @IntRange(min = 3, max = 8) n: Int) {
    val travel = TravelK(n)
    travel.generateInitialTravel()
    val distance = travel.getDistance()
    assertTrue(distance >= 0.0)
}
```

## Заключение

В этом кратком руководстве мы смогли узнать об алгоритме имитации отжига и решили задачу коммивояжера. Надеюсь, это показывает, насколько удобен этот простой алгоритм применительно к определенным типам задач оптимизации.
